# WORKFLOW.md — Aionis operating constitution

> Preserved verbatim-as-constitution from the owner's 2026-07-30 directive on
> re-standardizing the AI workflow. This file is the long-form operating manual; it is
> **referenced** from `CLAUDE.md` (not `@import`-ed) so it loads on-demand and does not
> consume every-session context. Do not paraphrase this content into other files — link
> here (§13: avoid re-expressing fixed rules differently across agents).
>
> Citations (Anthropic / arXiv / Google) are retained as the owner provided them.

## 总体架构（统一工作流）

```
机会发现 → 问题定义 → 证据研究 → 决策门禁 → 规格冻结 → 任务切片
       → Agent 执行 → 自动验证 → 差异审查 → 知识沉淀 → 阶段决策
```

## 一、统一项目目录

每一个项目（软件、科研、游戏）采用同一套基本目录：

```
project/
├─ README.md
├─ AGENTS.md            # Aionis 注：本项目运行 Claude Code，规范入口是 CLAUDE.md；
│                       #   AGENTS.md 为指向 CLAUDE.md 的符号链接（Codex 兼容）。
├─ docs/
│  ├─ 00-vision.md
│  ├─ 01-problem.md
│  ├─ 02-research.md
│  ├─ 03-prd.md         # Aionis 用 03-spec.md（phase 预注册索引）
│  ├─ 04-architecture.md
│  ├─ 05-acceptance.md
│  ├─ 06-risks.md
│  ├─ 07-roadmap.md
│  └─ 08-lessons.md
├─ decisions/
│  ├─ ADR-001-xxx.md
│  ├─ ADR-002-xxx.md
│  └─ index.md
├─ state/
│  ├─ current.md
│  ├─ backlog.md
│  ├─ blockers.md
│  └─ handoff.md
├─ tasks/
│  ├─ active/
│  ├─ completed/
│  └─ rejected/
├─ evals/
│  ├─ cases/
│  ├─ expected/
│  ├─ results/
│  └─ regression/
├─ reports/
│  ├─ milestone/
│  ├─ audits/
│  └─ cost/
└─ archive/
```

**文件职责必须固定。**

### AGENTS.md / CLAUDE.md（所有 Agent 的总入口）
只保存稳定规则：项目目标 / 不可违反的约束 / 技术栈 / 目录说明 / 开发命令 / 测试命令 /
提交规则 / Agent 权限边界 / 完成定义 / 禁止事项。**不要放当前任务，不要放聊天总结，
不要不断膨胀。** （Aionis：Claude Code 原生读取 `CLAUDE.md`；本节稳定规则落在 `CLAUDE.md`。）

### state/current.md（每次任务首先读取）
只包含：当前版本 / 当前里程碑 / 已完成事项 / 当前任务 / 下一步 / 已知问题 / 最近验证结果 /
最后更新时间。控制在约 500～1200 个中文字符。

### state/handoff.md（长任务中断 / 跨上下文恢复）
包含：本轮做了什么 / 修改了哪些文件 / 哪些测试通过 / 哪些测试失败 / 当前假设 / 下一步精确动作 /
禁止重复执行的操作。

> Anthropic 对长周期 Agent 的研究强调，跨上下文稳定推进的关键不是保存完整聊天，
> 而是建立明确的初始化状态、进度记录、可运行环境和下一步交接。否则新上下文中的 Agent
> 很容易重复探索、误判完成度或破坏已有成果。

### decisions/ADR-xxx.md（每项重要决策只记录一次）
字段：标题 / 日期 / 状态（提议·接受·废弃·替代）/ 背景 / 候选方案 / 采用方案 / 证据 / 代价 /
适用边界 / 重新评估条件。

## 二、阶段一：机会发现

任何新想法都不能立即进入开发。先创建一张 **Opportunity Card**：

```
名称：
用户是谁：
用户现在如何解决：
核心痛点：
现有方案为什么不够：
我们能提供的独特价值：
最小验证方式：
验证成本：
最大风险：
成功后下一步：
失败后损失：
```

该阶段只回答三个问题：这个问题真实存在吗？值得由你解决吗？是否存在低成本验证路径？

- **输出**：`docs/01-problem.md`
- **停止条件**（任一成立立即停止）：无法指出具体用户；痛点只是个人想象；没有可观测的
  成功信号；验证成本超过当前承受能力；已有方案明显更优且没有差异化空间。
- 此阶段禁止讨论完整架构、技术栈和复杂功能。

## 三、阶段二：研究问题定义

研究开始前，必须把模糊主题改写成可回答的问题。

- 错误形式：研究 AI Agent / 研究游戏市场 / 研究世界模型。
- 正确形式：*在个人开发者、低预算、低算力条件下，什么样的 Agent 编排结构能够降低长任务
  失败率，同时不显著增加 Token 成本？*

一个完整研究问题必须包含：对象 / 目标 / 约束 / 比较对象 / 评价指标 / 时间范围 / 证据标准。

```
主问题：
子问题：
  1.
  2.
  3.
当前假设：
可能推翻假设的证据：
评价标准：
研究边界：
本轮不研究：
```

最重要的一项是：**可能推翻假设的证据。** 这样研究目标才是寻找事实，而不是让 AI 帮你
证明原先的想法。

## 四、阶段三：证据研究流水线（固定六步）

1. **建立证据层级**（按可信度）：A 级原始论文/官方文档/源代码/真实数据/财报/法规；
   B 级高质量复现实验/系统综述/权威机构报告；C 级专业媒体/工程团队复盘；
   D 级个人博客/论坛/社交平台。结论不得只依赖 D 级材料。
2. **先宽搜，再深读**。第一轮只建立地图（主要概念 / 关键作者或机构 / 主流路线 / 争议点 /
   代表性论文 / 常用评价指标）；第二轮才读取最关键的 5～15 个来源。不要让 Agent 一开始
   就无限搜索。
3. **建立证据表**：

   | 主张 | 支持证据 | 反对证据 | 来源级别 | 适用条件 | 可信度 |
   |------|----------|----------|----------|----------|--------|

4. **单独运行反方 Agent**：不负责给新方案，只负责寻找反例 / 寻找隐含假设 / 寻找数据缺口 /
   判断因果是否被误当成相关 / 判断结论能否迁移到个人开发环境。
5. **区分事实、推断和建议**，文档中必须标记：`[F]` Fact / `[I]` Inference / `[H]` Hypothesis /
   `[D]` Decision。
6. **生成研究结论**，固定为：执行摘要 / 研究问题 / 证据概览 / 已确认事实 / 主要争议 / 反方观点 /
   对当前项目的适用性 / 尚未解决的问题 / 建议决策 / 置信度 / 引用。

## 五、阶段四：决策门禁

研究完成后，不直接进入开发。必须进行一次明确决策：**GO / CONDITIONAL GO / HOLD / PIVOT / STOP**。

**决策评分**（每项 1～5 分）：问题真实性 / 用户价值 / 差异化 / 技术可行性 / 个人资源匹配度 /
验证成本 / 长期复用价值 / 风险可控性。总分不是自动结论，只用于暴露分歧。

**决策文档必须写清楚**：决定是什么 / 为什么现在决定 / 依据是什么 / 放弃了哪些方案 /
我们可能错在哪里 / 什么新证据会触发复审 / 下次复审日期或事件。

> 这一步能够避免"同一问题反复讨论"。以后除非出现触发条件，不再重新研究已经确定的决策。

## 六、阶段五：规格冻结

决定继续后，建立规格，而不是直接写 Prompt。规格由五部分组成：

1. **Vision** — 为什么存在 / 为谁服务 / 最终改变什么 / 不做什么。
2. **PRD** — 用户场景 / 核心流程 / 功能需求 / 非功能需求 / 优先级 / 边界条件 / 失败体验 / 数据要求。
3. **Architecture** — 系统边界 / 模块 / 数据流 / 接口 / 依赖 / 安全边界 / 性能要求 / 部署方式 / 故障处理。
4. **Acceptance Criteria** — 每项功能必须可检验（Given / When / Then）。
   例：Given Neo4j 中无论文数据；When 用户上传一篇可解析的中文 PDF；Then 后端返回任务 ID →
   任务最终状态 completed → Neo4j 出现 Paper 节点 → 图接口 nodes>0 → 前端展示新增节点 →
   全流程无未处理异常。
5. **Non-goals** — 明确首版不做：不支持什么 / 不优化什么 / 不承诺什么 / 哪些需求延后。

**冻结规则**：进入开发后，规格原则上不再修改。发现新想法时记录到 backlog，不中断当前任务，
里程碑结束后评估。只有以下情况可以中途变更：原规格无法实现 / 出现安全问题 / 核心假设被证伪 /
继续开发会产生明显浪费。

## 七、阶段六：任务切片

Planner Agent 不直接写代码，只负责把规格拆成可验证任务。一个合格任务必须满足：单一目标 /
输入明确 / 输出明确 / 影响范围明确 / 验收方法明确 / 可以独立回滚 / 理想情况下在一个上下文周期内完成。

**任务文件模板**：编号 / 标题 / 目标 / 背景 / 允许修改 / 禁止修改 / 前置条件 / 实施要求 / 验收标准 /
必须运行的测试 / 失败处理 / 预期产物 / 完成后需要更新（state/current.md、state/handoff.md、对应测试结果）。

**任务大小标准**：
- **S** — 单文件或少量文件 / 明确缺陷 / 无架构变更（修一个点击事件、加一个校验、改一个 API 字段）。
- **M** — 一个完整子功能 / 多文件 / 需要单元与集成测试（实现 PDF 解析回退、新增邻居查询接口、实现一套卡牌效果）。
- **L** — 必须继续拆分，禁止直接交给 Agent（完成整个后端、重构整个项目、做完游戏核心玩法、优化所有性能）。

## 八、Agent 结构：单主 Agent + 按需专家

不建立十几个常驻 Agent。采用：

```
Orchestrator
├─ Researcher
├─ Planner
├─ Engineer
├─ Verifier
└─ Reviewer
```

> Anthropic 建议优先使用清晰、可组合的工作流（顺序链 / 路由 / 并行 / 主管—工作者 / 评估—优化）；
> 只有当步骤数量无法预先确定、环境反馈决定下一步时，才值得使用高自主 Agent。

- **Orchestrator** — 读取状态 / 判断当前阶段 / 选择下一个 Agent / 控制预算 / 检查停止条件 /
  更新交接状态。禁止：直接大规模写代码、擅自改变项目方向、绕过质量门禁。
- **Researcher** — 寻找证据 / 维护证据表 / 区分事实与推断 / 输出研究结论。禁止：未经决策直接修改
  产品规格、把搜索结果数量当成研究质量。
- **Planner** — 将规格拆成任务 / 分析依赖 / 确定执行顺序 / 定义验收标准。禁止：直接实现任务、
  把模糊目标交给 Engineer。
- **Engineer** — 读取任务文件 / 检查仓库现状 / 实施最小必要改动 / 运行测试 / 记录证据 / 更新 handoff。
  禁止：扩大任务范围、重写无关模块、修改已冻结约束、用 mock 结果代替真实结果、跳过测试后宣称完成。
- **Verifier** — 独立执行测试 / 核对验收标准 / 验证真实运行状态 / 检查日志和数据 / 给出
  PASS/FAIL/BLOCKED。Verifier 不相信 Engineer 的文字报告，只相信：代码、命令、测试结果、数据库状态、
  真实界面、构建产物、日志。
- **Reviewer** — 审查 Diff / 检查架构一致性 / 检查遗漏和回归 / 判断是否应合并。只读取：任务说明、
  相关规格、Git Diff、测试结果、必要的相邻代码。不重新读取整个项目。

## 九、工作流模式选择

- **模式 A：顺序流水线**（Planner → Engineer → Verifier → Reviewer）—— 默认。需求明确、步骤稳定、
  大多数软件任务。
- **模式 B：并行研究**—— 一个问题需要多个独立视角（论文/工程/竞品/风险），并行 Agent 只提交
  结构化结论、不互相聊天，最后统一综合。
- **模式 C：路由**—— 输入类型差异明显（Bug→Debug、新功能→Planner、架构问题→Architecture Reviewer、
  资料问题→Researcher）。
- **模式 D：评估—修正循环**—— 文案/方案/测试修复/可量化输出：生成 → 评估 → 只修复未达标项 → 再评估，
  最多两轮；两轮后仍失败则停止自动迭代，标记 BLOCKED 交由人判断。
- **模式 E：自主 Agent**—— 仅在以下条件同时成立时使用：无法预先定义完整步骤 / Agent 必须观察环境后
  决定下一步 / 有可靠工具反馈 / 有预算上限 / 有停止条件 / 操作可回滚 / 有人工审批点。否则不用。

## 十、上下文装配规则

每次 Agent 调用，不加载完整历史。统一装配六层：1 稳定规则 / 2 当前角色 / 3 当前任务 /
4 当前状态 / 5 相关证据或代码 / 6 输出格式。

推荐上下文结构：`[稳定规则] AGENTS.md/CLAUDE.md 中与当前任务相关的内容` → `[角色] 你是 Engineer，只完成指定任务`
→ `[当前任务] tasks/active/TASK-023.md` → `[当前状态] state/current.md` →
`[相关资料] 指定架构段落、相关文件、相关测试` → `[输出要求] 修改内容、测试证据、风险、下一步`。

**禁止进入上下文**：全部聊天记录、已失效方案、与当前任务无关的研究、大量原始日志、整个代码仓库、
重复的规则说明、已完成任务全文、未筛选的搜索结果。

> Google 对生产级 Agent 的上下文工程总结为三个核心原则：分层存储信息、只选择当前相关内容、
> 在多 Agent 交接时显式传递所需上下文切片，而不是将共享历史整体传递。

## 十一、Token 管理制度

1. **按阶段分配预算**（不按整个项目给无限额度）：研究/规划/执行/验证/返工各自设预算。
   推荐比例（默认基线，非硬性）：
   - 研究型任务：研究 50% / 综合 20% / 反方审查 20% / 最终整理 10%。
   - 开发型任务：规划 10% / 实现 55% / 验证 20% / Review 10% / 状态更新 5%。
2. **模型分层**：
   - 强推理——战略判断、架构设计、复杂研究综合、难定位 Bug、高风险 Review、关键决策。
   - 中等——任务拆分、普通代码实现、常规审查、测试设计、文档生成。
   - 轻量——日志分类、格式转换、文件归类、状态摘要、重复性修改、简单测试修复、检索结果初筛。
   - 路由按**风险**决定（非任务名）：可逆+易验证→轻量；可逆+中等复杂→中等；不可逆或高影响→强。
3. **上下文软上限**：达到预算约 60%～70% 时开始整理状态（不等窗口快满才压缩）→ 生成
   `state/handoff.md` → 用新上下文继续。
   > 2026 年一项小规模 SWE-bench Lite 实验中，主动压缩在保持任务成功率不变的情况下减少了约
   > 22.7% Token；样本仅 5 个任务，不能直接推广，但支持"在任务边界主动整理状态"。
4. **压缩时保存**：已确认事实 / 已完成动作 / 关键证据 / 当前工作状态 / 失败尝试及原因 / 剩余任务 /
   下一步动作 / 不可破坏约束。**删除**：寒暄、重复解释、已被否定的长篇方案、工具原始输出、无关探索、
   完整思考过程。
5. **不反复总结同一份摘要**——采用增量更新：Stable Facts / Current State / New Learning / Deprecated
   Information。
   > 研究指出反复重写上下文可能造成"上下文坍缩"（摘要越来越短但关键领域知识逐步丢失）。较稳妥的方式
   > 是维护结构化、增量更新的操作手册，而不是不断把旧摘要压成更短的新摘要。

## 十二、缓存与重复输入控制

通过 API 构建 Agent 时，提示词按稳定程度排序：最稳定内容放前面（系统规则 / 项目固定规则 / 工具定义 /
输出结构），动态内容放后面（当前状态 / 当前任务 / 本轮输入）。固定前缀更易被缓存复用。

项目内还要设置版本号：Prompt Version / Spec Version / State Version / Task ID。避免同一固定规则被多个
Agent 以不同表达重复发送（§13 反漂移）。

## 十三、工具设计规则

每个工具必须具备：清晰名称 / 单一职责 / 明确输入 / 结构化输出 / 错误类型 / 是否可重试 / 是否可回滚 /
副作用说明 / 权限要求。

- 错误工具：`manage_project()` / `do_everything()` / `process_data()`。
- 正确工具：`read_task(task_id)` / `run_backend_tests(test_path)` / `query_graph_statistics()` /
  `update_current_state(patch)` / `create_pull_request(branch, title, body)`。

> Anthropic 的工具工程实践指出：工具描述应帮助模型正确选择工具，返回结果应保留足够信息供后续决策，
> 同时避免无关输出占据上下文。

**工具结果必须结构化**，例如：
```json
{ "status": "success", "summary": "后端测试通过",
  "evidence": { "passed": 80, "failed": 0 },
  "artifacts": ["reports/test-2026-07-30.txt"], "next_action": null }
```
不要返回数千行未经筛选的日志。

## 十四、质量保证制度（四层证据）

1. **确定性检查**（优先级最高）：编译 / Lint / 类型检查 / 单元测试 / 集成测试 / 数据库约束 /
   接口 Schema / 性能阈值 / 真实文件测试。
2. **行为验收**：按 Acceptance Criteria 执行真实流程（启动依赖 → 启动后端 → 上传真实数据 →
   等待任务完成 → 检查存储 → 调用接口 → 启动前端 → 确认显示 → 检查控制台 → 保存日志/截图）。
3. **模型评审**（适用于难完全规则化的内容：可读性、产品逻辑、研究论证、架构一致性、交互体验）：
   必须使用评分量表，不允许只问"做得怎么样"。
4. **人工门禁**：项目方向改变 / 核心架构改变 / 高成本支出 / 对外发布 / 删除重要数据 / 合并高风险改动 /
   研究结论转为长期战略——由人工最终批准。

> Agent 评估不能只看最终结果：需覆盖模型、记忆、工具与环境，并观察系统级、轨迹级和单步骤错误；
> 同一任务的成功率、可靠性、恢复能力和资源消耗应分别记录。

## 十五、评估集制度

每个长期项目必须建立自己的 `evals/`（Aionis 的 eval 面映射见 `evals/README.md`：预注册=规格、
`runs/ledger.jsonl`=裁决日志、`tests/`=回归、H6+config_committed=可复现性评估）。

## 十六、Review 制度

Reviewer **只检查变化**。固定输入：任务文件 / 相关规格 / Git Diff / 自动测试结果 / Engineer 的 handoff。

固定输出：
```
结论：APPROVE / REQUEST CHANGES / BLOCKED
必须修复（高风险问题）：
建议改进（非阻塞问题）：
验证缺口：
是否符合任务范围：   是否符合架构：   是否存在回归风险：
```

Reviewer 不得：因个人偏好要求无关重构、重新设计整个系统、把建议项说成阻塞项、无证据否定测试结果、
在 Review 中扩展需求。

**修复轮次**：第 1 轮修复阻塞项 → 第 2 轮验证修复 → 仍未通过则升级给人工决策。禁止无限 Review—修正循环。

## 十七、停止条件

- **Engineer**：任务范围完成 / 验收标准满足 / 指定测试通过 / 状态文档更新 / 没有未说明的失败。
- **Researcher**：主问题已可回答 / 关键观点有证据 / 主要反方观点已覆盖 / 继续搜索的边际价值很低 /
  剩余不确定性已明确。
- **Reviewer**：阻塞问题已列出 / 证据充分 / 没有继续扩大审查范围。
- **自动流程强制停止**：连续两次相同失败 / 连续两轮无有效进展 / 超过 Token 预算 / 需要修改冻结规格 /
  需要不可逆操作 / 测试环境异常 / 证据相互冲突且无法判断。此时输出 **BLOCKED**，而不是继续消耗 Token。

## 十八、每次长任务的标准流程

**启动前必须读取**：`CLAUDE.md`（或 `AGENTS.md`）/ `state/current.md` / `state/handoff.md` /
当前任务文件 / 相关规格。然后：1 检查 Git 状态 → 2 检查运行环境 → 3 运行最小基线测试 →
4 确认当前事实与任务描述一致 → 5 再开始修改。

**执行中**：每完成一个可验证单元 → 修改 → 测试 → 记录 → 再继续。禁止累积大量修改后一次性测试。

**上下文即将过长时**：立即更新 `state/handoff.md` → 提交可恢复检查点 → 记录测试状态 → 结束当前上下文。

**完成后**输出：任务结论 / 修改文件 / 完成内容 / 测试证据 / 未完成内容 / 已知风险 / 是否达到验收标准 /
下一任务建议 / Git 状态。不得只输出一篇叙述性"最终报告"。

## 十九、人工的日常使用节奏

- **每天开始**：只看三个文件——`state/current.md`、`state/blockers.md`、`tasks/active/`。选一个最高优先级任务。
- **每个任务**：读取状态 → 装配最小上下文 → 执行 → 自动验证 → Diff Review → 更新状态。
- **每天结束**：更新（今天完成 / 测试状态 / 遗留风险 / 明天第一动作），控制在一页内。
- **每周一次**项目 Review：本周完成什么 / 哪些结果有证据 / 哪些任务没有产生价值 / Token 花在哪里 /
  哪些 Agent 失败 / 哪些规则需要调整 / 下周唯一核心目标。
- **每个里程碑结束**完整审计：目标是否完成 / 用户价值是否被验证 / 架构是否仍适用 / 质量指标 / 成本 /
  技术债务 / 风险 / 继续·暂停·转向·停止。

本项目应重点建立：任务状态机 / 检查点 / 跨上下文 handoff / 到期条件 / 暂停·恢复机制 / 工具调用证据 /
预测—结果—复盘闭环。长任务不得依赖单次聊天记忆。前期以研究系统为主：
研究问题 → 数据可得性 → 可证伪假设 → 最小实验 → 结果 → 决策。在事件表示语言、因果图和世界模型
尚未经过小实验验证前，不进入大规模系统建设。

## 二十、最终确定的工作原则（十条）

1. 先定义问题，再进行研究。
2. 先获取证据，再形成结论。
3. 先作决策，再投入开发。
4. 先冻结规格，再拆分任务。
5. 一个任务只解决一个可验证目标。
6. 上下文只加载当前需要的信息。
7. Agent 通过状态文件交接，不通过完整聊天交接。
8. 质量由测试和真实运行证据保证，不由文字报告保证。
9. Review 只审查变化，完整审计只在里程碑进行。
10. 所有流程都必须有预算、停止条件和失败出口。

**人工在这套系统中的定位**：不是负责逐项执行的开发者，而是研究负责人、产品负责人和最终决策者。
但人工必须保留三项不可外包的能力：**判断什么问题值得解决 / 判断什么证据值得相信 / 判断什么时候应该停止**。
