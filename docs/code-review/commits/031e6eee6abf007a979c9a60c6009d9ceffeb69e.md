# Commit 031e6eee6abf007a979c9a60c6009d9ceffeb69e 审查报告

**提交信息**: fix(phase-e1): run_confirmatory always saves artifacts (base=None->default)（2026-07-29，Rethymus）
**改动范围**: 1 个文件，+7/-6；`src/aionis/eval/phase_e1.py` 移除 `if results_base is not None` 的 save_run 守卫——`None` 本意是"用默认 runs 目录"，却被解释成"不保存"，导致真实 E1 运行不落产物。

## 四维度结论
| 维度 | 结论 | 一句话说明 |
|---|---|---|
| 稳定性 | ✅ | 修复方向正确：`save_run(base=None)` → `run_dir(sig, None)` 走默认目录（results.py:131 已核实） |
| 可扩展性 | ✅ | 与 phase_c.py:360 / phase_d.py:237 的无条件 save_run 模式对齐 |
| 生产可用 | ✅ | 时机关键：本 fix（11:38）先于首次真实 E1 confirmatory 运行（后续 21d6d3b5），首跑产物得以落盘 |
| 高内聚低耦合 | ✅ | 单点最小修复，无行为外溢 |

## 对照参考
不适用（7 行守卫修复）。语义对账：`save_run(..., base=None)` docstring 明写 "base: override the runs dir (hermetic tests)"，即 None=默认目录——原守卫把"默认值"误当"跳过"，属于典型的哨兵值语义冲突（None 同时承担"未提供"与"不执行"两义）。

## 问题清单
### P3-1 None-默认路径无回归测试
- 描述：`tests/test_phase_e1_run.py:57-83` 的封闭测试恒传 `results_base=tmp_path`——这正是原 bug 对测试不可见的原因（测试永远走显式 base 分支）；修复后 base=None → 默认 runs 目录的路径仍无任何测试锁定。
- 位置：`tests/test_phase_e1_run.py:70`
- 影响范围：同类"None 守卫"回归不会再被测试网捕获。
- 修复建议：补一条 monkeypatch 默认 runs 目录 + `results_base=None` 的断言测试（或参数化 base 两分支）。
- 修复成本：低
- 状态：未修复

## 领域红线核查
- 账本纪律 ✅：E1 config_committed（ledger #35，ts 03:29）已先于本 fix 与首跑入账；本 fix 不触碰 ledger 语义。
- 防泄漏/H6 ✅：无数据面改动。

## 总体评分
9/10 — 时机正确的最小修复，消除 None 双义陷阱；仅欠一条 None 路径回归测试（P3）。
