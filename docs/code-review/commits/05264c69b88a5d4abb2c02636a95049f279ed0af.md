# Commit 05264c69b88a5d4abb2c02636a95049f279ed0af 审查报告

**提交信息**: fix(phase-d): peer_momentum ex-self mean — count not size (pandas>=3.0 NaN bug)（2026-07-29，Rethymus）
**改动范围**: 2 个文件，+29/-2；`src/aionis/features/peer_momentum.py` 一行修复（`transform("size")`→`transform("count")`）+ 详细成因注释 + `tests/test_peer_momentum.py` 增加混合 NaN 组的精确回归测试。

## 四维度结论
| 维度 | 结论 | 一句话说明 |
|---|---|---|
| 稳定性 | ✅ | 修复后 leave-one-out 代数严格精确：mean 与 count 同为非 NaN 口径，(sum−self)/(n−1) 自洽；`.where(gn>1)` 顺带把"<2 个有效 peer"的组正确 NaN 化 |
| 可扩展性 | ✅ | 无签名变化；注释把 pandas 3.0 stack 语义翻转、size/count 差异、以及为何 H6 抓不住（确定性错误非泄漏）全部写进代码 |
| 生产可用 | ✅ | 回归测试给出可手算的量化复现（{0.20, 0.10, NaN} → 0.10 而非 0.125），钉死语义防回退 |
| 高内聚低耦合 | ✅ | 单点修复、无涟漪 |

## 对照参考
与 [pandas 官方 GroupBy 文档](https://pandas.pydata.org/docs/user_guide/groupby.html)（count 只计非 NA、仅无缺失时等于 size）及 [RealPython 指南](https://realpython.com/pandas-groupby/) 的表述逐字吻合；commit 信息对 "DataFrame.stack() 在 pandas≥3.0 不再丢弃 NaN（future_stack 成为默认）" 的判断与 pandas 变更日志一致，uv.lock 在 Python≥3.11 锁 pandas 3.0.5 说明该 bug 在冻结环境内是活跃的，修复必要且及时。出处域名：pandas.pydata.org、realpython.com。

## 问题清单
（通过。两点记录：① 修复只改新代码路径的 `count`，未对 `.stack()` 本身做版本防御——pandas 2.x 下 stack 丢弃 NaN 行、count 与 size 相等，行为一致，无回退风险；② 该特征当时尚未喂进任何已入账的 confirmatory 结果（Phase D 首个 confirmatory run 在其后约 12 小时），无需账本回溯。）

## 总体评分
9.5/10 — 教科书式缺陷修复：定位精确（评审 HIGH-1）、成因写透、回归测试可手算验证、20 分钟内落地。
