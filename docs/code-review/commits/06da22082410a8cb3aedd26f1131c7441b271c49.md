# Commit 06da22082410a8cb3aedd26f1131c7441b271c49 审查报告

**提交信息**: docs+ci: qlib/RD-Agent reuse-audit + bump refresh timeout 45→75（2026-08-11，Rethymus）
**改动范围**: 2 个文件，+155/-1；新增 `docs/qlib-reuse-audit.md`（151 行 qlib alpha158 / RD-Agent 复用审计：因子重叠矩阵、model_rolling 缺 purge+embargo 的判定、RD-Agent 定位为预注册门后的候选生成器、7 条集成建议），CI `timeout-minutes` 45→75。

## 四维度结论
| 维度 | 结论 | 一句话说明 |
|---|---|---|
| 稳定性 | ✅ | 文档主体 + 一行 CI 参数，无代码路径变化 |
| 可扩展性 | ✅ | 审计结论以"close-only factor-pack / candidate-generator seam"收口为可执行方向 |
| 生产可用 | ⚠️ | 75min 容量估算当天即被证伪（冷拉 >75min），次日再提到 150 |
| 高内聚低耦合 | ⚠️ | 自述"两处独立改动同船发布"，违背项目自身的原子提交约定 |

## 对照参考
审计对象本身即开源对照：microsoft/qlib（MIT，alpha158 因子集与 `model_rolling`）、microsoft/RD-Agent（MIT，NeurIPS 2025 R&D-Agent-Quant）。其"model_rolling 训练方向 PIT 安全但缺 purge/embargo"的判定与 qlib 公开实现一致；"RD-Agent 因子循环 = 结构性 rerun-to-significance"的批评切中其优化目标（raw IC）与预注册纪律的冲突。出处：github.com/microsoft/qlib、github.com/microsoft/RD-Agent。

## 问题清单
### P3-1 非原子提交：无关的 CI 参数调整与 151 行审计文档捆绑
- 描述：commit message 自述 "Two independent changes shipped together"。CONTRIBUTING.md 要求 commit 原子化（Conventional Commits）；CI 超时变更与审计文档无因果关联，回滚/追溯粒度被无谓放大。
- 位置：`.github/workflows/refresh-terminal-data.yml:31` + `docs/qlib-reuse-audit.md`（该 commit 版本）
- 影响范围：git 历史可追溯性（流程层面）。
- 修复建议：拆成 `docs(audit): ...` 与 `ci(refresh): ...` 两个 commit。
- 修复成本：低
- 状态：未修复（已成事实的历史）

### P3-2 75 分钟容量估算当天被证伪
- 描述：与 fd3e07f 的"45min 够用"、51b6d38a 的"5 issuers <30min"相同模式 —— 未实测冷拉总时长就给预算；本 commit 后 Form 4 冷拉仍超 75min，次日 1d75a7a5 提到 150，再以 66a3d3c8 的 step 级 20min 封顶收场。三次连涨反映的是"试错调参"而非测量驱动。
- 位置：`.github/workflows/refresh-terminal-data.yml:31-34`（该 commit 版本）
- 影响范围：过渡期 daily cron 连续超时（约 1 天）。
- 修复建议：已在后续 commit 解决；流程上应先本地计时冷拉再定预算。
- 修复成本：低
- 状态：已在 1d75a7a5、66a3d3c8 修复

### P3-3 审计表将 RSV 标为 "close-only" 存疑
- 描述：alpha158 的 RSV（随机指标 K 值）在 qlib 标准公式中使用 `$high/$low`（`(C - Min(Low,N)) / (Max(High,N) - Min(Low,N))`），而审计表把 RSV 归入 "NEW · close-only · PIT-safe"——若属实则是用 close 自身的滚动极值替代 H/L 的"变形 RSV"，应在文档中注明与 qlib 语义的差异（本环境网络超时无法打开 qlib 源码核实，留待人工确认；后续 fc91fe0d 的实现可作为对照）。
- 位置：`docs/qlib-reuse-audit.md:44`（该 commit 版本 RSV 行）
- 影响范围：fc91fe0d factor-pack 的因子语义标注与"与 qlib 可比"的声明强度。
- 修复建议：核实后在审计文档更正或注明"close-only 近似"。
- 修复成本：低
- 状态：待人工复核

## 总体评分
7/10 — 审计文档本身质量高（重叠矩阵诚实、把 RD-Agent 收敛到预注册门后是正确的设计判断），CI 变更理由链（超时→不存缓存→永久冷拉的恶性循环）分析到位；扣分在非原子提交与容量估算再度乐观。
