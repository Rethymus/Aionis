# Commit 08b0db569ef74addab45c6bd70b4697af94c4f11 审查报告

**提交信息**: feat(phase-d): confirmatory runner orchestrator (relationship bundle)（2026-07-29，Rethymus）
**改动范围**: 2 个文件，+361/-0；新增 `src/aionis/eval/phase_d.py`（config 构建/commit、bundle-shuffle placebo、differential、run_confirmatory 编排：双臂 OOS → H6 复跑 → placebo → leave-one-out → save_run → ledger）与 `tests/test_phase_d_run.py`（合成数据端到端，含 config_committed BEFORE result 顺序断言）。

## 四维度结论
| 维度 | 结论 | 一句话说明 |
|---|---|---|
| 稳定性 | ✅ | 纯计算编排、注入式数据（无网络），H6 双臂+raw scores 比对；LOO 列缺失时防御性回退 |
| 可扩展性 | ⚠️ | feature/rel_cols/horizon/splits/embargo/params 全参数化，但 `differential()` 是 phase_c 的逐字复制（漂移风险） |
| 生产可用 | ⚠️ | HIGH-1..4 锚点齐备且被测试钉死；但 `results_base=None` 默认路径跳过 save_run，首次真实 run 不落 artifacts（次日上午修复） |
| 高内聚低耦合 | ✅ | 复用 phase_c 的 `_frozen_params`/`_scores_equal`/`append_ledger`、two_arm 的共享折，eval 层不碰 ingest |

## 对照参考
"参数先落账、指标后落账"的顺序纪律与 [MLflow 官方 Tracking 文档](https://mlflow.org/docs/latest/ml/tracking/) 及社区实践（[APXML 教程](https://apxml.com/courses/data-versioning-experiment-tracking/chapter-3-tracking-experiments-mlflow/logging-params-metrics)：params 在 run 开始时一次记、metrics 随训练记录）同构；本项目把它升级为 ledger 顺序断言（`[config_committed, confirmatory:first]` 且 sig 相同），比一般 MLOps 工具更严格。出处域名：mlflow.org、apxml.com。

## 问题清单
### P2-1 save_run 被 `results_base is not None` 守卫，默认调用不落 artifacts
- 描述：`if results_base is not None: results.save_run(...)`——脚本 main() 以默认参数调用时跳过 save_run，导致首次 Phase D confirmatory run 只写 ledger、无 dashboard artifacts（Phase C 曾有同样 bug）。ledger 头条不受影响，但 OOS panel/IC 序列不可复查。
- 位置：`src/aionis/eval/phase_d.py:235-241`
- 影响范围：真实 run 的 `runs/results/` 产物完整性。
- 修复建议：去掉守卫，`save_run(base=None)` 走默认 runs 目录（测试显式传 tmp base）。
- 修复成本：低
- 状态：已在 ef4c94806657a185ee4a855eefedd6040535542d 修复

### P3-1 `differential()` 与 phase_c 逐字重复（DRY 漂移风险）
- 描述：`phase_d.differential` 与 `phase_c.differential` 函数体完全相同（common 交集 → HAC 摘要 → `groups=np.arange` → MBB-DM → publishable_ci_half）；两份实现将来一处改动另一处漏改即产生 B/C/D 差分口径漂移。另 `_sha` 内 `import hashlib` 与模块级 import 冗余。
- 位置：`src/aionis/eval/phase_d.py:129-150`
- 影响范围：跨 phase 差分统计口径的一致性维护成本。
- 修复建议：`from aionis.eval.phase_c import differential`（或在 metrics 层建公共函数）。
- 修复成本：低
- 状态：未修复（main 上仍为两份拷贝）

## 总体评分
8/10 — 防泄漏编排（config 先行、placebo 只扰动处理臂、H6 双重复核、LOO 显式标注非 gate）结构严谨且测试钉住关键顺序；扣分在 save_run 默认路径缺陷与一处复制粘贴。
