# Commit 091260b4e0d8e472a2cab68fed0fe7b55a813c30 审查报告

**提交信息**: docs(readme): rewrite for the current project — 4 nulls, anti-leakage, TCR roadmap（2026-07-30，Rethymus）
**改动范围**: 1 个文件，+108/-100；README.md 全面重写为「4 个 confirmatory null + 防泄漏身份 + TCR 路线图」的现状描述。

## 四维度结论
| 维度 | 结论 | 一句话说明 |
|---|---|---|
| 稳定性 | ✅ | 纯文档；B/C/D/E1 数字与 docs/RESULTS.md 及 E2 预注册脚注逐项核对一致（−0.0008/0.87、−0.0065/0.36、−0.0030/0.60、−0.0028/0.53） |
| 可扩展性 | ✅ | 路线图 A→E3 与命令块与实际 scripts 对齐（phase_{b,c,d,e1}_run 均存在） |
| 生产可用 | ⚠️ | B 期 ci_half=0.0098 在本 commit 无 ledger 出处（见 P2-1），对一个以可审计为卖点的项目是溯源缺口 |
| 高内聚低耦合 | ⚠️ | Docs 一节指向被 gitignore 的 `.omc/wiki/`（见 P3-1） |

## 对照参考
不适用（纯文档重写）。README 的「结果表 + 复现命令 + 数据治理红线」结构与优秀开源研究仓库（如 mlflow 的 README 结果可复现性表述）方向一致。

## 问题清单
### P2-1 Phase B 的 ci_half=0.0098 在本 commit 无账本出处
- 描述：headline 表给 B 期填 `ci_half 0.0098` 并断言「all are publishable-as-null (ci_half < 0.015)」，但本 commit 时点的 docs/RESULTS.md 明确写「Phase B 的 ledger 行只单列了各臂 ci_half（0.0150–0.0160，贴边），differential CI 未单列」，Phase B 的 schema-1 differential.json 也无 ci_half 键。该 0.0098 是写 README 时的临时计算，未锚定任何 ledger 行；表内 C/D/E1 三行均为 ledger 锚定值，唯独 B 行来源不可追溯。后续 efb4f39（2026-08-05）补算 paired HAC CI [−0.01057,+0.00897]（ci_half=0.00977，与本表数值吻合）并在 README 标注「computed 2026-08-05」，等于承认本 commit 时该数字是未入账的。
- 位置：`README.md:22`（headline 表 B 行）与 `README.md:29-30`（publishable 断言）
- 影响范围：文档可信度/可审计性——headline 表混入一个 post-hoc 计算值且未披露口径，与项目「config_committed BEFORE result」的审计叙事形成落差（数字本身最终被验证为正确，非事实错误）。
- 修复建议：对无 ledger 出处者标注「computed post-hoc <date>, ledger row append-only unchanged」（efb4f39 正是这么做的）。
- 修复成本：低
- 状态：已在 efb4f39 修复（补算 + 三处 living-doc 溯源标注）

### P3-1 Docs 一节指向 gitignored 的 `.omc/wiki/`
- 描述：README 把 `.omc/wiki/` 列为「durable knowledge base」，但 `.gitignore:52` 忽略整个 `.omc/`——cloner 拿不到该目录，指路失效（AGENTS.md 亦明确 `.omc/` 不是 durable handoff、`state/` 才是）。
- 位置：`README.md:205`
- 影响范围：新读者/复现者找不到所指内容。
- 修复建议：改指 `docs/` 与 `state/` 下被跟踪的文档。
- 修复成本：低
- 状态：未修复（main 的 README 仍含该引用）

## 总体评分
8/10 — 重写质量高、数字与 living docs 一致性好，但 headline 表混入一个当时无出处的统计量，且指向仓库外目录。
