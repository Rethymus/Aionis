# Commit 099a3a5b90829f76bca02ac9031046f4598edd49 审查报告

**提交信息**: test(phase-d): joined-panel PIT glue for the relationship bundle (§5 #4)（2026-07-29，Rethymus）
**改动范围**: 1 个文件，+108/-0；新增 `tests/test_phase_d_pit.py`——3 个 hermetic 测试，钉住 rel bundle 经 `build_selection_panel(extra_features=)` 组装时的胶水层 PIT 契约。

## 四维度结论
| 维度 | 结论 | 一句话说明 |
|---|---|---|
| 稳定性 | ✅ | 纯测试、合成数据、无网络、seed 固定 |
| 可扩展性 | ✅ | 三测各钉一个正交不变量：不跨日期 forward-fill、不跨 ticker 泄漏、端到端 13D filing 在 d<F 不可见 |
| 生产可用 | ✅ | 正是 freeze 前置条件（pre-reg §5 #4）所需的防泄漏证据；断言 NaN 永不被插补 |
| 高内聚低耦合 | ✅ | 只依赖 features 层公共 API，镜像 test_phase_c_pit 的结构 |

## 对照参考
不适用（测试专用 commit；PIT 不变量断言模式与组件层 `test_peer_momentum.py` / `test_stakes_13d_signal.py` 构成三层证据链）。

## 问题清单
### P3-1 第三个测试的 docstring 承诺了两项检查，测试体只实现了一项
- 描述：`test_assembled_rel_bundle_is_pit_end_to_end` 的 docstring 写"A future price perturbation must not move a past peer_mom, and a 13D filing at F must not appear at d < F"，但测试体只有 13D 可见性断言——未来价格扰动不变性检查并未出现在本测试中（该不变性由 `test_peer_momentum.py::test_trailing_return_is_strictly_backward` 单独覆盖，但两处不在同一文件，读者按 docstring 理解会产生"已在本测试验证"的错觉）。
- 位置：`tests/test_phase_d_pit.py:73-76`（docstring）vs `:100-108`（测试体）
- 影响范围：仅文档-测试一致性；测试覆盖本身无缺口（组件层已覆盖）。
- 修复建议：删去 docstring 中未实现的前半句，或补上扰动断言。
- 修复成本：低
- 状态：未修复

## 总体评分
8.5/10 — 胶水层 PIT 测试是这类研究管线最该有而常缺的测试，三个不变量选择准确；仅 docstring 与测试体有轻微错位。
