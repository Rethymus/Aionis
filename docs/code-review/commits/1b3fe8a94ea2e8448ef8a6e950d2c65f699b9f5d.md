# Commit 1b3fe8a94ea2e8448ef8a6e950d2c65f699b9f5d 审查报告

**提交信息**: refactor(web): dissolve the seven themes into the hub taxonomy（2026-08-11，Rethymus）
**改动范围**: 6 个文件，+74/-58；新增 `theme-slice.tsx`（按 key 过滤主题卡的复用组件），把 7 主题数据重新分配到对应 hub 的 tab（picks+factors、confirmation+news、track+cost），/themes 页瘦身为纯漏斗总览；导出 `ThemeCard` 供复用，删除前一日刚加的 LAYER_* 分组；dict 改名 + 新键（zh/en）。

## 四维度结论
| 维度 | 结论 | 一句话说明 |
|---|---|---|
| 稳定性 | ✅ | 纯展示重组；`themes.length === 0 → return null` 防空面板；`themes.awaiting` 键仍被 ThemeCard 使用无悬空 |
| 可扩展性 | ⚠️ | 主题卡从此只出现在各 hub 的白名单 slice 里 —— 导出端新增主题 key 将在全站任何页面都不可见 |
| 生产可用 | ✅ | 信息架构与漏斗叙事一致（数据归于其 funnel 角色），tab/哈希路由同步更新 |
| 高内聚低耦合 | ✅ | ThemeCard 单一来源、ThemeSlice 薄复用层，hub 页面零逻辑新增 |

## 对照参考
不适用（展示层 IA 重组）。按角色归组到 hub + 独立"系统如何工作"总览页是常见的 dashboard 信息架构实践。

## 问题清单
### P3-1 新主题 key 将全站不可见（无兜底全集页面）
- 描述：改造后 /themes 不再渲染任何主题卡（只留漏斗图+运行信号+方法说明），各 hub 只渲染 `keys` 白名单内的卡（`theme-slice.tsx:27` 的 filter + 空则 return null）。导出端未来新增主题（如再接一个新数据源主题）时，若忘了在某个 hub 的 `keys` 里登记，该主题在任何页面都不会出现，且无警告。
- 位置：`web/src/components/themes/theme-slice.tsx:27-30`、各 hub page 的 `<ThemeSlice keys={[...]} />`（该 commit 版本）
- 影响范围：未来主题扩展的可见性（维护风险）。
- 修复建议：加一个开发期断言（导出 key 集合 ⊆ 所有 slice keys 的并集）或在 /themes 页保留折叠的"全部主题"区块。
- 修复成本：低
- 状态：未修复（该结构随后续 9d260c9/37b4196 的 IA 再重组继续演化）

### P3-2 一日寿命的架构churn
- 描述：463136a9（前一晚）刚建立的 LAYER_KEYS 分组在本 commit 整体删除；且本 commit 的"themes 页=漏斗总览"结构在次日 batch_22 的四枢纽重组中再次被推翻。连续三次 IA 推翻反映方向未定型即落码。
- 位置：`web/src/components/themes/themes-view.tsx`（删除段）
- 影响范围：返工成本（展示层，无研究影响）。
- 修复建议：IA 变更先以纸面原型/设计文档定稿再落码。
- 修复成本：低
- 状态：未修复（流程观察）

## 总体评分
8/10 — 干净、方向正确的"数据归位"重构，组件复用与 i18n 同步到位；主要遗憾是白名单可见性无兜底与前夜成果即弃的churn。
