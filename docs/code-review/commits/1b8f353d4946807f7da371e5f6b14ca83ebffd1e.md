# Commit 1b8f353d4946807f7da371e5f6b14ca83ebffd1e 审查报告

**提交信息**: feat(phase-c): confirmatory runner (surprise bundle vs fundamentals-only)（2026-07-28，Rethymus）
**改动范围**: 6 个文件，+988/-0；新增 `eval/phase_c.py`（编排器：config_committed 先于结果、双臂、H6、placebo/sanity/LOO）、`eval/phase_c_controls.py`（NaN 保持 + seed 确定的洗牌 placebo）、薄壳 `scripts/phase_c_run.py` 及 401 行 hermetic 测试。

## 四维度结论
| 维度 | 结论 | 一句话说明 |
|---|---|---|
| 稳定性 | ✅ | 编排纯函数化、数据注入无网络；缺 key/空输入路径在下游显式抛错 |
| 可扩展性 | ✅ | LOO/placebo/sanity 全部走同一 `_arm_ic` 通道，参数全部可注入覆盖 |
| 生产可用 | ⚠️ | haircut_table 复用 Phase B 的口径错误；VIX 分量仍接在错误的 vintage 路径上（本 runner 在真实数据上跑不通，当日被 b8270049 重写） |
| 高内聚低耦合 | ✅ | 决策全部收进 eval 层、script 只是 IO 薄壳——正是 Phase B 的教训落实 |

## 对照参考
placebo 的 NaN-preserving 置换与 scikit-learn `permutation_tester`/`check_permutation` 的保结构置换思想一致（scikit-learn.org）；`np.random.default_rng(seed)` 逐列/逐组重用同一 rng 保证 H6 可复现，符合 numpy 官方 Generator 最佳实践（numpy.org/doc）。FF5 sanity 锚经 pandas-datareader 取 Tuck 官方 zip（单次批量请求，无礼貌性问题）。

## 问题清单
### P2-1 haircut_table 复制了 Phase B 的口径错误（t-stat 当 Sharpe）
- 描述：`se = float(summary.get("se_hac") or 1e-12); sharpe = float(summary["mean_ic"]) / se` —— 与 phase_b_run 相同，把 HAC t-stat 喂给期望每期 Sharpe 的 harvey_liu_haircut，显著性放大一个量级；叠加上游 `ppf(1-p)` 消零缺陷（见 bdf3fd9 审查），§6 门同样形同虚设。
- 位置：`src/aionis/eval/phase_c.py:263-277`
- 影响范围：Phase C 的 multiple_testing_haircut 报告
- 修复建议：与 Phase B 一并修复（传 IC-IR；上游改 `norm.isf`）
- 修复成本：低
- 状态：未修复（main 的 phase_c.py 同段落原样）

### P2-2 VIX 分量建立在已被证伪的 vintage 路径上
- 描述：`build_bundle` 经 `vix_surprise_panel` → `vix_as_of` → `fetch_vix_vintages`（ALFRED vintage）取数；VIXCLS 无 ALFRED vintage 历史，真实运行时该路径必然失败或取到错误口径——本 commit 的 runner 在 b8270049 之前无法正确执行。
- 位置：`src/aionis/eval/phase_c.py:148-152`（build_bundle 的 vix 行）
- 影响范围：Phase C 首次确认性运行（当日即发现并修复，未产出错误结果）
- 修复建议：G3 no-revision 契约取数
- 修复成本：中
- 状态：已在 b8270049 修复

### P3-1 账本仍为 CWD 相对路径 + 非 strict JSON 写入
- 描述：`LEDGER = "runs/ledger.jsonl"`（相对路径）；`append_ledger` 未设 `allow_nan=False`，与 Phase B 同族的系统性瑕疵。
- 位置：`scripts/phase_c_run.py:31`、`src/aionis/eval/phase_c.py:76-78`
- 影响范围：从非仓库根运行的账本落点；严格 JSON 消费者
- 修复建议：统一绝对路径 helper + strict 序列化
- 修复成本：低
- 状态：待人工复核（全仓库脚本系统性模式）

## 总体评分
8/10 — 编排结构是 Phase B 教训的正确沉淀（注入式可测、placebo 确定性、账本时序），扣分在两处已知缺陷的复用/传递。
