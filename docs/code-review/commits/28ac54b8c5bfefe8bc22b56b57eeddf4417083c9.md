# Commit 28ac54b8c5bfefe8bc22b56b57eeddf4417083c9 审查报告

**提交信息**: feat(site): add gross risk-metrics section (tearsheet via empyrical, mount 1)（2026-08-03，Rethymus）
**改动范围**: 2 个文件，+80/-1；`build_static_site.py` 新增风险指标段（读 mount_metrics.json → treatment/price-only/等权 三列 × 6 指标暗色表格 + 醒目 GROSS 免责条），index.html 同步再生。纯展示层。

## 四维度结论
| 维度 | 结论 | 一句话说明 |
|---|---|---|
| 稳定性 | ✅ | JSON 缺失/结构异常均有降级文案；_fmt_pct/_fmt_num 对 None 显示「—」 |
| 可扩展性 | ✅ | 指标行以元组表驱动，加指标改一行；与挂载产出解耦 |
| 生产可用 | ✅ | GROSS/探索性/非可交易免责置于表格上方，展示诚实性到位 |
| 高内聚低耦合 | ✅ | 只消费 mount_metrics.json，不触碰研究管线 |

## 对照参考
不适用（静态 HTML 展示段；数据由 1879af1f/475447bd 已审的 empyrical 路径产出）。

## 问题清单
### P3-1 NaN 值会打印为 "nan%"，且等权列取自 treatment 臂的隐式假设未声明
- 描述：`_fmt_pct`/`_fmt_num` 只判 `isinstance(x, (int, float))`——若上游序列化遗漏使 NaN 以 float 到达，页面显示 `nan%`（当前 475447bd 的 serializer 已把 NaN 转 null，未实际触发）；「等权(universe)」列取 `mount["treatment"]["tearsheet"]["ew"]`，隐含两臂等权序列恒同（当前成立：同一 OOS 面板），若未来两臂月份覆盖分叉则列间不可比。
- 位置：`scripts/build_static_site.py:64-69, 82-84`
- 影响范围：展示层边角。
- 修复建议：`_fmt_*` 加 `math.isfinite` 判断；等权列来源加注释或断言两臂 ew 一致。
- 修复成本：低
- 状态：未修复

## 总体评分
8/10 — 干净的展示层增量：降级路径完整、免责声明醒目、表格实现表驱动；仅存边角健壮性小病。
