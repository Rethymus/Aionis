# Commit 2a1958da2f24468d1ce093a00a746f70df1ce79e 审查报告

**提交信息**: feat(web): market risk panel — Sharpe/Sortino/MaxDD/VaR/CVaR（⑤ risk）（2026-08-10，Rethymus）
**改动范围**: 4 个文件，+68/-0；`export_terminal_data.py` 在 `export_market_context` 中接入 `portfolio_risk.summarize`（periods_per_year=12，<12 期则 risk=None），`market-view.tsx` 新增 8 指标 RiskStat 卡片，`market_context.json` +risk 字段，i18n zh/en 各 2 键。

## 四维度结论
| 维度 | 结论 | 一句话说明 |
|---|---|---|
| 稳定性 | ✅ | ≥12 期门槛 + `mc.risk ?` 空值守卫；NaN 收益由 summarize 内部 drop |
| 可扩展性 | ✅ | 指标卡由 JSON 字段驱动，后续加指标只改导出 |
| 生产可用 | ✅ | 诚实框定"等权市场风险、非策略风险"；真实数据（125 月，Sharpe 0.86/MaxDD −27%）与 commit message 一致 |
| 高内聚低耦合 | ✅ | 复用 b0e07b6 已测模块，导出/渲染各司其职 |

## 对照参考
月频收益 + `annualization=12` 的 empyrical 用法与官方 API 约定一致（quantopian.github.io/empyrical：annual_return/sharpe_ratio/sortino_ratio 均接受 period='monthly'）；8 指标风险摘要卡对应标准 tearsheet 头部（quantopian pyfolio/empyrical 生态惯例）。

## 问题清单
### P3-1 退化序列 NaN 展示未消毒（继承自 b0e07b6）
- 描述：`mc.risk.sharpe.toFixed(2)` 在 sharpe 为 NaN（常数收益序列、std=0）时会渲染字面 "NaN"，且 JSON 里是非法字面量；本 commit 接线时未加 nan→null 消毒（ic_monthly.json 的同类问题后来在 33035c3 专修过）。真实月度市场数据触发概率极低。
- 位置：`scripts/export_terminal_data.py:1122-1129`、`web/src/components/market/market-view.tsx:209-223`
- 影响范围：仅退化输入下的展示。
- 修复建议：导出前 `math.isnan(x) → None`，或渲染端 `Number.isFinite` 回落 "—"。
- 修复成本：低
- 状态：待人工复核（市场序列实际不会全常数，风险为理论性）

（另：同模块两个连续 import 语句拆成两段属风格噪音，ruff 已放行，不构成问题。）

## 总体评分
9/10 — 小而正确的集成：模块复用、空态守卫、诚实披露齐备，仅存理论性 NaN 边界。
