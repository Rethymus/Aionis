# Commit 2e2ab30935178b8e88853eaa5e217e13bd02e768 审查报告

**提交信息**: feat(eval): Track Adaptive OOS result — NULL (IC_diff -0.0037, p=0.26)（2026-08-09，Rethymus）
**改动范围**: 2 个文件，+2/-1；ledger 追加行 #54（oos_result, 绑定 #53 sig b7621e6b）、runner `FROZEN_SIG` 常量由已作废的 #52 修正为 #53（1 行）。

## 四维度结论
| 维度 | 结论 | 一句话说明 |
|---|---|---|
| 稳定性 | ✅ | 结果行字段自洽（-0.003658 = 0.011974−0.015631；t=−1.135、p=0.256 重算一致） |
| 可扩展性 | ✅ | 绑定修正后 runner 与最新冻结 config 对齐 |
| 生产可用 | ⚠️ | H6 双跑证据在入账时仍是"后台跑中"的承诺；预注册次门（等价 RCI）未随结果报告（见问题） |
| 高内聚低耦合 | ✅ | 结果与配置解耦清晰；诚实 null 叙述与 power-floor 预期一致 |

## 对照参考
不适用（结果沉积 + 1 行常量修正；统计推断 NW-HAC 已在 089f256 报告核对过）。

## 问题清单
### P2-1 预注册的次门（IC_diff RCI ⊆ ±SESOI_diff）未随 oos_result 报告
- 描述：#53 冻结 config 明文注册 `secondary_equivalence: "RCI of IC_diff_weekly within +/-sesoi_diff (examined only if primary null)"`；本结果 primary 为 null（p=0.26），按预注册次门应被"examined"并报告，但 ledger 行 #54 的 result 中无任何 RCI/等价字段，verdict 仅写 "NULL (CI crosses zero)"。审查者按行内数字估算：95% CI ≈ [−0.00997, +0.00265]，相对 ±0.010 是**极勉强包含**（下界距门仅 ~3e-5）——恰恰是这种边界情形最需要按预注册明示判定，而非留给读者自行计算。
- 位置：`runs/ledger.jsonl` 行 #54（result dict 缺 `secondary_equivalence` 键；对照 #53 config 的 gates 注册）
- 影响范围：Track Adaptive 结论的完整性（等价性解读缺位于账本）；预注册分析选择性省略的先例风险。
- 修复建议：补一条 ledger 修正行（或后续 sediment commit）给出 RCI 与等价判定（EQUIVALENT/NOT_EQUIVALENT），并注明按 #53 次门规则"primary null 后 examined"。
- 修复成本：低
- 状态：未修复（HEAD 行 #54 仍无该字段）

### P2-2 结果入账时 H6 bit-identical 证据尚未存在（承诺制而非 asserted）
- 描述：commit message 自述 "H6 double-run (bit-identical confirmation) follows as a background check"——即 oos_result 行写入时，支撑其可复现性的 H6 双跑**尚未完成**；此后全库检索（state/、docs/、reports/）只找到 27a18f3 中"后台跑中（PID 815221）→ 预期 PASS，结果在 gitignored 的 runs/track_adaptive_h6.log"的记载，**无任何已跟踪的 PASS 证据**。结合 runner main() 从未实现双跑断言（见 089f256 报告 P2-1），Track Adaptive 的 H6 合规停留在口头/日志级。
- 位置：`runs/ledger.jsonl` 行 #54（入账时刻）；`scripts/track_adaptive_run.py`（无断言）
- 影响范围：该研究线结果的可复现性主张（红线要求 "asserted"）。
- 修复建议：将双跑比较写入脚本（`--h6-verify` 输出 PASS/FAIL 并断言），跑通后把 PASS 结论 sediment 进 tracked state。
- 修复成本：低
- 状态：未修复

## 观察项（非缺陷）
1. **FROZEN_SIG 标签错位**：fdd9398 改 n_estimators=100（#53 参数）时未同步改 sig 常量，导致正式 run 产生的 `track_adaptive_summary.json` 曾自标为 #52——本 commit 修正并诚实披露（"label-only, run used #53 throughout"）；账本绑定正确，风险已闭合，根源记在 fdd9398。
2. dsr_n1=0.0 是把 HAC t 统计量喂给 DSR 公式的退化输出（见 089f256 P3-2），无信息量，建议删除或改名。

## 账本纪律核查
#54 oos_result（03:01Z）晚于其绑定 config #53（19:23Z 前一日）——config_committed BEFORE result 成立；三次 supersede 均先于任何 OOS。方向为 null 且 adaptive 略差，无 rerun-to-significance 受益。合规。

## 总体评分
6/10 — 诚实 null、绑定正确、数字自洽，但预注册次门未报告 + H6 证据承诺化两处纪律缺口使这条 OOS 记录低于本项目自身的账本标准。
