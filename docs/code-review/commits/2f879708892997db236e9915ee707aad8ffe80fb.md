# Commit 2f879708892997db236e9915ee707aad8ffe80fb 审查报告

**提交信息**: docs(tasks): rewrite 4 defective RES specs (D5) via 4 parallel worktree-free agents（2026-08-03，Rethymus）
**改动范围**: 8 个文件，+679/-340；重写 4 个 HOLD-REPLAN 规格（TASK-RES-02/03/08/10）+ 新增 4 个 ORCH 派发任务文件。纯任务规格文档，无代码。

## 四维度结论
| 维度 | 结论 | 一句话说明 |
|---|---|---|
| 稳定性 | ✅ | docs-only；不触及 src/、ledger、frozen surface（commit 声明与 stat 一致） |
| 可扩展性 | ✅ | 每个 spec 结构化为目标/合约/允许-禁止文件/验收标准，且把缺陷根因写进 header |
| 生产可用 | ✅ | 关键锚点经核验属实（见对照参考）；owner gate 前置条件保留 |
| 高内聚低耦合 | ✅ | 4 个并行 agent 文件隔离（每 agent 只写自己的 task 文件），ORCH 文件与 spec 一一对应 |

## 对照参考
事实核验（代替开源对照，docs commit）：
- RES-03 引用的 RD-15 冻结 API 与 `git show <hash>:src/aionis/eval/ranking_contract.py` 逐项吻合（`lambdarank`/`rank_xendcg` 枚举 L28-29、`n_bins ∈ {5,10}` 校验 L150-151、`fit_monthly_bins` L125、`transform_to_relevance` L189、`get_group_sizes` L105）——spec 对冻结实现的引用准确，且明确禁止修改该文件。
- RES-02 引用的 `src/aionis/eval/ff5_residual.py`、`src/aionis/features/diagnostics.py`（RD-13）均存在；「市场-wide 月度因子横截面无变异 → 不可 rank-IC」的根因诊断与 RD-13 护栏逻辑自洽（对照 pandas/rank-IC 惯例：横截面常量列 Spearman 未定义，诊断正确）。
- RES-08 引用的 `src/aionis/schema/gold_annotation.py` 存在；`tests/test_gold_sampler.py` 存在。

## 问题清单
### P3-1 RES-08 spec 引用错误的 sampler 模块路径
- 描述：重写后的 RES-08 阶段 2 写道「sampler 实现在 `src/aionis/extraction/gold_set_loader.py`（或 RD-05 指定模块）」，但该 commit 时点 `gold_set_loader.py` 不存在；`tests/test_gold_sampler.py:15` 实际导入的是 `from aionis.extraction.gold_sampler import ...`——真实模块为 `src/aionis/extraction/gold_sampler.py`。虽然「（或 RD-05 指定模块）」的括号留了余地，但具体路径指错，可能诱导 Engineer 新建重复模块（与本 spec「复用而非另起炉灶」的目标相悖）。
- 位置：`tasks/active/TASK-RES-08-gold-set.md:113-115`（该 commit 版本新增行）
- 影响范围：仅后续 RES-08 执行者的文件定位；不影响已冻结代码。
- 修复建议：把路径更正为 `src/aionis/extraction/gold_sampler.py`（或直接引用 test 导入语句）。
- 修复成本：低
- 状态：待人工复核（后续 RES-08 stage 1 提交 d97c78c8 报告「schema contract verified (22 tests)」，未涉及 sampler 路径）

## 总体评分
8/10 — 四份缺陷规格的重写根因诊断准确、冻结锚点核验属实、防泄漏条款（RD-13 护栏、PIT 证明、7-gate、owner gate）显著强化；仅存一处模块路径引用错误（P3）。
