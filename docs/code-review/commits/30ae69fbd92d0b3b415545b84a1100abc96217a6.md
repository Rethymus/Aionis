# Commit 30ae69fbd92d0b3b415545b84a1100abc96217a6 审查报告

**提交信息**: feat(track-c): freeze Track C config_committed (ledger #46, sig 758ca4d7)（2026-08-03，Rethymus）
**改动范围**: 4 个文件，+201/-3；新增 `scripts/track_c_commit.py`（纯 stdlib 冻结 runner）+ `runs/ledger.jsonl` 追加行 #46 + prereg 状态 PROPOSED→FROZEN + handoff 沉淀。

## 四维度结论
| 维度 | 结论 | 一句话说明 |
|---|---|---|
| 稳定性 | ✅ | 纯 stdlib、dry-run 默认、`--commit` 显式门；无网络/无 OOS 观察 |
| 可扩展性 | ✅ | TRACK_C_CONFIG 结构化字典完整（universe/features/regime/learner/validation/J-T 门/multiplicity），复用 phase_b commit_config 机制 |
| 生产可用 | ✅ | **sha256 自洽已独立复核**（本审查重算 ledger #46 行内 config 的 sha256 == config_sig，MATCH）；append-only、绝不覆盖 |
| 高内聚低耦合 | ✅ | 冻结面隔离声明明确；ledger 行与脚本内 config 完全一致 |

## 对照参考
不适用（冻结 runner 属项目自有机制）。其"config 哈希先于结果入账"模式与 OSF 预注册 time-stamp 及 DVC/MLflow 的 run-commit 语义一致；sig=sha256(json.dumps(config, sort_keys=True)) 的 canonical-JSON 哈希是跨语言可复现的标准做法（参照 json canonicalization 惯例，ietf 来源的 JCS 思路）。

## 问题清单
### P3-1 track_c_commit.py 创建即带 E501 lint debt，ruff 不干净
- 描述：本 commit 版本的 `TRACK_C_CONFIG` 中多行字符串字面量超长（如 `"mirror us_fundamentals_13 via cninfo as-filed PDF (rollysys/use_cninfo "` 拼接行及 jt_gate 长注释），违反项目"ruff check must be clean"约束；后续 commit a86fe8a 专门"clear pre-existing track_c_commit.py E501 lint debt"证实了该债务在创建时即存在。
- 位置：`scripts/track_c_commit.py:52-66`（cn_fundamentals_13/cn_price_12 长字符串）
- 影响范围：CI/本地 `uv run ruff check` 在 a86fe8a 之前对该文件报 E501。
- 修复建议：提交前本地跑 ruff（已由 a86fe8a 完成）。
- 修复成本：低
- 状态：已在 a86fe8a10c8bf 修复

### P3-2 prereg 冻结后残留 PROPOSED 措辞（§13 标题与 §12 尾行未同步）
- 描述：文档头部已改为 "FROZEN v1.0"，但 `## 13. 不越界声明（PROPOSED，未冻结）` 标题、该节第三条 "`[F]` 本预注册 PROPOSED；owner 写 …" 以及 §12 尾部 "仍待 owner（冻结前）：feature_cols 完整枚举…`config_committed` ledger 行授权（= 冻结）" 均为冻结前措辞，与 FROZEN 状态自相矛盾（feature_cols 事实上已在 §10.1/ledger 枚举冻结）。
- 位置：`docs/track-c-preregistration.md:173`（§13 标题）、`:177`（§12 尾行）
- 影响范围：仅文档一致性；读者可能误判冻结状态。
- 修复建议：将 §13 标题改为"（FROZEN）"，删除 §12 已完成的"仍待"行。
- 修复成本：低
- 状态：待人工复核（后续 commit 未专门清理）

## 总体评分
9/10 — 反泄漏锚点执行到位：sha256 自洽（独立复核通过）、dry-run 默认、先于任何 OOS；扣分仅因创建即带 lint debt 与冻结后文档残留措辞。
