# Commit 30e27d8972991948d61d951a86fa1908686da201 审查报告

**提交信息**: feat(scripts): IC noise-floor survey + empirical power-floor verification（2026-08-05，Rethymus）
**改动范围**: 3 个文件，+248/-1；新增 `scripts/ic_noise_floor_survey.py`（对 disk 上全部持久化 IC 系列计算 σ(IC)，20 系列）+ `tests/test_ic_noise_floor_survey.py`（5 个 hermetic 测试）+ power-floor 文献锚定 note 增补 §3.1 实测表。

## 四维度结论
| 维度 | 结论 | 一句话说明 |
|---|---|---|
| 稳定性 | ✅ | n=0/n=1 边界返回 None、NaN-drop、无输入时友好退出 |
| 可扩展性 | ✅ | glob 模式集中为常量，测试可 monkeypatch |
| 生产可用 | ⚠️ | 汇总键名 `in_documented_range_0p08_0p15` 与实际校验区间不符 |
| 高内聚低耦合 | ✅ | 只读持久化工件、无网络/重跑，输出仅 gitignored JSON |

## 对照参考
对照 pandas 官方 `Series.std(ddof=1)`/`describe` 语义（样本 std、NaN 默认跳过）：`_stats` 与之一致。数值区间断言的命名语义应与实际谓词一致是通用工程规范（无专门开源对照，不强行附会）。出处：pandas.pydata.org。

## 问题清单
### P2-1 「20/20 落在文献区间 [0.08,0.15]」与自家数据 max=0.163 矛盾，实际校验用的是宽容差 [0.06,0.20]
- 描述：summary 键 `in_documented_range_0p08_0p15` 的实际谓词是 `all(0.06 <= s <= 0.20 for s in stds)`；§3.1 表格同列展示 max σ=0.163 又声称「落在文献区间 [0.08, 0.15]（容差 [0.06, 0.20]）— 全部 20/20 ✓」——0.163 并不在 [0.08,0.15]，20/20 成立仅对宽容差成立。commit message 同样自相矛盾（「all 20 fall in the documented [0.08, 0.15] literature range」+「max=0.163」）。这是发表支撑文档里可被审稿人直接抓住的过度陈述。
- 位置：`scripts/ic_noise_floor_survey.py:107-109`；`reports/design/2026-08-05-power-floor-literature-anchoring.md` §3.1 表（该 commit 版本）
- 影响范围：power-floor 实证支撑的可信度陈述；不影响任何管线/账本。
- 修复建议：键名与文案改为「in_tolerance_range_0p06_0p20」，并如实写「18/20 在 [0.08,0.15]，2 个 Track C 臂达 0.163，在宽容差内」。
- 修复成本：低
- 状态：未修复（后续 c10993b 将发布线文档整体归档，脚本未再改动）

### P3-1 「20 系列」并非独立样本的表述偏强
- 描述：commit message 称 Track B「4 independent sigs」，但 4 个 sig 是 shared-fold 的 B/C/D/E1 各自 run（README 明言 shared-fold），且 ic_state/ic_base 同折配对、Track C 各配置共享同一市场数据——σ 跨系列一致更多说明共同数据/方法下的稳定噪声水平，而非 20 个独立证据。§3.1 判读「在 Track B 与 Track C 上一致出现」尚可，但「独立」措辞不严谨。
- 位置：commit message 及 `reports/design/2026-08-05-power-floor-literature-anchoring.md` §3.1
- 影响范围：文档表述严谨性。
- 修复建议：改为「4 个不同 phase 配置（shared-fold，非独立）」。
- 修复成本：低
- 状态：未修复（文档已随 c10993b 归档）

### P3-2 PHASE_B_NO_LEDGER 无效旋钮（与 3a3c2cfd 同型）
- 描述：`__main__` 中 `PHASE_B_NO_LEDGER` 仅打印提示、无行为分支。
- 位置：`scripts/ic_noise_floor_survey.py:131-134`
- 影响范围：可读性。
- 修复建议：删除或注释说明。
- 修复成本：低
- 状态：未修复

## 总体评分
7/10 — 脚本本身干净、测试到位，但作为「empirical verification」卖点的汇总断言存在键名/文案与数据矛盾（0.163 > 0.15）的过度陈述，恰发生在最需要精确的发表支撑材料中。
