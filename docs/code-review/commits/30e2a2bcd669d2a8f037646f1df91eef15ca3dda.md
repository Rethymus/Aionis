# Commit 30e2a2bcd669d2a8f037646f1df91eef15ca3dda 审查报告

**提交信息**: feat(track-b): (a-run) first rank-IC runner (binds config #41)（2026-08-02，Rethymus）
**改动范围**: 1 个文件，+51/-0；新增 `scripts/track_b_a_run.py`——装载物化面板、调用 `fit_track_b_baseline`（23 特征、horizon=21、min_train=60、embargo=21、bin_count=5）、打印 rank-IC/HAC CI/DM。

## 四维度结论
| 维度 | 结论 | 一句话说明 |
|---|---|---|
| 稳定性 | ✅ | 薄 runner，parquet 缺失自然 FileNotFoundError，参数显式 |
| 可扩展性 | ⚠️ | 超参与特征清单内联（与 config #41 对应但无机制性绑定） |
| 生产可用 | ❌ | 号称 "binds config #41" 却无任何 sha256/账本校验，账本纪律仅靠 print 字符串维系（见 P1-1） |
| 高内聚低耦合 | ✅ | 纯编排、零新逻辑，复用 fit_track_b_baseline 与特征清单常量 |

## 对照参考
不适用（51 行薄 runner，无新算法逻辑）。最有价值的对照是仓库自身的 `scripts/phase_d_run.py`：它在观察任何结果前计算 config sha256 并调用 `commit_config(config, LEDGER)`，同时钉住 fund/prices/membership/uv.lock 的数据指纹——本 runner 与该既有范式相比缺失全部绑定机制。

## 问题清单
### P1-1 「binds config #41」无机制性绑定：不计算配置哈希、不校验账本、不钉数据指纹
- 描述：runner 仅以字面量打印 `"(config #41, sig bf620b...)"`。对比同仓库 `phase_d_run.py:136-140` 的范式——`sig = hashlib.sha256(json.dumps(config, sort_keys=True).encode()).hexdigest()` → `commit_config(config, LEDGER)`（BEFORE result）→ 并钉 `fund_sha256/prices_sha256/uv_lock_sha256` 等数据指纹。本 runner：(a) 不计算生效参数（`_DEFAULT_FROZEN_PARAMS`、feature_cols、horizon/embargo/bin_count）的哈希并与账本 #41 比对，任何人对 `track_b_baseline.py` 冻结参数的静默改动都会仍被标记为 config #41；(b) 不记录面板 parquet 指纹，同 sig 重跑可在不同数据上产出不同 IC 而不被察觉。账本排序纪律（#41 冻结 commit 早于本观察）在 repo 层面成立，故不构成 P0 的「先观察后补账」；但防「rerun-to-significance」的**验证机制**在本 runner 完全缺位，与其自我声明不符。
- 位置：`scripts/track_b_a_run.py:10-11, 26-38`
- 影响范围：Track B 全部 a-run 结论的配置可追溯性；config #41/#42 标签的可信度。
- 修复建议：复刻 phase_d_run 范式——生效 config 序列化哈希 + `commit_config`/校验 + 面板 sha256 钉入 ledger 行。
- 修复成本：中
- 状态：未修复（main 上仍为 `ARM_CONFIG = {"treatment": "#41 (sig bf620b...)"}` 纯标签）

### P3-1 结果只打印不落盘
- 描述：headline 数字（mean_ic/CI/DM）仅写 stdout，无 save_run/artifact；复现依赖 commit message 与后续文档转述。
- 位置：`scripts/track_b_a_run.py:40-49`
- 影响范围：结果可复现性与审计链。
- 修复建议：至少将 `TrackBBaselineResult` 关键字段 + ic_series 落 JSON/parquet。
- 修复成本：低
- 状态：未修复（5c64e571 以 `docs/track-b-results.md` 人工记录弥补，runner 本身仍不落盘）

## 附注（不另计问题）
本 commit（16:55）先于 1376c758（17:01）合入，彼时管线尚未做月末横截面采样，runner 在真实 1.17M 行面板上会触发 LightGBM 大 query 问题而无法完成（1376c75 message 自述 "(a-run) now executes"）——缺陷归属 574e1910（已在其报告 P2-3 计入，已在 1376c75 修复），此处仅记录时序。

## 总体评分
6/10 — 编排干净，但作为「首个 OOS 观察」的确认性 runner，没有继承仓库已确立的 config-哈希-账本绑定范式，防泄漏纪律从「机制保证」退化为「字符串声明」，且该缺口延续至 main。
