# Commit 33035c3a21c7b5025cd4835cbc46dce395359041 审查报告（从简）

**提交信息**: fix(site): sanitize NaN→null in ic_monthly.json (JSON spec violation)（2026-08-07，Rethymus）
**改动范围**: 1 个文件，+11/-11；quarto-site/data/ic_monthly.json 中 11 处字面量 `NaN` 改为 `null`（纯数据再导出）。

## 四维度结论
| 维度 | 结论 | 一句话说明 |
|---|---|---|
| 稳定性 | ✅ | 修复 JSON 规范违例，strict 解析器（Jekyll/部分浏览器）不再拒绝该文件 |
| 可扩展性 | ✅ | 根因已在导出器修复（export_quarto_data.py: pd.isna→None + allow_nan=False），本 commit 只是工件对齐 |
| 生产可用 | ✅ | 经核对，该 commit 时点仓库全部 tracked JSON 已无残留 NaN，修复完整 |
| 高内聚低耦合 | ✅ | 单工件数据修正，无逻辑变更 |

## 对照参考
JSON 规范（RFC 8259，iana.org）禁止非限定 `NaN` 字面量；Python `json.dumps(allow_nan=False)` 官方文档即推荐用其防裸 NaN。做法与规范一致。

## 问题清单
通过。

## 总体评分
9/10 — 一次干净完整的规范修复对齐，无遗漏。
