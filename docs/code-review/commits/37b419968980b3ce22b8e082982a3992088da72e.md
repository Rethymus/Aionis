# Commit 37b419968980b3ce22b8e082982a3992088da72e 审查报告

**提交信息**: refactor(web): terminal IA → validity-argument chain (paradigm α, ECD)（2026-08-12，Rethymus）
**改动范围**: 8 个文件，+546/-206；第三次 IA 重建：sidebar 导航改为「语境→证据→效度（+守卫横贯）」论证链、Overview FunnelCards 改为箭头链 + guard band、i18n 字典重写 59 个编号/角色 token（zh+en）、新增 2 份设计文档 + state 沉淀。URL 均未变。

## 四维度结论
| 维度 | 结论 | 一句话说明 |
|---|---|---|
| 稳定性 | ✅ | 纯展示层重命名/重排；tsc+build 自述绿；数据引用（`aionis.metrics.jt_look1`）已核实存在于 `metrics.json` |
| 可扩展性 | ⚠️ | 第三次 IA 范式重建（6步漏斗→四枢纽→论证链），token 全量重写 churn 大，但属业主驱动的产品决策 |
| 生产可用 | ✅ | `/track#evidence` 链接经核实由 track 页 `hashToTabMap` 正确处理 |
| 高内聚低耦合 | ✅ | 改动严格限于 web 展示层（dict/sidebar/overview/themes 组件），零触碰 ledger/冻结 panel/config/prereg |

## 对照参考
不适用（纯 i18n 文案 + 布局重排，无新算法/数据逻辑；遵循 Next.js App Router 客户端组件 + 类型化字典的常规实践）。

## 问题清单
### P3-1 死字典键 `nav.group.corroboration`
- 描述：zh/en 均新增 `"nav.group.corroboration"`（"独立佐证"/"Corroboration"），但全仓库除 `dict.ts` 外无任何引用——sidebar 中独立佐证项实际复用 `nav.group.confirm` 作条目标题，`corroboration` 分组键从未渲染。
- 位置：`web/src/i18n/dict.ts:10`（zh 段；en 段对称位置）
- 影响范围：仅字典冗余，无运行时影响（类型上 DictKey 是联合类型，多余键不报错）。
- 修复建议：删除该键，或让 confirmation 条目真正使用它以保持「分组标签 vs 条目标题」语义一致。
- 修复成本：低
- 状态：未修复（后续 commit 未见删除该键；非阻塞）

其余核查通过：删除的 `nav.group.insights/monitor` 无残留引用；tsx/ts 中 ①-⑥ 圈号已清零（仅 dict 内新文案不含编号）；`FunnelCards` 第 4 卡 `/track#evidence` 有 `hashToTabMap` 支撑；`overview.loop.feedback` 从页脚移作 verdict 卡 intro，键复用无孤儿。

## 总体评分
8/10 — 干净的展示层范式迁移，边界纪律（不碰研究管线）执行到位；仅一处死字典键与高 churn 的 IA 反复重建值得记录。
