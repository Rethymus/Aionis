# Commit 38342d4ca5449faeded67e7db23033ef05f46d6e 审查报告

**提交信息**: fix(worker): rename to 'api' for clean URL (api.aionis-prices.workers.dev)（2026-08-08，Rethymus）
**改动范围**: 2 个文件，+2/-2；`workers/prices/wrangler.toml` 的 Worker name 改为 `api`，`web/src/lib/live-prices.ts` 的 `WORKER_URL` 同步改为新 URL。

## 四维度结论
| 维度 | 结论 | 一句话说明 |
|---|---|---|
| 稳定性 | ✅ | 两处引用同一 commit 内原子更新，无「URL 指向旧名」的断裂窗口 |
| 可扩展性 | ✅ | 无扩散引用，改动面最小 |
| 生产可用 | ⚠️ | 更名=新 Worker 部署，旧 `aionis-prices.aionis-prices.workers.dev` 若未在控制台删除将继续提供旧版本服务（仓库内无删除记录） |
| 高内聚低耦合 | ✅ | 命名与 URL 一一对应，耦合点显式 |

## 对照参考
不适用（纯重命名）。Cloudflare wrangler 官方文档（developers.cloudflare.com/workers/wrangler/commands/）：`wrangler deploy` 按 name 部署/覆盖同名 Worker，改名会创建新 Worker 而非迁移——旧 Worker 需 `wrangler delete` 或控制台手动清理。

## 问题清单
### P3-1 旧 Worker（aionis-prices.aionis-prices.workers.dev）疑似成为孤儿资源
- 描述：wrangler `name` 由 `aionis-prices` 改为 `api` 后，同名旧 Worker 在 Cloudflare 账号内仍存在并继续响应旧 URL（含 TIINGO_API_KEY 绑定），仓库中无删除记录；任何曾缓存旧 URL 的客户端会拿到不再更新的旧后端（东财 502 版）数据。
- 位置：`workers/prices/wrangler.toml:1`
- 影响范围：潜在配额消耗与旧数据误导（影响面极小——正式前端已同步改 URL）。
- 修复建议：`wrangler delete --name aionis-prices` 清理旧 Worker，或在 README 记录退役说明。
- 修复成本：低
- 状态：待人工复核（git 无法确认账号侧是否已删除）

## 总体评分
8/10 — 原子、最小、同步到位的重命名；仅剩云端孤儿资源的运维收尾不确定。
