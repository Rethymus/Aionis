# Commit 387ac0217cf9a67e792f343899fb366e0435dfba 审查报告

**提交信息**: feat(phase-c): first confirmatory run (null) + save-artifact fix（2026-07-28，Rethymus）
**改动范围**: 2 个文件，+9/-6；ledger 追加 Phase C config_committed(#30, 12:01 UTC) 与 confirmatory:first(#31, 12:21 UTC)；`run_confirmatory` 修复为总是写 save_run 工件（原 `if results_base is not None` 守卫在 main 传 None 时跳过落盘）。

## 四维度结论
| 维度 | 结论 | 一句话说明 |
|---|---|---|
| 稳定性 | ✅ | save 修复方向正确（None → 默认 runs 目录，测试显式 base 重定向不受影响） |
| 可扩展性 | ✅ | 不适用（小修复） |
| 生产可用 | ❌ | 冻结 config 的 vix_sha256 钉住的是管线**并不消费**的 3 个月探测缓存；实际消费的 alfred_VIXCLS.json 未入锚；haircut=Infinity 第三次入库 |
| 高内聚低耦合 | ✅ | 修复诚实披露（工件缺失、复跑补齐） |

## 对照参考
不适用（运行结果 + 小修复；无新逻辑模式）。账本时序（commit 12:01 → result 12:21）符合 HIGH-1。

## 问题清单
### P1-1 冻结 config 的 VIX 锚钉错文件：实际输入未入 sha256
- 描述：config_committed #30 记录 `"vix_sha256": _sha(CACHE/"vix_cls.parquet")`（sha `abbd476…`），而该 sha 在 ledger 中仅出现过一次于 02:15 的 data_ingest 行——range `["2024-01-02","2024-03-28"]`、63 行，是当日早些时候的**探测性抓取**。Phase C 管线实际消费的 VIX 输入是 `vix_as_of` → `fetch_vix_vintages` 读取的 `alfred_VIXCLS.json`（本提交前一小时的 b8270049 重写后为全跨度合成数据），其 sha **不在 config 里**。后果：(a) HIGH-4「same sig ⇒ bit-identical」对 VIX 分量不成立——刷新 alfred_VIXCLS.json 会改变结果而不改 config_sig、不产生新 ledger 行；(b) 预注册锚记录的 VIX 数据溯源（3 个月）与实际使用（全跨度）不符，审计叙事失真。
- 位置：`scripts/phase_c_run.py:80`（`"vix_sha256": _sha(CACHE / "vix_cls.parquet")`）；消费路径 `src/aionis/features/vix_surprise.py:150`（vix_surprise_panel → vix_as_of → alfred_VIXCLS.json）
- 影响范围：Phase C 冻结运行的可复现性声明（H6/HIGH-4）与数据溯源审计
- 修复建议：把 pin 改为实际消费的 `alfred_VIXCLS.json` 的 sha（或同时 pin 两者并注明语义）；以 amendment ledger 行公告修正
- 修复成本：低
- 状态：未修复（main scripts/phase_c_run.py:80 原样；ledger 无 alfred_VIXCLS 的 sha）

### P2-1 haircut=Infinity 第三次写入 tracked ledger
- 描述：ledger #31 的 `multiple_testing_haircut` 全部 trial 均为 `{"haircut_sharpe": Infinity, "survives": true}`（arm_macro t_hac 仅 0.99 却「存活」全部 Bonferroni 修正）——bdf3fd9 审查 P1-1 缺陷（ppf 消零 + t-stat 当 Sharpe）的延续，且再次以非法 JSON 字面量入账本。
- 位置：`runs/ledger.jsonl`（#31 行 multiple_testing_haircut 字段）；根因 `src/aionis/eval/multiple_testing.py:484`、`src/aionis/eval/phase_c.py:263-277`
- 影响范围：Phase C §6 多重检验记录无效；账本严格 JSON 兼容性
- 修复建议：同 bdf3fd9 P1-1（`norm.isf` + 传 IC-IR）
- 修复成本：低
- 状态：未修复

### P3-1 结果与修复同 commit：代码在 config_committed 与 confirmatory:first 之间发生变化
- 描述：save-artifact 修复改动了 `run_confirmatory`，与运行结果同一 commit 落库。修复不触及任何计算路径（仅落盘），且 commit message 明确披露 + 以 H6 复跑补齐工件，属可接受的边界情形；但严格的「冻结后代码不变」叙事在此有一个已披露的例外。
- 位置：`src/aionis/eval/phase_c.py:355-363`
- 影响范围：流程叙事完整性（无统计影响）
- 修复建议：今后将此类修复与结果分 commit，或预先把「落盘」并入冻结语义
- 修复成本：低
- 状态：待人工复核

## 总体评分
6/10 — 结果本身（null、placebo 消失、H6 为真）与账本时序合格；但冻结锚钉错 VIX 输入文件属 HIGH-4 契约实质破洞且至今未修。
