# Commit 3a3c2cfd6107cfc3d764c4baf451cd51d5b18169 审查报告

**提交信息**: feat(scripts): bps-cost sensitivity sweep -- Track B economic-validity lens（2026-08-05，Rethymus）
**改动范围**: 2 个新文件，+304；`scripts/track_b_net_cost_sweep_run.py`（对 Track B treatment 面板做 bps 成本网格扫描）+ `tests/test_net_cost_sweep.py`（8 个 hermetic 测试）。

## 四维度结论
| 维度 | 结论 | 一句话说明 |
|---|---|---|
| 稳定性 | ✅ | 输入校验（负 bps/空网格拒绝）、panel 缺失 fail-fast、测试覆盖插值边界 |
| 可扩展性 | ⚠️ | 默认「最新 mtime 面板」选择器沿用 mount2 模式，存在选错面板的隐患 |
| 生产可用 | ✅ | 输出记录 panel 路径可追溯；明确 exploratory、绝不写 ledger |
| 高内聚低耦合 | ✅ | 复用 `net_cost_summary`，薄壳循环不重复造成本模型 |

## 对照参考
对照 pandas 官方对「确定性管道输入应显式指定而非依赖文件系统时序」的常见实践，以及 numpy 官方 `default_rng(seed)` 用法：本脚本测试 fixture 用 `np.random.default_rng(seed)` 符合现代随机 API；但默认面板选择依赖 `os.path.getmtime` 排序（文件系统元数据），不属于确定性输入选择。出处：pandas.pydata.org、numpy.org。

## 问题清单
### P2-1 默认面板按 mtime 取「最新」，可能静默选中非 Track B 的 oos_state.parquet
- 描述：`paths = sorted(glob.glob("runs/results/*/oos_state.parquet"), key=os.path.getmtime); return paths[-1]`。`oos_state.parquet` 由 `phase_b_run`、`phase_c`、`phase_d`、`phase_e1` 的 save_run 统一落盘（见 `src/aionis/reporting/results.py:31` 注释与各 phase orchestrator）。任何其他 phase 重跑后，本 sweep 默认模式会静默改用该 phase 的面板，产出仍写 `runs/track_b_net_cost_sweep.parquet`（文件名宣称 Track B）。缓解因素：每行记录 `panel` 路径且启动时打印，非完全不可见；`--run-sig` 提供显式指定；该模式继承自姊妹脚本 `track_b_net_cost_run.py:36-38` 而非本 commit 首创。
- 位置：`scripts/track_b_net_cost_sweep_run.py:47-50`
- 影响范围：exploratory 经济有效性镜头的结论归属（若拿 Phase C/D 面板算出的 decay 曲线被误读为 Track B）。
- 修复建议：默认路径下断言面板确为 Track B treatment（如读取随面板持久化的 config/phase 元数据校验），或强制要求 `--run-sig`。
- 修复成本：中
- 状态：未修复（该文件后续无改动）

### P3-1 PHASE_B_NO_LEDGER 环境变量为无效旋钮
- 描述：`if os.environ.get("PHASE_B_NO_LEDGER") == "1": print(...)` 只打印提示，不改变任何行为（脚本本就从不写 ledger）。与其余脚本的 NO_LEDGER 模式（真正切换路径）语义不一致，易让读者误以为有关键行为分支。
- 位置：`scripts/track_b_net_cost_sweep_run.py:169-172`
- 影响范围：可读性/维护性。
- 修复建议：删除该分支或注释说明仅为兼容提示。
- 修复成本：低
- 状态：未修复

## 领域红线核查
合规：exploratory 场景扫描，OOS 面板不变，仅成本假设变动；只写 gitignored `runs/`，不写 ledger，不构成 rerun-to-significance。测试 hermetic（seed=0 合成面板），无 mock 进入真实管线。

## 总体评分
8/10 — 复用优先、测试扎实（不变量断言 gross 不变/net 单调/成本线性）、边界处理完善；扣分在 mtime 默认面板选择的错配风险（继承自 mount2 的既有隐患）与无效环境变量旋钮。
