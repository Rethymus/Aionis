# Commit 3aa2c1b846a11a5f32d8983874f069fafc9ce665 审查报告

**提交信息**: feat(scripts): Track LLM Phase-0 feasibility pilot (coverage + projected cost)（2026-08-09，Rethymus）
**改动范围**: 2 个文件，+313/-0；新增 `scripts/track_llm_feasibility_pilot.py`（204 行：G1 CIK 解析率 + G2 token 成本投影 + 门限判级）与 `tests/test_track_llm_feasibility_pilot.py`（9 个纯函数测试）。

## 四维度结论
| 维度 | 结论 | 一句话说明 |
|---|---|---|
| 稳定性 | ⚠️ | `_measure_llm_sample` 为 NotImplementedError 桩；OOS parquet 缺失时 fail-fast 明确 |
| 可扩展性 | ✅ | 常量命名齐全、tokens_per_10k/train_years 可参数化、门限判级纯函数可测 |
| 生产可用 | ✅ | 0 ledger/0 网络（默认路径）、幂等缓存假设已披露、JSON 输出机器可读 |
| 高内聚低耦合 | ✅ | 复用 cik_resolver/normalize_ticker；不写任何冻结面 |

## 对照参考
不适用（项目自有的可行性试点脚本；G1/G2 为纯本地统计，无外部框架对照必要）。`--measure-llm` 留桩至 freeze 时刻的做法与"冻结前不烧 token"的成本纪律一致，但见 P2-1。

## 问题清单
### P2-1 `--measure-llm` 公开 CLI 标志指向未实现桩
- 描述：argparse 注册了 `--measure-llm`（`scripts/track_llm_feasibility_pilot.py:191-195`），usage 文档也示例 `--measure-llm 8`，但 `_measure_llm_sample` 直接 `raise NotImplementedError`（:164-171）——用户按 usage 传参会先看到完整 G1/G2 JSON，随后收到未捕获 traceback。项目 DoD 明言"无 stub/未实现分支（blocker）"；此处是有意披露的桩（"implemented at freeze time"），但 CLI 合同与实现不一致。
- 位置：`scripts/track_llm_feasibility_pilot.py:158-171,191-195`
- 影响范围：仅试点脚本使用者；无数据/管线影响。
- 修复建议：在 argparse 层用 `ap.error("--measure-llm is not yet implemented; rerun at freeze time")` 或注册为 suppressed 选项；或实现后再生效。
- 修复成本：低
- 状态：未修复（`git log <hash>..main -- <file>` 无后续改动，桩仍在）

### P3-1 判级阈值内联魔法数
- 描述：`summarize()` 中 amber 阈值 `0.70`、token 门 `20`/`50` 内联硬编码，而 green 阈值 `_COVERAGE_GATE=0.90` 已提为命名常量，风格不一致。
- 位置：`scripts/track_llm_feasibility_pilot.py:141-143`
- 影响范围：可读性/可调性。
- 修复建议：提取 `_G1_AMBER = 0.70`、`_G2_GREEN_MTOK = 20`、`_G2_AMBER_MTOK = 50`。
- 修复成本：低
- 状态：未修复

其余核查：G1 纯本地去重计数无泄漏（只读 ticker/date，不触 score）；G2 训练侧用 n_distinct 保守高估已披露；测试 9 个断言与实现一致。

## 总体评分
8/10 — 反泄漏与成本纪律执行到位，扣分在公开 CLI 标志的未实现桩与 DoD 冲突。
