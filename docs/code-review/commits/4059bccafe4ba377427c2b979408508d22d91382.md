# Commit 4059bccafe4ba377427c2b979408508d22d91382 审查报告

**提交信息**: feat(track-b): FREEZE config (sha256 7a1357... -> ledger #40); prereg PROPOSED->FROZEN（2026-08-02，Rethymus）
**改动范围**: 2 个文件，+12/-9；`runs/ledger.jsonl` 追加行 #40（track_b config_committed，sig 7a13579e…08436）+ `docs/track-b-preregistration.md` 状态 PROPOSED→FROZEN（记录 sig、owner 5 点 KEEP 决议、feature_cols 修订预告）。

## 四维度结论
| 维度 | 结论 | 一句话说明 |
|---|---|---|
| 稳定性 | ✅ | 账本 append-only（39→40 行，历史行零改动）；prereg 与 ledger 内容逐项一致 |
| 可扩展性 | ✅ | feature_cols 预告为「新 ledger 行修订」而非静默补写，扩展路径合规 |
| 生产可用 | ✅ | **config_committed BEFORE result 红线实测通过**（见下）|
| 高内聚低耦合 | ✅ | 复用 `commit_config`（sha256 of sort_keys JSON）机制，未新造冻结流程 |

## 审查验证（独立复核）
1. **时序**：本 commit 账本恰 40 行，track_b 相关仅此一行 config_committed；首个 track_b 结果行（#41/#42）在 a09b9e4/5c64e57 才出现——冻结确实先于任何 OOS 观测。
2. **签名**：提取行 #40 的 config 对象，按 `commit_config` 的规范化（`json.dumps(config, sort_keys=True)`）重算 sha256 = `7a13579eb0ad4db0d5d04489bd8bc6eb7c36b74b0157ea302b2d5449cb708436`，与账本记载 **MATCH**——冻结签名可复现、非口说。
3. **一致性**：frozen_params（n_jobs=1、全 seed=0、lambdarank、500/0.05/31/20/1.0/0.8/0.8/1）、looks {60,90,120}、obf_z (2.772/2.263/1.960)、SESOI 0.01、n_trials 30 均与 prereg §4/§7 及 sesoi_gate 实现一致。

## 对照参考
「冻结配置先于结果 + 可复现哈希」与临床试验预注册的 SAP（Statistical Analysis Plan）冻结及 OSF 注册的时间戳公证机制同构；sha256(sort_keys JSON) 的规范化做法与 The Update Centre/WHO ICTRP 一致性校验思路一致。不适用代码对照。

## 问题清单
### P3-1 冻结的 `hac_se: "Newey-West maxlag=4"` 与 sesoi_gate 自动滞后规则在 look-1 不一致
- 描述：config 冻结 `maxlag=4`；但 `sesoi_gate.look_summary` 默认 `maxlag=None` → 自动规则 `4*(n/100)^(2/9)` 在 look-1（n=60）得 **3**。若后续运行器未显式传 maxlag=4，look-1 的 RCI 将偏离冻结配置（SE 略小、更易判等价）。
- 位置：`runs/ledger.jsonl:40`（config `hac_se` 字段）vs `src/aionis/eval/sesoi_gate.py:224`
- 影响范围：Track B J-T 等价门 look-1 的推断口径。
- 修复建议：运行器调用 `look_summary`/`rank_ic_summary` 时显式传 `maxlag=4`，或加断言对齐冻结值。
- 修复成本：低
- 状态：待人工复核（取决于 a-run 运行器是否显式传参）

## 总体评分
9/10 — 红线级 commit 经受住独立复核：append-only、签名可复现、先冻结后观测、预注册-账本-实现三方数值一致；仅 maxlag 口径需在运行器侧确认显式传参。
