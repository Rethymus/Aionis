# Commit 40f3b9d4a7a046c26dccf27fa9bff633fa3082a4 审查报告

**提交信息**: refactor(web): decision-funnel nav + delete template cruft + daily fetch cron（2026-08-09，Rethymus）
**改动范围**: 53 个文件，+53/-8903；删除 13 个未使用的 shadcn-fintech 模板组件目录（accounts/cards/budgets/crypto/investments/transactions/transfers/notifications/settings/support/dashboard/analytics 等 ~8900 行），侧边栏重组为六步决策漏斗（nav.group.* zh/en），`refresh-terminal-data.yml` 新增 3 个 continue-on-error 抓取步骤（COT/Form4/Reddit）。

## 四维度结论
| 维度 | 结论 | 一句话说明 |
|---|---|---|
| 稳定性 | ✅ | 删除组件经 `git grep` 核验 0 引用方；build 绿 |
| 可扩展性 | ✅ | 漏斗分组由 i18n 标签承载，后续面板可按步骤归位 |
| 生产可用 | ⚠️ | cron 新增网络抓取叠加 30min job 超时，冷拉取有超时风险（后修） |
| 高内聚低耦合 | ✅ | 删除模板残渣大幅降低噪音；抓取失败保留旧缓存（continue-on-error） |

## 对照参考
continue-on-error + 末尾统一 git-auto-commit 的"尽力抓取、失败降级"模式与 GitHub Actions 官方 actions/cache/CI 数据刷新实践一致（docs.github.com/actions）；删除零引用模板组件是 shadcn 初始化项目的标准瘦身操作。

## 问题清单
### P2-1 cron 新增抓取未同步评估 30 分钟 job 超时
- 描述：本 commit 在 `refresh-terminal-data.yml` 加入 `cot_fetch.py`/`form4_fetch.py`/`reddit_fetch.py` 三个网络抓取步骤，但 job 级 `timeout-minutes: 30` 未调整。`continue-on-error` 只兜住步骤失败，兜不住 job 超时——Form 4 冷拉取（历史深挖）一旦超过 30min 会杀死整个 job，连累后面的 export/commit 步骤，当日数据刷新整体丢失。后续事实证明确实发生：82fbd6 将超时 30→45、06da220 再 45→75、1d75a7a 75→150，并加 66a3d3c/ cd239f3 步骤级超时上限。
- 位置：`.github/workflows/refresh-terminal-data.yml:31`（timeout）与 `:63-74`（新增抓取步骤）
- 影响范围：每日终端数据刷新 CI 的可靠性。
- 修复建议：加入抓取即评估超时预算，并给长抓取步骤设 step 级 timeout（后来正是这么修的）。
- 修复成本：低
- 状态：已在 82fbd6daaebd66a7f9be734451f3d87ac8ceb22e 修复（超时 30→45，后续 06da220→75、1d75a7a→150、66a3d3c 步骤封顶）

### P3-1 遗留死 i18n 键
- 描述：侧边栏不再使用 `nav.group.insights`/`nav.group.alternative`/`nav.group.monitor`，但 dict.ts zh/en 段均未删除这三个旧键。
- 位置：`web/src/i18n/dict.ts:6-9`（zh）、`:245-248` 附近（en）
- 影响范围：仅维护性/包体。
- 修复建议：删除三个未被引用的旧组键。
- 修复成本：低
- 状态：待人工复核（后续 9d260c9/e211a97 等大重构中是否顺手清理未确认）

## 总体评分
8/10 — 大胆且干净的 IA 重构（-8900 行零引用残渣、漏斗导航语义清晰），扣分在 cron 改动的超时预算欠考虑（后由 3 个 commit 补修）。
