# Commit 459d96e5673494626dc50a14480db3c74eab8fc2 审查报告

**提交信息**: feat(web): calibration reliability view (Track A slice A1 display)（2026-08-09，Rethymus）
**改动范围**: 6 个文件，+1436/-0；新增 `/calibration` 路由页 + `calibration-view.tsx`（225 行 recharts 可靠性图/ECE 折线）、`web/src/data/aionis/calibration_reliability.json`（1128 行真实导出数据）、data namespace 类型、zh/en i18n 各 21 键、侧边栏 nav 入口。

## 四维度结论
| 维度 | 结论 | 一句话说明 |
|---|---|---|
| 稳定性 | ✅ | `status!=="ok"`/空 regions/`series.at(-1)` 为空均回落到诚实占位或 return null；`pred_mean != null` 过滤空 bin |
| 可扩展性 | ✅ | 数据契约类型化（CalibrationReliability），i18n 双语对称，区域卡片按 Object.entries 泛化 |
| 生产可用 | ✅ | awaiting_fetch 守卫 + 方法学串 + 诚实 null 免责声明（#49 NULL 明示） |
| 高内聚低耦合 | ✅ | 纯展示组件消费静态 JSON，不触研究管线；display-only 边界保持 |

## 对照参考
对角线完美校准参考线用 `<ReferenceLine segment={[{x:0,y:0},{x:1,y:1}]} ifOverflow="extendDomain"/>`，与 recharts 官方 API（recharts.github.io ReferenceLine `segment` 画对角线段）及社区实践一致，且加了 `ifOverflow` 规避旧版 segment 截断 bug（recharts issue #2506）。参考 recharts.github.io。

## 问题清单
### P3-1 未使用的 i18n 键
- 描述：本 commit 新增 `calibration.perfect`、`calibration.eceHint` 两个键，但 `calibration-view.tsx` 中均未引用（ReferenceLine 无 label、ECE 图无 hint 文本），成为死键。
- 位置：`web/src/i18n/dict.ts:47,53`（zh 段；en 段对称）
- 影响范围：仅维护性/包体，无功能影响。
- 修复建议：给 ReferenceLine 加 `label={t("calibration.perfect")}` 或删除死键。
- 修复成本：低
- 状态：未修复

（另注：`RegionCard` 在 `series` 为空时静默 `return null`，与后端"region 存在则 series 非空"的约定一致，可接受。）

## 总体评分
9/10 — 展示层实现规范、防泄漏披露到位，仅一处死键瑕疵。
