# Commit 463136a90642c179554756a47c38060001a7282e 审查报告

**提交信息**: feat(web): 5-layer stock-picking funnel + theme regrouping（2026-08-11，Rethymus）
**改动范围**: 3 个文件，+280/-52；新增 `themes-funnel.tsx`（L0 context→L1 signals→L2 model→L3 validation→L4 cost 的 5 层漏斗总览，L2/L3 链到 picks/track hub），themes-view 按 funnel 层重组 7 张主题卡（提取 `ThemeCard` 组件消除重复），dict.ts 新增 zh/en 各 ~20 键。

## 四维度结论
| 维度 | 结论 | 一句话说明 |
|---|---|---|
| 稳定性 | ✅ | `ThemeCard` 提取后新旧渲染路径同构；空 signals/短 series 的边界处理保留 |
| 可扩展性 | ⚠️ | 主题卡分组由前端硬编码 `LAYER_KEYS` 决定，导出端新增主题会被静默隐藏 |
| 生产可用 | ✅ | 诚实标注 frozen LightGBM/rank-IC/power floor，不夸大预测器；跨面板闭环链接清晰 |
| 高内聚低耦合 | ✅ | 纯展示层（web/ only），漏斗概览与卡片分组解耦为两个组件 |

## 对照参考
不适用（纯展示重组）。literal-union Record 的类型安全 i18n 键模式与 next-intl/Next.js `<Link>` 用法同主流实践。

## 问题清单
### P3-1 不在 LAYER_KEYS 里的主题会被静默隐藏
- 描述：旧代码 `themes.map(...)` 渲染所有主题；新代码 `themes.filter((theme) => LAYER_KEYS[layer].includes(theme.key))` 只渲染白名单里的 key —— 导出端未来新增主题（或改 key）而前端忘同步时，该卡片不显示、无任何提示。
- 位置：`web/src/components/themes/themes-view.tsx:320-330`（该 commit 版本）
- 影响范围：/themes 面板对新主题的可见性（维护期风险，当前 7 键齐全）。
- 修复建议：分组后对未归层的 theme 渲染一个"ungrouped"兜底区块，或断言 `LAYER_KEYS` 覆盖导出全部 key。
- 修复成本：低
- 状态：不适用（该结构随后续 1b3fe8a9/2045a32a 的 IA 重组被整体替换）

## 总体评分
8/10 — 方向正确的信息架构重构（漏斗叙事 + 消除 ThemeCard 重复 + 双语齐全），仅白名单分组存在静默丢卡维护风险；本组件生命周期较短（后续被 argument-chain 范式取代）。
