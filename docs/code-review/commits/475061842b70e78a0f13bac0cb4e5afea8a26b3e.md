# Commit 475061842b70e78a0f13bac0cb4e5afea8a26b3e 审查报告

**提交信息**: test(eval): add realization invariant — proves embargo=5 is leak-free（2026-08-09，Rethymus）
**改动范围**: 1 个文件，+41/-0；`tests/test_track_adaptive_run.py` 新增 `test_expanding_arm_train_labels_realized_by_predict_time`。

## 四维度结论
| 维度 | 结论 | 一句话说明 |
|---|---|---|
| 稳定性 | ✅ | monkeypatch + synthetic fixture，hermetic |
| 可扩展性 | ✅ | 不变式独立成测试，embargo 参数化 |
| 生产可用 | ✅ | 把"embargo=5 足够"的关键论证固化为回归防线 |
| 高内聚低耦合 | ✅ | 只测 run_expanding_arm 的泄漏不变式，单一职责 |

## 对照参考
不适用（单测新增；不变式"训练标签实现时刻 ≤ 预测时刻"是 purge/embargo 文献的标准判定，与 López de Prado CPCV 的 purge 语义一致）。

## 问题清单
通过。审查者复核了该 commit 对外部评审"CRITICALLY insufficient"质疑的裁决，**裁决正确**：
- 预测发生在周收盘 t_w（close[t_w] 已知），扩窗臂 cutoff = t_w−5 sessions ⇒ 每条训练标签的实现时刻（date+5 sessions）≤ t_w，无前视；要求严格 '<' 会错误排除恰在 t_w 实现的标签。
- 测试断言 `idx_train_max + 5 <= idx_test` 直接固化该不变式——对当前实现是"构造性通过"，但作为回归防线有效（若有人把 embargo 改小/换成日历日，会立刻失败）。
- 微小瑕疵（不构成问题）：`realize_idx = min(idx_train_max + 5, len(sess) - 1)` 的 clamp 实际为死代码——train 行已 dropna(fwd_5s)，其 +5 session 必在面板内。

## 总体评分
9/10 — 用不变式测试正确终结了一场泄漏争议，论证与裁决质量高。
