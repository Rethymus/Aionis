# Commit 482144626cfbf8207fbba5a5153c77081fc006be 审查报告

**提交信息**: chore(data): re-export form4 with full 5-issuer deep history (2013-2026, 13544 txns)（2026-08-08，Rethymus）

## 跳过原因
纯机器生成数据文件再导出：仅 `web/src/data/aionis/form4.json` 一个 JSON 展示载荷（+273/-258，由 1a0f4bf5 引入的后台 merge-by-issuer 抓取深化后重新运行导出器产生），无任何源码/配置/CI/文档改动，属规程定义的 skip 类（同 "chore(data): daily terminal refresh"）。红线检查：display-only 数据、无代码路径、无研究管线接触——通过。

顺带观察（不构成问题计数）：JSON 内 methodology 字符串仍写 "Window spans 2016→today (Trump Era)"，而本导出实际窗口已扩至 2013–2026——该字符串由 `export_form4` 硬编码（1a0f4bf5 引入动态 window 字段时未同步改方法论文案），建议后续把文案改为引用动态窗口。
