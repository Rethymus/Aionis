# Commit 4ae335ea2b10282f5355128e93e04abb80f6f9d9 审查报告

**提交信息**: feat(web): special alt-data modules — TACO pressure index + Reddit retail buzz（2026-08-06，Rethymus）
**改动范围**: 12 个文件，+1433/-3；新增 /taco 与 /reddit 两个展示页（taco-view.tsx / reddit-view.tsx）、export_terminal_data.py 增加 export_taco + export_reddit_meta 导出、侧边栏"另类数据"分组、i18n 双语词条、taco.json/reddit.json 数据与状态文档。

## 四维度结论
| 维度 | 结论 | 一句话说明 |
|---|---|---|
| 稳定性 | ⚠️ | export_taco 无守卫直读 data/cache/alfred_VIXCLS.json，缓存缺失时整个导出脚本崩溃 |
| 可扩展性 | ⚠️ | 5 条关税事件硬编码在函数体内，事件表无法外部维护 |
| 生产可用 | ✅ | 展示层模块，方法论披露诚实（"示意性、非研究 claim"），Reddit 零快照如实报告而非 mock |
| 高内聚低耦合 | ✅ | 数据经静态 JSON 导入（build-time），web 不触碰研究管线；边界清晰 |

## 对照参考
展示组件采用 shadcn/ui Card + recharts `ResponsiveContainer/LineChart` 组合，与 recharts 官方示例（recharts.org/en-US/examples）及 shadcn/ui 官方 cards 模式一致；"诚实占位"（状态卡片而非假数据）与 Next.js 官方 loading/empty-state 指南（nextjs.org/docs）方向一致。无新算法逻辑，对照结论：实现规范。

## 问题清单
### P3-1 export_taco 无守卫读取本地缓存文件
- 描述：`raw = json.loads(Path("data/cache/alfred_VIXCLS.json").read_text())` 无 try/except；该缓存文件位于 gitignored 的 data/ 下，fresh checkout / CI 环境缺失时 FileNotFoundError 会使 main() 整体中断，殃及同批的 picks/metrics 等导出。本 commit 时点为本地手动运行脚本，风险有限。
- 位置：`scripts/export_terminal_data.py:110`（export_taco 内）
- 影响范围：export_terminal_data.py 全部导出（崩溃时 taco 之前的文件已写、之后的 reddit.json 不写）。
- 修复建议：读取处捕获 FileNotFoundError/JSONDecodeError，跳过并告警，或前置存在性检查。
- 修复成本：低
- 状态：已在 a08ec16 修复（后续引入 `_safe_export("taco", export_taco)` 包装，仅吞 FileNotFoundError、保留已提交 JSON）

### P3-2 关税事件表硬编码于导出函数
- 描述：`events = [{"date": "2025-02-01", ...}, ...]` 5 条事件以字面量写死在 export_taco 函数体内；更新事件需改源码并重跑导出，且事件表与 taco.json 中的已生成数据无版本对账。
- 位置：`scripts/export_terminal_data.py:119-125`
- 影响范围：仅展示层数据新鲜度与可维护性；无研究影响。
- 修复建议：事件表外置为 data/ 下 JSON/YAML 输入或模块级常量并注明来源与更新日期。
- 修复成本：低
- 状态：未修复（后续 commit 将 TACO 事件扩展进 fetch 流程，硬编码样式延续）

## 总体评分
8/10 — 展示层模块边界干净、反泄漏与"不造 mock"纪律执行到位，仅有缓存读取无守卫与事件表硬编码两处小瑕疵。
