# Commit 4c47add21fca97ac42154aac6fe5d0bc48a1e5d2 审查报告

**提交信息**: feat(scripts): power-floor theoretical derivation (pure-noise bound + ML excess)（2026-08-05，Rethymus）
**改动范围**: 5 个文件，+407；新增 `scripts/ic_pure_noise_bound.py`（闭式纯噪声 Spearman 下界 1/sqrt(N-1) vs 实测 σ(IC)，21 系列）+ 6 个 hermetic 测试 + 推导 note + manuscript §5.3 + state。

## 四维度结论
| 维度 | 结论 | 一句话说明 |
|---|---|---|
| 稳定性 | ✅ | N<2 返回 NaN、缺失列返回空 dict、文件缺失 continue，边界处理完整 |
| 可扩展性 | ✅ | glob 模式常量化，per-region/combined 分支统一 |
| 生产可用 | ✅ | 数学正确（Var(r_s)=1/(n-1) 为标准结果），结论方向诚实（floor 由 excess 驱动而非纯下界） |
| 高内聚低耦合 | ✅ | 只读持久化工件，与 survey 脚本互补不重叠 |

## 对照参考
闭式 Var(Spearman rho)=1/(n-1) 属标准秩相关结果，与 scipy.stats.spearmanr 文档及经典秩统计教材（Kendall's Advanced Theory）一致；n_min 推导与 76bacbfd 的 `_min_n_for_equivalence` 同式。对照 arch/statsmodels 生态无直接对应物（这是解析推导）。出处：docs.scipy.org。

## 问题清单
### P3-1 combined 臂的纯噪声下界用「合并 N」作代理，未注明近似性质
- 描述：Track C 的 combined IC 实为 us/cn 月度 IC 的等权平均（`src/aionis/eval/track_c_joint.py:457-459`），其零假设方差应为 (1/(N_us−1)+1/(N_cn−1))/4；脚本对 combined 臂取 pooled `n_cross_median`（如 929）套 1/sqrt(N−1)。两区规模均衡时二者数值几乎重合（均衡 N 下 (2/(N−1))/4 ≈ 1/(2N−1)），结果无害，但代码与 note 均未注明这是代理近似，若未来两区规模失衡会静默偏离。
- 位置：`scripts/ic_pure_noise_bound.py:87-89`
- 影响范围：combined 臂 excess_ratio 的理论参照（exploratory 展示精度）。
- 修复建议：对 combined 臂改用 (1/(N_us−1)+1/(N_cn−1))/4 的精确式，或加注释注明均衡近似。
- 修复成本：低
- 状态：未修复

### P3-2 main() 打印中的硬编码展示值
- 描述：`("observed median", 0.109)` 与 `("pure-noise (N=462)", pure_noise_sigma(462))` 为字面量（来自 30e27d89 survey 的结果转录），非由本轮 rows 计算；若换面板重跑，打印与 JSON 不同步。
- 位置：`scripts/ic_pure_noise_bound.py:165-168`
- 影响范围：仅控制台展示。
- 修复建议：由本轮 ratios/rows 现算展示值。
- 修复成本：低
- 状态：未修复

## 领域红线核查
合规：exploratory、只读持久化工件、不写 ledger、不动冻结面。自我审计日志（bound 0.047 < 0.10 是否削弱结论）推理正确——该发现实际上把「σ≈0.10 地板」细化为「ML 噪声超额」，manuscript §5.3 表述与之一致且更诚实。

## 总体评分
9/10 — 数学正确、边界处理完整、结论比前序 commit 更诚实精确；仅剩两个低危展示层近似/硬编码 nit。
