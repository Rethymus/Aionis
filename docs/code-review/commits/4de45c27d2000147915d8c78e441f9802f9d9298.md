# Commit 4de45c27d2000147915d8c78e441f9802f9d9298 审查报告

**提交信息**: feat(eval): E1 horizon-robustness sweep (h=10/42) + TASK-STRAT decision brief（2026-07-30，Rethymus）
**改动范围**: 10 个文件，+775/-52；核心代码 `scripts/sensitivity_horizon.py`（E1 双臂 h∈{10,42} 扫描 + 退化护栏）、`scripts/phase_b_run.py`（PHASE_B_NO_LEDGER artifacts-only 模式 + 冻结 sig 守卫）；其余为 TASK-STRAT 决策简报、ledger +1 行 exploratory、state/tasks 文档沉淀。

## 四维度结论
| 维度 | 结论 | 一句话说明 |
|---|---|---|
| 稳定性 | ✅ | E1 退化（peer_earnings_surprise 全 NaN）时降级为 null_holds=None 而非崩溃，B/C/D 仍落账；`_print_table` 对退化行有独立打印分支不会触发 None 格式化崩溃 |
| 可扩展性 | ⚠️ | `_build_self_prop` 与 `scripts/phase_e1_run.py` 近逐字复制（约 40 行 PIT 构造），仅日志前缀不同，存在漂移风险 |
| 生产可用 | ✅ | 账本纪律合规：exploratory 追加 1 行、confirmatory 数量不变；artifacts-only 模式在计算任何指标/写任何 artifact 之前先校验 sig==FROZEN，fail-closed；序列化与 `commit_config` 完全一致（`sha256(json.dumps(sort_keys=True))`） |
| 高内聚低耦合 | ✅ | E1 用 `differential(ic_pr, ic_bs)`（prop vs base_self）而非共享 base_ic，注释明确说明"prop vs fundamentals-only 无科学意义"的结构陷阱，复用 `phase_c.differential`/`two_arm.run_arm_oos` 而非新造 |

## 对照参考
冻结 sig 守卫（rerun 必须与已入账 hash 一致否则中止）与 DVC 的 `dvc.lock` 依赖 md5 锁定同构：DVC 在依赖 hash 漂移时拒绝 `repro` 并标记 "changed deps"，需显式 `dvc commit` 才接受新状态（[doc.dvc.org/command-reference/commit](https://doc.dvc.org/command-reference/commit)）。本 commit 的做法（fail-closed + 显式 FROZEN 常量）等价且更严格——不允许静默接受。退化护栏（coverage guard → 优雅降级 + 账本如实记 note）符合 sklearn `check_array`/coverage-check 的惯例。

## 问题清单
### P3-1 `_build_self_prop` 在两个 runner 间逐字复制（DRY 漂移风险）
- 描述：`sensitivity_horizon._build_self_prop` 文档串自述"mirrors `scripts.phase_e1_run._build_self_prop` exactly"，两份实现仅日志前缀（`[E1]` vs `[H]`）与断言文案不同。这是 E1 的 PIT 核心构造（filed-date surprise、13D filing_date、ex-self SIC 传播）；若将来只改其中一份，horizon 扫描与 confirmatory 运行将静默度量两种不同构造，且无测试强制两者一致。
- 位置：`scripts/sensitivity_horizon.py:125`（`_build_self_prop` 定义）与 `scripts/phase_e1_run.py`（同名函数）
- 影响范围：E1 horizon-robustness 结论与 E1 confirmatory 结论的可比性；后续维护者。
- 修复建议：将 `_long` + `_build_self_prop` 提升为 `src/aionis/features/propagation.py`（或 `eval/phase_e1.py`）的共享函数，参数化日志前缀；两个 runner import 同一份。
- 修复成本：低
- 状态：未修复（至 main 两文件均未再改动，重复保留）

## 总体评分
8/10 — 防泄漏纪律与账本纪律执行到位（共享折、退化护栏、fail-closed 冻结守卫），唯一实质问题是 PIT 构造的跨脚本复制。
