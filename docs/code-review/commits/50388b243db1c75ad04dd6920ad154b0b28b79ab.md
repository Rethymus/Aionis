# Commit 50388b243db1c75ad04dd6920ad154b0b28b79ab 审查报告

**提交信息**: fix(ingest): GDELT news_sentiment — ECON_MKT (invalid) → ECON_STOCKMARKET（2026-08-11，Rethymus）
**改动范围**: 3 个文件，+33/-19；`src/aionis/ingest/news_sentiment_gdelt.py`（主题码修正 + 间隔 5s→15s）、`scripts/news_sentiment_gdelt_fetch.py`（docstring 同步）、`.github/workflows/refresh-terminal-data.yml`（GDELT 步骤 20min step-cap）

## 四维度结论
| 维度 | 结论 | 一句话说明 |
|---|---|---|
| 稳定性 | ⚠️ | 主题码根因修对、有 CI run 证据链；但同 commit 引入重复常量留下隐患（已被下一 commit 清掉），且 step-cap 注释与持久化事实不符 |
| 可扩展性 | ✅ | 单一 `DEFAULT_THEME` 常量即一处修复（KISS，无运行时 fallback），符合原作者预留的修复路径 |
| 生产可用 | ✅ | 15s 间隔 + bounded retry + step-cap 三层防 429/超时；display-only 边界在 docstring 中重申 |
| 高内聚低耦合 | ✅ | HTTP 仍走项目 `HttpRequestPolicy`（未用 gdeltdoc 自带裸 requests 路径），模块边界清晰 |

## 对照参考
GDELT 官方博客（blog.gdeltproject.org）确认：DOC 2.0 API 的礼貌下限是请求间隔 ≥5s，超限返回 HTTP 429，且限流是动态的（重大新闻事件期间收紧、配额按"几分之一 QPS"粒度执行）——本 commit 从 5s 提到 15s 的决策与官方配额机制及通用 429 处理最佳实践（放慢节奏而非仅重试）一致；`gdeltdoc`（MIT）的 Filters 仅用于查询串构造、绕开其无礼貌间距的 HTTP 路径，是正确的取舍。来源：blog.gdeltproject.org（DOC 2.0 API Debuts / API Quotas & The Impact Of A Fraction Of A QPS）。

## 问题清单
### P2-1 引入重复的 `_GDELT_MIN_INTERVAL` 常量（5.0 与 15.0 并存，靠绑定顺序决定生效值）
- 描述：本 commit 新增 `_GDELT_MIN_INTERVAL = 15.0`（第 75 行）却没有删除旧的第 53 行 `_GDELT_MIN_INTERVAL = 5.0  # GDELT demands ≥5s (stricter than project 2s default)`。Python 模块级后绑定生效，运行时确为 15s，但存在两处同名定义：读者易被旧注释误导，且若未来有人把新定义上移或删除其一，间距会静默回落到 5s（正是触发 429 的值）——顺序依赖的隐式正确性。
- 位置：`src/aionis/ingest/news_sentiment_gdelt.py:53`（旧）与 `:75`（新，本 commit 版本行号）
- 影响范围：仅本模块的 `HostSpacingPolicy`；当前运行时行为正确，风险是维护期回归。
- 修复建议：删除第 53 行旧定义，保留唯一常量。
- 修复成本：低
- 状态：已在 d5e02b0366be88a27bb80384480713c1512c7055 修复（紧随其后的 "remove stale duplicate _GDELT_MIN_INTERVAL"）

### P2-2 workflow 注释声称"超时可续传"，但该版本持久化只在全部 chunk 完成后发生
- 描述：本 commit 在 `refresh-terminal-data.yml` 新增的注释写 "Incremental cache means a timeout resumes on the next run without re-fetching cached months"，但在本 commit 版本中 `collect_news_sentiment` 只在 `fetch_tone_series` 全部 chunk 结束后一次性写缓存（`(cache_dir / _CACHE_NAME).write_text(...)` 在函数末尾）。冷拉取（38 个季度 chunk × ≥15s + 429 退避）一旦撞上同 commit 加的 20min step-cap，已抓取的 ~25 个 chunk 全部丢失，下次运行从头冷拉——注释与代码事实相反。
- 位置：`.github/workflows/refresh-terminal-data.yml:112-114`（注释）；`src/aionis/ingest/news_sentiment_gdelt.py:352`（末尾统一写缓存）
- 影响范围：CI 日频刷新的 GDELT 步骤；冷启动期间每天可能白抓 20 分钟数据（continue-on-error 掩盖失败）。
- 修复建议：逐 chunk 增量落盘（on_chunk 回调 checkpoint）。
- 修复成本：中
- 状态：已在 34eacb23740895628680bc369521098f1ffb5a86 修复（per-chunk checkpoint，含超时存活测试）

## 总体评分
8/10 — 根因诊断（无效主题码）与修复路径干净、证据链完整；扣分在同 commit 引入常量重复与一处与代码不符的 CI 注释（均已在后续 1~3 个 commit 内自愈）。
