# Commit 51b6d38aa5de686fb2a2e5974096a7c65cffd65e 审查报告

**提交信息**: fix(ci)+feat(data): form4 back to 5 issuers (CI reliability) + smart_money 2025 filled（2026-08-11，Rethymus）
**改动范围**: 2 个文件，+11/-8；`scripts/form4_fetch.py` 的 ISSUERS 从 10 个回退到 5 个（10 issuer 冷拉超 45 分钟预算导致 job 取消、后续步骤全跳过），smart_money.json 补齐 2025 年回填（yearly 2025=11280、2026=6360、total 21732）。

## 四维度结论
| 维度 | 结论 | 一句话说明 |
|---|---|---|
| 稳定性 | ✅ | 回退到实测 <30min 的 5-issuer 集合，恢复 cron 可完成性；数据改动为本地重导出产物 |
| 可扩展性 | ⚠️ | issuer 集合仍硬编码于脚本，扩展依赖人工计时；注释出现新旧两版叠放 |
| 生产可用 | ⚠️ | "被裁 5 个 CIK 零成交"的解释与 EFTS 查询代码对不上，可能掩盖覆盖率问题 |
| 高内聚低耦合 | ✅ | CI 可靠性决策与数据回填分处两个文件，互不纠缠 |

## 对照参考
不适用（配置收缩 + 数据导出更新）。CI 预算内收缩批处理集合属常规运维决策；无新逻辑可对照。

## 问题清单
### P2-1 「META/TSLA/BRK.B/JPM/LLY 无 EFTS 命中」的说法存疑，可能掩盖覆盖率缺陷
- 描述：commit message 断言被裁的 5 个 mega-cap "contributed no transactions anyway (their CIKs resolved to no EFTS hits)"。但 `_efts_url`（form4_efts.py:49-55）按 10 位零填充 CIK + `forms=4` 查询，META/TSLA/JPM 2016-2026 有 Form 4 是公开事实；更简单的解释是 job 在轮到它们之前就被取消/超时。若真是"零命中"，则说明 EFTS `ciks=` 过滤在部分 issuer 上失配 —— 同一机制也可能让保留的 5 个 issuer 覆盖不全，但被当作"正常"接受了。
- 位置：`scripts/form4_fetch.py:26-33`（该 commit 版本；关联 `src/aionis/ingest/form4_efts.py:49-66`）
- 影响范围：/insiders 面板的 issuer 覆盖完整性判断；后续"扩展 issuer 需先计时冷跑"的决策依据。
- 修复建议：单独冷跑一次 META/TSLA 的 EFTS 查询并记录命中数，把结论（真零命中 vs 没跑到）写进注释/state；若是 ciks 失配应改用 `q=` ticker 或 submissions 端点。
- 修复成本：低
- 状态：待人工复核

### P3-1 ISSUERS 上方注释新旧两版叠放
- 描述：回退时只删了字典条目，旧注释 "Small bounded set keeps the pull polite + fast; expand cautiously..." 与新注释 "Bounded to 5 — ... 10 issuers timed out..." 同时保留，6 行重复铺垫。
- 位置：`scripts/form4_fetch.py:26-30`（该 commit 版本）
- 影响范围：可读性。
- 修复建议：删除旧版两行。
- 修复成本：低
- 状态：未修复

### P3-2 硬编码 `END = "2026-08-31"` 时间炸弹（本 commit 未引入，顺带记录）
- 描述：`START/END` 固定字符串，2026-09 起 daily cron 的抓取窗口上界将早于 today，增量抓取会静默停止或空转（该常量由 1a0f4bf 引入，非本 commit 责任，但本 commit 触碰该文件却未处理）。
- 位置：`scripts/form4_fetch.py:36`
- 影响范围：2026-09 起的 /insiders 数据新鲜度。
- 修复建议：`END = date.today().isoformat()`。
- 修复成本：低
- 状态：未修复

## 总体评分
7/10 — 正确的可靠性回退（数据支撑：10-issuer 超时记录），但"被裁 issuer 零命中"的解释未经证实即写入决策叙事，且留下注释叠放与既有时间炸弹未清。
