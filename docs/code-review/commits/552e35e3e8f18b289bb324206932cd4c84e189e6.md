# Commit 552e35e3e8f18b289bb324206932cd4c84e189e6 审查报告

**提交信息**: fix(scripts)+test: form4_fetch buy_or_sell column + web terminal data contract tests（2026-08-07，Rethymus）
**改动范围**: 2 个文件，+93/-2；修复 form4_fetch.py 汇总打印引用已更名列的 KeyError，新增 tests/test_web_terminal_data.py（cot.json / form4.json / pick_conviction.json 三个 tracked 载荷的 6 条契约测试）。

## 四维度结论
| 维度 | 结论 | 一句话说明 |
|---|---|---|
| 稳定性 | ✅ | 修复正确且补上了防止 React 依赖的 JSON schema 漂移的守卫 |
| 可扩展性 | ✅ | 契约测试按载荷分节，新增面板可照模式扩展 |
| 生产可用 | ✅ | 锁定 P/S→buy/sell 语义作回归守卫，直接针对此前 hollow-green 教训 |
| 高内聚低耦合 | ✅ | 纯 tracked-artifact 检查，hermetic 无网络/无 runs 依赖 |

## 对照参考
不适用（一行脚本修复 + pytest 契约测试；模式与 pytest 官方推荐的断言式数据契约一致，无新算法）。

## 问题清单
### P3-1 契约测试的 conviction 枚举窄于生产者枚举
- 描述：`assert pc["conviction"] in {"high", "low"}`，而 export_pick_conviction 在无数据时可产出 "unknown"——生产者枚举是 {high, low, unknown}。一旦出现 unknown（OOS parquet 缺失的退化态），契约测试会以断言失败而非跳过告终；若这是有意"大声失败"应加注释，否则应对齐枚举。
- 位置：`tests/test_web_terminal_data.py:89`
- 影响范围：退化态下测试失败的语义清晰度。
- 修复建议：允许 "unknown" 并另断言 unknown ⇒ series 为空，或注释声明大声失败是有意的。
- 修复成本：低
- 状态：待人工复核（后续 current.md 版本 conviction 枚举未变）

另注（非问题）：status != "ok" 时相关断言整段 return（跳过式通过），这是 tracked-artifact 契约测试的固有折衷——退化态下 schema 不被检验，属已知设计而非缺陷。

## 总体评分
9/10 — 小而准的修复 + 面向真实教训（P/S 语义、schema 漂移）的契约守卫，是前两个 commit 教训的正确沉淀；仅枚举对齐一处小瑕疵。
