# Commit 5c64e571829ecf30b97eace76604091828fef1ce 审查报告

**提交信息**: feat(track-b): price-only baseline arm (#42) + differential (null, non-significant)（2026-08-02，Rethymus）
**改动范围**: 5 个文件，+222/-25；`runs/ledger.jsonl` 追加 config_committed 行 #42（price-only 10 特征基线臂，sig 9af9e8b...）；`track_b_a_run.py` 扩展 `--mode differential`（双臂 + 按月配对 IC 差分 + HAC）；新增 `docs/track-b-results.md`（151 行）；state 更新。

## 四维度结论
| 维度 | 结论 | 一句话说明 |
|---|---|---|
| 稳定性 | ✅ | argparse 选项校验、逐臂隔离执行、差分 dropna 后 n_months 显式打印 |
| 可扩展性 | ⚠️ | ARMS/ARM_CONFIG 硬编码两臂（配置仍以标签字符串绑定，未机制化） |
| 生产可用 | ✅ | 账本纪律严格：#42 freeze（13:03 UTC）先于观察（13:21 UTC 提交）；结果如实报 null 且明示「NOT strict equivalence（CI 上界 0.020 > SESOI 0.010）」 |
| 高内聚低耦合 | ✅ | 复用 fit_track_b_baseline + rank_ic_summary，无新轮子；文档/账本/状态分层清晰 |

## 对照参考
Diebold & Mariano (1995)《Comparing Predictive Accuracy》（jstor.org/stable/1392155）与 Newey-West HAC 实践（en.wikipedia.org/wiki/Newey–West_estimator、stats.stackexchange.com）：成对差分序列 dₜ 上做 E[d]=0 的 HAC 检验正是 DM 框架——两臂同期相关由差分自动处理，本 commit 对「按月配对 IC 差分 + Newey-West」的实现与该标准一致。文献同时警示**嵌套模型**（treatment 特征集包含 price-only）下 DM 类检验功效偏低、易偏向零假设——本 commit 结论方向是 null，且明确拒绝宣称等价，处理诚实；n=66 个月未用 HLN 小样本修正，属可接受近似。

## 问题清单
### P2-1 账本行 #42 内部字段自相矛盾（feature_cols_amendment 文案复制自 #41）
- 描述：#42 行的 `feature_cols` 是 10 个价格特征，但同行的 `feature_cols_amendment` 字段仍是 #41 的原文 "S0-S02 materialized **23 pre-specified features (13 fundamentals + 10 price)**"——append-only 账本中该行自我矛盾，需依赖末尾 `feature_cols_note`（"10 PRICE-ONLY features..."）才能正确解读。账本是审计的最终事实源，字段级不一致会误导后续自动/人工审计。
- 位置：`runs/ledger.jsonl:42`（本 commit 追加行）
- 影响范围：审计链可读性；任何按 feature_cols_amendment 字段做解析的消费方。
- 修复建议：append-only 下不可改写——后续为 #42 追加一条勘误行，或在消费侧统一以 `feature_cols` 数组为准。
- 修复成本：低
- 状态：未修复

### P3-1 `_run_arm` 返回类型注解为 `object`
- 描述：`def _run_arm(...) -> object`——差分路径用到 `rt.ic_series/rp.ic_series`，类型信息全部丢失，IDE/静态检查失效。
- 位置：`scripts/track_b_a_run.py:26`
- 影响范围：可维护性。
- 修复建议：`-> TrackBBaselineResult`。
- 修复成本：低
- 状态：未修复

### P3-2 双臂仍以打印字符串绑定 config sig（继承 30e2a2bc 的 P1 缺口）
- 描述：`ARM_CONFIG = {"treatment": "#41 (sig bf620b...)", ...}`——本 commit 把无校验的标签模式扩展到两臂；生效参数与账本 sig 的哈希比对仍缺位（详见 30e2a2bc 报告 P1-1，不重复计数）。
- 位置：`scripts/track_b_a_run.py:24`
- 影响范围：同 30e2a2bc P1-1。
- 修复成本：中
- 状态：未修复

## 总体评分
8/10 — 预注册主张的完整评估落地：freeze 先于观察、配对差分方法论正确、null 与「非严格等价」边界披露诚实；扣分在账本行字段级不自洽与配置绑定仍是标签而非机制。
