# Commit 616253e65efbfcfe162bfc3bc4220f791528db13 审查报告

**提交信息**: fix(ingest): stakes_13d _get_json — fail fast on permanent 4xx, retry only transient（2026-07-29，Rethymus）
**改动范围**: 2 个文件，+68/-13；`src/aionis/ingest/stakes_13d.py` 的 `_get_json` 重构（4xx≠429 立即失败，仅网络/5xx/429 退避重试，直接读 `status_code` 取代 `raise_for_status`）+ 2 个新测试。

## 四维度结论
| 维度 | 结论 | 一句话说明 |
|---|---|---|
| 稳定性 | ✅ | 重试分类正确：网络异常/5xx/429 → 退避重试；4xx(≠429) → 立即 RuntimeError；最终 attempt 后不再空睡（`attempt < retries-1` 守卫） |
| 可扩展性 | ✅ | 无新参数，行为契约写进 docstring；测试 `_Resp` 增加 status_code 使 fake 对齐真实 response 接口 |
| 生产可用 | ✅ | 坏 CIK 从 ~24s×N 的 fail-slow 变为立即失败，批量拉取 587 issuer 时收益显著 |
| 高内聚低耦合 | ✅ | 变更局限在单个私有函数，调用方签名不变 |

## 对照参考
与 [urllib3 官方 Retry 文档](https://urllib3.readthedocs.io/en/stable/reference/urllib3.util.html) 及社区共识（[MajorNetwork](https://majornetwork.net/2022/04/handling-retries-in-python-requests/)、[ScrapeOps](https://scrapeops.io/python-web-scraping-playbook/python-requests-retry-failed-requests/)：`status_forcelist=[429,500,502,503,504]`，404/400/401/403 永不重试）完全一致——本实现"429 例外重试、其余 4xx fail-fast"正是该白名单语义的手写版。出处域名：urllib3.readthedocs.io、majornetwork.net、scrapeops.io。

## 问题清单
### P3-1 HTTP 200 但 body 非 JSON 时抛裸 JSONDecodeError，脱离统一错误契约
- 描述：重构后 `data = r.json()` 移出了 try 块——200 响应体损坏时 `requests.exceptions.JSONDecodeError` 直接向外传播，不再被包装成 `_get_json` 统一的 `RuntimeError("fetch failed for ...")` 契约，也不重试。影响极小（data.sec.gov 返回 200+非 JSON 属异常场景），仅错误类型不一致。
- 位置：`src/aionis/ingest/stakes_13d.py:81`
- 影响范围：调用方按 RuntimeError 捕获的路径（批量脚本）在该场景会漏接。
- 修复建议：包一层 try 或在最终 RuntimeError 中兜底。
- 修复成本：低
- 状态：未修复（e8545d40a34b6cf73444d4621926c17ac6d707857 以 `_policy_get` 重写后，`r.json()` 仍位于 try/except 之外，该边缘被原样继承）

## 总体评分
9/10 — 精准的小修复，重试语义与业界标准一致，测试把 fail-fast 与 5xx-重试两个方向都钉死。
