# Commit 63aec0133829f5238466287424ec2cc89d65f9a0 审查报告

**提交信息**: feat(web): pick-conviction index — Aionis-unique model meta-signal（2026-08-06，Rethymus）
**改动范围**: 9 个文件，+425/-3；export_terminal_data.py 新增 export_pick_conviction（OOS 评分逐月截面分散度：std + 头尾 decile 价差）、/conviction 展示页（双线 recharts）、pick_conviction.json、侧边栏与 i18n。

## 四维度结论
| 维度 | 结论 | 一句话说明 |
|---|---|---|
| 稳定性 | ✅ | groupby+dropna+len<10 跳过，空数据路径有 None 兜底，本地导出脚本语境下足够 |
| 可扩展性 | ✅ | 聚合逻辑集中单一函数，参数（窗口 12/24、decile 0.1/0.9）虽硬编码但语义稳定 |
| 生产可用 | ⚠️ | 方法论字符串引用文献作者有误（Kelly-Pruitt-Su），UI KPI 标签错位 |
| 高内聚低耦合 | ✅ | 复用既有 OOS parquet 工件派生展示指标，不产生新数据、不触研究管线 |

## 对照参考
截面分散度与动量关联的学术出处实为 Kelly, Moskowitz & Pruitt (2021, JFE 140(3)) "Understanding Momentum and Reversal"（ideas.repec.org / sethpruitt.net）——commit 中方法论字符串与 i18n 写的 "Kelly-Pruitt-Su" 作者列表有误（无 Su；Su 是 Kelly 的另一篇 demand-system 合作者）。计算本身（groupby→quantile/std）为 pandas 官方文档标准用法（pandas.pydata.org），无 API 误用。

## 问题清单
### P3-1 方法论文献引用作者错误
- 描述：docstring 与 UI 文案（`conviction.explain` 中英双语、pick_conviction.json methodology）均写 "cf. Kelly-Pruitt-Su"；对应文献实为 Kelly, Moskowitz & Pruitt (2021)。项目自身后续做过 citation-integrity audit（20191fd），此处属于同类精度问题。
- 位置：`scripts/export_terminal_data.py:134`（docstring）；`web/src/i18n/dict.ts`（conviction.explain 中英两处）
- 影响范围：展示层方法论披露准确性；对研究无影响。
- 修复建议：改为 Kelly-Moskowitz-Pruitt (2021, JFE)。
- 修复成本：低
- 状态：未修复（main 上文案未更正）

### P3-2 trailing-12m 均值包含最新月自身
- 描述：`recent = rows[-12:]` 含最新月，`trailing_mean` 把 latest 计入基准再与 latest 比较，比较基准被自身拉动、略偏"低确信"方向；严格应为 rows[-13:-1] 的前 12 月。
- 位置：`scripts/export_terminal_data.py:157-159`
- 影响范围：conviction 高/低判定阈值轻微偏移（展示层示意指标）。
- 修复建议：trailing 窗口排除当月。
- 修复成本：低
- 状态：未修复（main 同版）

### P3-3 第二张 KPI 卡标签复用了副标题文案
- 描述：KPI 数组第二项 `label: "conviction.window"`，其文案是整句副标题"模型 OOS 评分的截面分散度 · 每月"，但卡片值是 `latest.std.toFixed(3)`——标签与数值语义不匹配，应为"最新 std"类标签。
- 位置：`web/src/components/conviction/conviction-view.tsx:39`
- 影响范围：/conviction 页 KPI 卡可读性。
- 修复建议：新增 `conviction.latest_std` 词条并替换。
- 修复成本：低
- 状态：未修复（main 同版）

## 总体评分
8/10 — 用截面分散度替换"峰度=确信度"的语义纠偏值得肯定，计算实现干净；扣分在引用作者错误与两处小 UI/统计瑕疵。
