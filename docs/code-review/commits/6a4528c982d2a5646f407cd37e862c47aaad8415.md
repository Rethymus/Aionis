# Commit 6a4528c982d2a5646f407cd37e862c47aaad8415 审查报告

**提交信息**: fix(res): harden RES runners with SIG_ONLY + ledger gate; fix FF5 parser footer bug（2026-08-03，Rethymus）
**改动范围**: 4 个文件，+87/-0；为 res_03 runner 补上 config_committed 闸门（镜像 RES-02）、两 runner 新增 SIG_ONLY 模式（打印 sig 后在任何 OOS 指标前退出）、FF5 解析器 footer 过滤修复 + 回归测试。

## 四维度结论
| 维度 | 结论 | 一句话说明 |
|---|---|---|
| 稳定性 | ✅ | footer 过滤在 dtype coerce 前执行，带真实格式回归测试（Copyright 2026 Fama/French 行） |
| 可扩展性 | ⚠️ | `_ledger`/`_require_owner_commit` 在两个 runner 间复制粘贴（未提取共享 helper） |
| 生产可用 | ✅ | 关键修复：RES-03 的 config_committed-before-result 闸门从「文档声明」变为代码强制；SIG_ONLY 让 owner 可以先取 sig 再落 ledger 行 |
| 高内聚低耦合 | ✅ | 改动局限在两个脚本 + 解析器 + 测试 |

## 对照参考
footer 过滤与 pandas-datareader `famafrench.py` 的空行分节解析、手工 `read_csv` 的 `skipfooter` 惯例一致（Ken French 文件尾部 Copyright 行是已知解析坑；见 randlow.github.io 教程与 github.com/pydata/pandas-datareader）。`str.fullmatch(r"\d{8}", na=False)` 在 coerce 前过滤是稳妥做法。

## 问题清单
### P3-1 footer 行被静默丢弃，无告警日志
- 描述：`df = df[df["date"].str.fullmatch(r"\d{8}", na=False)]` 静默丢弃所有非 8 位日期行——Copyright footer 被正确排除，但若真实数据行因上游格式变化（如日期列损坏）被误过滤，无任何 warning（对比：同函数对缺失数值行有 `ff5_daily_missing_values_dropped` 告警）。
- 位置：`src/aionis/ingest/ff5.py:140`（该 commit 版本）
- 影响范围：极端情况下数据行静默减少（G4 可复现性由快照 sha256 兜底，可检测）。
- 修复建议：过滤后若被丢弃行数 > 0 记一条 structlog warning（含样例行内容）。
- 修复成本：低
- 状态：未修复

### P3-2 闸门逻辑在两个 runner 间复制
- 描述：`_ledger()` + `_require_owner_commit()` 与 `res_02_baseline_ff5_run.py` 逐行近似复制（仅 TRIAL_ID 与环境变量名不同）；后续若新增 baseline runner 需第三次复制，且闸门逻辑修 bug 时易漏改一处。
- 位置：`scripts/res_03_baseline_rank_run.py:209-244`（该 commit 版本）
- 影响范围：脚本层维护成本；无行为缺陷。
- 修复建议：提取 `aionis.eval.owner_gate.require_owner_commit(trial_id, sig)` 共享 helper（脚本是薄 runner，可接受现状）。
- 修复成本：低
- 状态：未修复

## 亮点
- commit message 诚实自曝前版缺陷（「the previous version ran CV and printed OOS metrics with no gate at all」），修复与披露同步——这是良好的审计文化。
- SIG_ONLY 模式的放置正确：RES-02 中在 RD-13 闸门与 config 构建之后、CV 之前返回——owner 拿到的 sig 已含通过闸门的列集合，与真实运行 config 一致。
- 回归测试用真实 footer 文案（"Copyright 2026 Eugene F. Fama and Kenneth R. French"），封住 mock-与-真实差距。
- 同时披露 DFF ALFRED 2000-vintage 上限缺陷为 owner 决策项（随后 ddbb47d7 处理）。

## 总体评分
9/10 — 修复精准、带回归测试、自曝缺陷诚实；仅存两处脚本级 P3（静默过滤无日志、闸门复制）。
