# Commit 6bd360ff5e55d25032bf0f8026198a239cbc276f 审查报告

**提交信息**: docs(track-c): commit amendment #47 — A-share cninfo fundamentals → exploratory-only（2026-08-04，Rethymus）
**改动范围**: 6 个文件，+135/-2；ledger 追加修订行 #47（sig 252cf7df）、新增 `scripts/track_c_amend1.py`（82 行可复现修订脚本）、prereg §0/§3 修订说明、design 报告 G1 处置、state 沉淀。

## 四维度结论
| 维度 | 结论 | 一句话说明 |
|---|---|---|
| 稳定性 | ✅ | deepcopy 父 config + 定点改 claim/cn_fundamentals，dry-run 默认、`--commit` 门；无 OOS 观察 |
| 可扩展性 | ✅ | 修订以"新行 + amends 注记"表达（durable-registry），复用 track_c_commit 的 config_sig 机制 |
| 生产可用 | ✅ | **sha256 自洽独立复核通过**（行 #47 重算 == config_sig，MATCH；47 行总数、#46 未动）；GPL 孤儿 finsaber_mount.py 同步清除 |
| 高内聚低耦合 | ✅ | claim 诚实收窄（A 股基本面 exploratory、cninfo 反爬不规避），G1 保守处置符合许可/礼貌红线 |

## 对照参考
不适用（账本修订机制）。"修订 = 新 append 行而非改写冻结行"与临床预注册的 amendment 登记惯例（如 OSF 的 prereg-revision 时间戳链）及 ADR-006 durable-registry 一致；GPL 传递依赖排除（FINSABER→backtrader GPLv3）与 Apache 2.0 §4/FSF 传染性依赖解释一致（来源：pypi.org Requires-Dist、apache.org）。

## 问题清单
### P2-1 修订遗漏同一张 §3 表中的 FINSABER 引用（GPL 已排除但 prereg 仍标 ✅ 复用）
- 描述：本 commit 正在编辑 prereg §3 表并清除了 finsaber_mount.py（GPL 孤儿），且 25 分钟前的 420fba18 调研已判 FINSABER 为 "GPL trap"；但 §3 ⑥ 行 "FINSABER ✅ \| 复用"（现 main 版 :71）与 §11 "净成本（FINSABER）是 exploratory 次级透镜"（:159）、§14 引用 "FINSABER（Apache）"（:186）均未同步修订，`git log 6bd360ff..main -- docs/track-c-preregistration.md` 为空——至今未修。照表施工会重新引入被排除的 GPL 依赖。
- 位置：`docs/track-c-preregistration.md:71`（§3 ⑥ 行，本 commit 版本）
- 影响范围：Track C §6/S6 净成本切片的数据源指引；与 CLAUDE.md 许可白名单红线冲突的文档残留。
- 修复建议：将 §3 ⑥/§11/§14 的 FINSABER 全部改为 "empyrical + execution_costs.py 薄成本层（FINSABER 因 backtrader GPLv3 硬依赖排除）"。
- 修复成本：低
- 状态：未修复

### P3-1 track_c_amend1.py 依赖同级目录导入，无 Usage 说明其运行目录约束
- 描述：`from track_c_commit import LEDGER, PHASE, TRACK_C_CONFIG, config_sig` 仅当 CWD/sys.path 含 `scripts/` 时成立（`python scripts/track_c_amend1.py` 可用；`python -m scripts.track_c_amend1` 不可用），docstring 未说明；且与 track_c_commit.py 同为无测试的冻结类脚本。
- 位置：`scripts/track_c_amend1.py:21`
- 影响范围：可复现性（未来 rerun 时若从仓库根以模块方式调用会 ImportError）。
- 修复建议：docstring 加一行 "run as `uv run python scripts/track_c_amend1.py`"。
- 修复成本：低
- 状态：未修复

## 总体评分
8/10 — 账本修订纪律无可挑剔（append-only、sig 自洽复核通过、claim 诚实收窄、GPL 孤儿清除）；扣分在修订未扫净同表的 FINSABER 残留，留下与许可红线相悖的文档指引。
