# Commit 6cf4f24dc23353b95967c3b120f9be63e8c6921c 审查报告

**提交信息**: feat(market): gloss the risk-stat terms (title tooltips) for plain-language access（2026-08-11，Rethymus）
**改动范围**: 2 个文件，+26/-10；`RiskStat` 组件加可选 `hint` 渲染为原生 `title` tooltip，market 面板 8 个风险指标全部挂上 zh/en 白话释义（i18n 各 +8 键）。

## 四维度结论
| 维度 | 结论 | 一句话说明 |
|---|---|---|
| 稳定性 | ✅ | `hint?` 可选、`title={undefined}` 自动省略，无 hint 的调用方零影响 |
| 可扩展性 | ✅ | 释义集中在 dict.ts，组件只透传 |
| 生产可用 | ✅ | 指标释义内容准确（Calmar/VaR/CVaR 定义无误）；双语言齐备 |
| 高内聚低耦合 | ✅ | 展示层局部改动，无数据/逻辑耦合 |

## 对照参考
不适用（纯展示文案）。原生 title tooltip 是 W3C/MDN 认可的最小方案；shadcn 生态的 Radix Tooltip 可作后续升级项。

## 问题清单
### P3-1 原生 title tooltip 在触屏与读屏下不可达
- 描述：`title` 属性在移动端浏览器不显示、多数屏幕阅读器不朗读，白话释义在手机上完全不可见；本终端是响应式布局（md:grid-cols-4），移动端是真实使用面。
- 位置：`web/src/components/market/market-view.tsx:50-53`（该 commit 版本）
- 影响范围：移动端/读屏用户拿不到 owner 要求的"每个术语有简单解释"。
- 修复建议：后续统一换 Radix Tooltip 或 `aria-describedby` + 说明文本（项目后续 gloss 系列 commit 均沿用 title，可一并处理）。
- 修复成本：低
- 状态：未修复

## 总体评分
9/10 — 小而完整的可访问性/易用性改进，内容准确、双语言、无回归面；仅触屏可达性留有升级空间。
