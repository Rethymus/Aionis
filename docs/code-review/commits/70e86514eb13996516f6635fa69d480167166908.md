# Commit 70e86514eb13996516f6635fa69d480167166908 审查报告

**提交信息**: feat(smart-money): link recent 13D targets to EDGAR filing (recover the filer)（2026-08-11，Rethymus）
**改动范围**: 5 个文件，+137/-61；export 透传 daily-index 行的 filing `url`，`SmartMoney` 类型加 `url?`，smart-money-view 把 target 渲染为外链（`target="_blank" rel="noopener noreferrer"`），i18n 补 zh/en 链接标题，smart_money.json 重导出。

## 四维度结论
| 维度 | 结论 | 一句话说明 |
|---|---|---|
| 稳定性 | ✅ | `r.url ?` 真值判断同时兼容缺键与空串；EFTS 行走 else 分支不受影响 |
| 可扩展性 | ✅ | 类型可选字段 + 导出端单行透传，扩展面小 |
| 生产可用 | ✅ | 外链带 noopener noreferrer；补齐了"(申报人见原文)"的信息闭环 |
| 高内聚低耦合 | ✅ | 数据契约（export→type→view→i18n）四处同步，无越界改动 |

## 对照参考
不适用（展示层小特性）。`rel="noopener noreferrer"` 与新开标签的做法与 MDN "Link types" 指南一致。

## 问题清单
### P3-1 外链 href 未做来源校验（纵深防御）
- 描述：`"url": str(r.get("url", ""))` 直接透传 cache JSON 里的任意字符串到 `<a href>`。URL 虽来自自家的 EDGAR daily-index 解析（sec.gov），但 `parse_daily_13d` 对 `fields[4]` 未做 scheme/host 校验 —— 若上游索引被污染或 cache 被改动，面板可能渲染非 sec.gov（甚至 `javascript:`）链接。
- 位置：`scripts/export_terminal_data.py:961`（透传）、`src/aionis/ingest/stakes_13d_daily_index.py:107-110`（url 提取）、`web/src/components/smart-money/smart-money-view.tsx:101-110`（渲染，该 commit 版本）
- 影响范围：展示层链接完整性；威胁模型弱（数据源为自有抓取）。
- 修复建议：解析或导出时断言 `url.startswith("https://www.sec.gov/Archives/")`，否则置空。
- 修复成本：低
- 状态：未修复

## 总体评分
8/10 — 干净、完整的最小特性（数据链路四处契约同步、i18n 双语齐全），仅缺一个廉价的 URL 来源断言。
