# Commit 715c91bc581640d322a1ccbc1e47ea4f60dd37f7 审查报告

**提交信息**: fix(features): CN panel builder bugs + first A-share rank-IC (null, exploratory)（2026-08-04，Rethymus）
**改动范围**: 4 个文件，+162/-18；修复 `build_cn_price_panel.py` 三处阻塞性 bug（stack 列误命名/自检日期越界/索引名依赖）+ 新增 `scripts/track_c_a_run.py`（CN 侧 exploratory rank-IC runner）+ `_build_features` 列集回归测试 + handoff。

## 四维度结论
| 维度 | 结论 | 一句话说明 |
|---|---|---|
| 稳定性 | ✅ | 三个 bug 修复正确且附带"能同时抓住三者"的回归测试；真实数据重建通过（leakage self-check PASSED、15 列正确） |
| 可扩展性 | ✅ | runner 镜像 track_b_a_run 模式复用 fit_track_b_baseline；`rename(columns[0]->"date")` 对未命名索引健壮 |
| 生产可用 | ✅ | 结果披露纪律好：DM p=0.031 边际主动注明"n_trials=30 haircut 会洗掉"、明示"NOT rescued (rerun-to-significance forbidden)"、不写 ledger |
| 高内聚低耦合 | ✅ | 修复聚焦包装层、不动内核；exploratory 与 confirmatory 边界在 docstring 重申 |

## 对照参考
Google Testing Blog《Don't Mock Types You Don't Own》（testing.googleblog.com）的核心建议——"prefer to test the wrapper class with calls to the real implementation"——正中本组缺陷根因：agent 的 15 个测试只测了被复用的 `compute_price_features` 内核，从未执行 `_build_features` 包装层，于是三处包装级 bug 全部漏网；本 commit 补的"跑真实包装 + 断言列集"回归测试与该建议一致（也符合《Just Say No to More End-to-End Tests》的测试尺寸分层思想）。来源域名：testing.googleblog.com。

## 问题清单
### P3-1 上一版遗留的 PIT CSI300 成员过滤缺失在本修复中仍未处理
- 描述：本 commit 修复的是包装层崩溃类 bug，但 97184c95 报告的 P1-1（面板横截面=ever-member 池、无 `constituents_on(t)` 交集）依旧存在，且新建的 `track_c_a_run.py` 直接消费未过滤面板——该问题已在 97184c95 计数，此处仅记录传导链延续（后续 track_c_joint.py confirmatory 路径同样未过滤）。
- 位置：`scripts/track_c_a_run.py:33-49`（加载即用）
- 影响范围：同 97184c95 P1-1。
- 修复建议：同 97184c95 P1-1。
- 修复成本：中
- 状态：未修复（不重复计数）

### P3-2 泄漏自检仍为单点采样且独立复刻 momentum 公式
- 描述：修复后自检从面板中取"存在的" (ticker, date)，但仍是 1 ticker × 1 日期的抽查；Check-1 用 `pct_change().rolling(21).sum()` 手写复刻 momentum_21d 定义与 `compute_price_features` 耦合——内核定义一旦改变（如改为价格比率式动量），自检将误报 LEAKAGE；另外 `future_idx = min(mid_idx + H, len-1)` 在序列尾部会把"未来日"钳到非 H 步（中位采样下实际不触发）。
- 位置：`scripts/build_cn_price_panel.py:222-249`（本 commit 版本）
- 影响范围：自检的灵敏度与误报率（不影响生产行为）。
- 修复建议：自检改为随机抽 3-5 个 (ticker,date)（seed=0），并直接调用 `compute_price_features` 重算而非手写公式。
- 修复成本：低
- 状态：未修复

### P3-3 track_c_a_run.py docstring 拼写错误 "EXPLORORATORY"
- 描述：runner docstring 首行 "EXPLORORATORY"（多一个 OR）。
- 位置：`scripts/track_c_a_run.py:5`
- 影响范围：仅文档。
- 修复建议：改为 EXPLORATORY。
- 修复成本：低
- 状态：未修复

## 总体评分
8/10 — 教科书式的当日缺陷闭环：三个包装层 bug 修复 + 能抓住它们的回归测试 + 真实数据验证 + 诚实的 null 结果披露（边际 DM 主动降权）；扣分在自检单点/公式复刻的脆弱性与 PIT 过滤问题继续顺延。
