# Commit 728f752fe32a4c05d0defa28a326f650c1a9d60b 审查报告

**提交信息**: feat(web+data): /themes display — seven-theme signals surfaced (display-only)（2026-08-08，Rethymus）
**改动范围**: 9 个文件，+908/-0；`export_terminal_data.py` 新增 `export_themes()`（135 行）、themes-view.tsx（174 行）、/themes 路由 + 侧边栏 + i18n、themes.json（505 行数据）+ 契约测试。

## 四维度结论
| 维度 | 结论 | 一句话说明 |
|---|---|---|
| 稳定性 | ✅ | 面板缺失→awaiting_fetch；bps_sweep 损坏→异常捕获降级为 partial；空态处理完备 |
| 可扩展性 | ✅ | 主题按 key/status 数据驱动；前端 literal-key map 适配严格 i18n 联合类型 |
| 生产可用 | ⚠️ | bps_sweep 解析失败静默吞掉无日志；macro 分支依赖 parquet 生产者有序（见 P3） |
| 高内聚低耦合 | ✅ | 展示层导出复用既有 PIT 面板/regime 复合/bps 扫描，不触研究管线与冻结面 |

## 对照参考
不适用（展示层聚合 + 契约测试；无新统计逻辑）。与项目"诚实展示"纪律一致：forward_only/needs_work 主题 signals 强制为空（契约测试断言"no mock data"），methodology 声明非 Track-B 研究结论。

## 问题清单
### P3-1 macro 分支假定 regime_macro.parquet 按日期有序，未显式排序
- 描述：`mr["macro_regime"].iloc[-1]` 与 `mr.tail(60)` 均假定行按日期升序。该假定当前由上游（FRED 观测升序 → Series → parquet）保证，但导出端未自守；若上游写入方式变更（如按 ticker 分块写、增量 append 重排），"最新值"与 60 点序列会静默取错。
- 位置：`scripts/export_terminal_data.py:476-485`（commit 版本行号）
- 影响范围：/themes 页 macro 主题的最新 z 值与 sparkline。
- 修复建议：读入后 `mr = mr.sort_values("date")` 一次，成本为零。
- 修复成本：低
- 状态：未修复

### P3-2 bps_sweep.json 解析异常被静默吞掉
- 描述：`except (json.JSONDecodeError, KeyError, ValueError, TypeError): pass` — 契约文件损坏时 net_cost 主题静默降级为无 headline 的 partial，无任何日志/打印，CI 与人工都无从分辨"没有 5bps 行"与"文件坏了"。
- 位置：`scripts/export_terminal_data.py:498-512`
- 影响范围：仅展示；但排障成本增加。
- 修复建议：except 分支加一行 print/logging 说明降级原因。
- 修复成本：低
- 状态：未修复

## 防泄漏核查
读取 track_b PIT 面板截面均值（特征，非标签）、regime 复合（ALFRED PIT）、bps 扫描产物；纯展示导出，不写 ledger、不触冻结面。契约测试强制非 live 主题无伪造信号。合规。

## 总体评分
8/10 — 诚实展示纪律执行到位（空主题不 mock、契约测试兜底），仅两处 P3 级健壮性小疵。
