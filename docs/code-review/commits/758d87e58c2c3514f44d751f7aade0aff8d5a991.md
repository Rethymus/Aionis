# Commit 758d87e58c2c3514f44d751f7aade0aff8d5a991 审查报告

**提交信息**: feat(ingest): warn when persist_snapshot writes the REAL ledger (runs_dir=None)（2026-08-03，Rethymus）
**改动范围**: 1 个文件，+5/-0；`src/aionis/ingest/forward/_common.py` 的 `persist_snapshot` 在 `runs_dir is None`（落到 `settings.runs_dir` 即真实 `runs/ledger.jsonl`）时先打 structlog warning。

## 四维度结论
| 维度 | 结论 | 一句话说明 |
|---|---|---|
| 稳定性 | ✅ | 5 行防御性日志，无控制流变化；`ledger_path(None)`→settings.runs_dir 语义经核验属实，警示位置正确（写账本之前） |
| 可扩展性 | ✅ | 挂在共享 persist 尾部，三个收集器一次覆盖 |
| 生产可用 | ⚠️ | warning 级别会在**正常生产路径**上每次触发（生产默认就是 runs_dir=None），日志噪音语义偏重；且该分支以"不可测"为由跳过测试，理由不成立 |
| 高内聚低耦合 | ✅ | 位于 _common，未改调用方 |

## 对照参考
结构化日志惯例（structlog/Python logging 级别语义）：warning 应表示"意外但可恢复"的状态；正常生产路径打 warning 会稀释真实告警的信噪比（docs.python.org/3/library/logging.html 级别定义）。

## 问题清单
### P3-1 warning 在正常生产路径上必然触发，级别语义偏重
- 描述：生产调用收集器时通常不传 `runs_dir`（默认 None → 真实账本），该 warning 因此会在每次合法生产持久化时触发，而非仅"意外 None"时。注释自述针对"accidental None (intended for a hermetic test scratch dir)"，但 None 同时也是生产默认值，两态不可区分。
- 位置：`src/aionis/ingest/forward/_common.py:268-272`
- 影响范围：日志信噪比（每日 3 条例行 warning）。
- 修复建议：降为 `log.info("forward_persist_real_ledger", ...)`，或仅当检测到测试上下文（如 env 标记）时才 warning。
- 修复成本：低
- 状态：未修复

### P3-2 "不可单测"的理由不成立——可 monkeypatch settings.runs_dir
- 描述：commit message 称"exercising the warning branch writes to the REAL ledger, which tests must never do"故不测。实际上 monkeypatch `_common.settings.runs_dir` 指向 tmp_path 即可安全触发该分支并断言 warning，无需触碰真实账本。
- 位置：`src/aionis/ingest/forward/_common.py:268`（无对应测试）
- 影响范围：防御性日志本身的回归保护（若有人误删该分支，无测试会红）。
- 修复建议：补一个 `monkeypatch.setattr(settings, "runs_dir", tmp_path)` + `caplog`/structlog 捕获断言的测试。
- 修复成本：低
- 状态：未修复

## 总体评分
7/10 — 方向正确的防御性增强，但级别选择欠斟酌且测试缺口本可低成本补上。
