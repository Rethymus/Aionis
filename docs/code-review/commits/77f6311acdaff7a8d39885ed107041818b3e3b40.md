# Commit 77f6311acdaff7a8d39885ed107041818b3e3b40 审查报告

**提交信息**: refactor(cot): 10-market cross-asset set + resilient per-year fetch（2026-08-07，Rethymus）
**改动范围**: 1 个文件，+15/-3；cot_fetch.py 市场集合从 8（含 2 死条目）改 10 个跨资产市场（股/波/汇/能/金属），逐年拉取加 try/except 隔离，全部失败时早退不写 parquet。

## 四维度结论
| 维度 | 结论 | 一句话说明 |
|---|---|---|
| 稳定性 | ✅ | 单年失败跳过并记日志，no-frames 早退保护既有 parquet——修复了上一 commit 的脆弱点 |
| 可扩展性 | ✅ | 市场集合仍是单处声明、MISS 弹性跳过；跨资产覆盖结构更合理 |
| 生产可用 | ✅ | cftc.gov 不可用时诚实保留旧快照并在 message 披露，不硬刷 |
| 高内聚低耦合 | ✅ | 纯脚本内聚改动，无接口变更 |

## 对照参考
不适用（配置清单 + 容错小改；CFTC legacy_fut 报告含 Russell/Silver/Euro FX/Yen 与 cftc.gov 官方历史数据说明一致，commit 自述经真实 probe 验证名称精确匹配）。

## 问题清单
### P3-1 全年失败早退仍跳过 annual.txt 清理
- 描述：所有年份拉取失败时 `return` 在 `Path("annual.txt").unlink(missing_ok=True)`（位于市场循环之后）之前执行——恰好在这条新加的路径上留下 cot_year 写入的临时文件垃圾。
- 位置：`scripts/cot_fetch.py:57-59`（早退）与 `:80`（清理）
- 影响范围：极端退化路径下的工作目录卫生。
- 修复建议：unlink 移到 finally 或 main 入口首行/末尾各清一次。
- 修复成本：低
- 状态：未修复

### P3-2 composite 指数的成分变更未在方法论中披露
- 描述：市场集合 6→10 后，composite mean-z / crowding 及 composite_series 历史点都将在下次刷新时按新集合重算——同一序列的成分口径前后不一致（新旧点的可比性断裂），methodology 字符串只写"per key futures market"未披露集合与变更。
- 位置：`scripts/cot_fetch.py:29-42`（MARKETS）；`scripts/export_terminal_data.py` export_cot methodology
- 影响范围：/positioning 页 composite 序列的解读（展示层）。
- 修复建议：methodology 中注明当前市场集合与生效日期，或在序列中标注成分变更点。
- 修复成本：低
- 状态：待人工复核（后续 1a0f4bf 把历史拉长到 2016 并同步刷新，序列已整体重算，口径断裂被"整段替换"缓解但披露问题同构）

## 总体评分
8/10 — 精准的小步重构：修复上版的两个 P3（逐年容错、死条目清理），保留旧快照的诚实处理得当；仅剩临时文件清理的边角与 composite 口径披露问题。
