# Commit 7d90176fb136dd92b98eb4c83aabc763416e93df 审查报告

**提交信息**: feat(web): 川普元年 market context — VIX × index × events (2016→today)（2026-08-08，Rethymus）
**改动范围**: 9 个文件，+2093/-4324；新增 `/market` 页（recharts 双轴 ComposedChart：等权美股指数面积 + VIX 折线 + 事件参考线 + 8 条策划事件时间线 + KPI 行）、`export_market_context()` 导出器（VIX 月均 + 等权指数 2016-02 重基 + 事件表）、i18n 词条、契约测试；**同时** picks_backtest.json 由 131 个月无声缩回 45 个月。

## 四维度结论
| 维度 | 结论 | 一句话说明 |
|---|---|---|
| 稳定性 | ⚠️ | 前端对空数据有 awaiting 降级；但导出器新增两个无守卫 gitignored 依赖（alfred_VIXCLS.json / track_b_panel.parquet），延续 fresh-CI 崩溃族；且顺带发生 131→45 个月数据回退 |
| 可扩展性 | ✅ | 复用已有 panel 数据零新抓取；事件表数据驱动；KPI/图表结构清晰 |
| 生产可用 | ⚠️ | 无条件 `+` 前缀与硬编码中文事件类型标签；等权指数方法论披露诚实（PIT constituents、display-only 声明） |
| 高内聚低耦合 | ✅ | display-only 边界在 methodology 文本与 commit message 双重声明，未触碰研究管线 |

## 对照参考
pandas 月度重采样用 `groupby(period).last()` + `pivot` + `pct_change` + 横截面 `mean(axis=1)`（NaN 自动跳过）符合 pandas 官方 resample/pivot 惯例（pandas.pydata.org/docs）；recharts 双轴 `ComposedChart` + `YAxis yAxisId` + `ReferenceLine` + `domain="dataMin - 20"` 字符串表达式均与 recharts 官方文档示例一致（recharts.org）。等权指数以 PIT 成分面板为基，避免了 today-snapshot 幸存者偏差，方法论表述与实现相符。

## 问题清单
### P1-1 本 commit 无声把 track record 从 131 个月缩回 45 个月（与本特性无关的连带再导出）
- 描述：diff 中 picks_backtest.json 删除 4864 行——`summary.n_months` 由 131 变 45（US 24 + CN 21）、n_picks 655→225；原因是本次重跑导出时 `BACKTEST_MONTHS` 走了新默认值 24（上一 commit 0a17416b 引入），而 commit subject 只字未提 track record 回退。展示层核心「诚实 track record」凭空损失 2/3 历史。
- 位置：`web/src/data/aionis/picks_backtest.json`（本 commit diff；根因 `scripts/export_terminal_data.py:275` 默认 24）
- 影响范围：/picks 页 track record 长度与汇总统计（hit_rate 等随窗口漂移）。
- 修复建议：默认改回全历史（BACKTEST_MONTHS=99）并重导出——即下一 commit 的做法。
- 修复成本：低
- 状态：已在 a5ca91582299f219211d6f7309ef64585a1e7e8a 修复（默认 99 + 全历史重导出）

### P2-1 export_market_context 新增两个无守卫 gitignored 数据依赖，扩大 fresh-CI 崩溃面
- 描述：`vix_raw = json.loads(Path("data/cache/alfred_VIXCLS.json").read_text())` 与 `us = pd.read_parquet(Path("data/cache/track_b_panel.parquet"))` 均无 exists 守卫——在每日刷新 cron 的全新 runner 上必然 FileNotFoundError，使上一 commit 已不可用的刷新管线雪上加霜。
- 位置：`scripts/export_terminal_data.py:664-665, 681-682`（该 commit 版本）
- 影响范围：refresh-terminal-data 工作流（导出步骤崩溃）。
- 修复建议：纳入函数级优雅跳过机制。
- 修复成本：低
- 状态：已在 a08ec16cd901bfea6c46d9887156d969865b5aa0 修复（graceful artifact skip 统一覆盖）

### P3-1 事件类型标签硬编码中文，绕过 i18n 体系
- 描述：`const EVENT_TYPE_LABEL = { political: "政治", trade: "贸易", crisis: "危机", monetary: "货币" }` —— 同一 commit 给 dict.ts 加了 34 行中英词条，事件类型却未走 `t()`，英文界面下事件徽章仍显示中文。
- 位置：`web/src/components/market/market-view.tsx:34-39`（该 commit 版本）
- 影响范围：/market 页英文用户体验。
- 修复建议：迁入 i18n dict（`market.event.political` 等）。
- 修复成本：低
- 状态：未修复（main:41-42 仍为硬编码中文）

### P3-2 KPI 总回报无条件加 "+" 前缀；重基表述与实现有细微出入
- 描述：`value: \`+${totalReturn.toFixed(0)}%\`` 不看符号（若未来区间收益为负会渲染 "+-5%"）；methodology 写 "rebased to 100 at 2016-02"，实现上首个 index = 100×(1+2016-02 月收益)（真正基点是 1 月末收盘），测试也只断言 90–105 容差——展示口径小误。
- 位置：`web/src/components/market/market-view.tsx:106-110`；`scripts/export_terminal_data.py:706-710`（该 commit 版本）
- 影响范围：KPI 极端情形显示错误；方法论文本精度。
- 修复建议：`(totalReturn >= 0 ? "+" : "") + ...`；methodology 改为 "rebased to ~100 at end of Jan 2016"。
- 修复成本：低
- 状态：未修复（main:110-115 仍无条件 "+"）

## 总体评分
6/10 — /market 页本身实现规范（双轴图、降级、契约测试、PIT 方法论披露都到位），但一次与特性无关的重导出把上一 commit 刚交付的 131 个月 track record 无声砍到 45 个月，属于流程纪律失误（提交前未 diff 数据文件语义变化）。
