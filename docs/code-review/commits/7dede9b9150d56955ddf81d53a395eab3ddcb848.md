# Commit 7dede9b9150d56955ddf81d53a395eab3ddcb848 审查报告

**提交信息**: chore: finalize dashboard and health remediation（2026-08-01，Rethymus）
**改动范围**: 21 个文件，+1993/-1564；`dashboard/app.py` 1651→129 行，视图/助手/demo 生成器按 tab 拆入 `dashboard/views/{charts,event,fit,forward,helpers,uncertainty,volatility,data_loading,overview,...}.py`（向后兼容再导出）；`scripts/health_check.py` 移除 akshare/EastMoney 探测（未批准源）+ `tests/test_health_check.py` 断言阻断源不出现。

## 四维度结论
| 维度 | 结论 | 一句话说明 |
|---|---|---|
| 稳定性 | ✅ | 纯机械搬移（函数计数核对：旧 68 → 各 views 模块合计一致），测试导入路径经 app.py 再导出保持兼容 |
| 可扩展性 | ✅ | 偿还上一 commit 自认的 app.py 膨胀 follow-up；每 tab 一模块 |
| 生产可用 | ✅ | health_check 源清单与白名单一致性有 hermetic 测试锁定（blocked 源断言） |
| 高内聚低耦合 | ⚠️ | app.py 变薄 + views 分层达成；但为兼容旧测试再导出约 35 个下划线私有符号（P3-1） |

## 对照参考
Streamlit 多页/模块化组织与官方文档（docs.streamlit.io）推荐一致；health_check 的 ≥2s MIN_GAP + 2^n 退避与项目礼貌红线一致。不适用进一步对照。

## 问题清单
### P3-1 app.py 为测试兼容再导出大量私有符号，测试 API 演进被冻结
- 描述：新 app.py 从 views 导入并 `__all__` 再导出 `_calmar`、`_car_ci`、`_demo_event_windows` 等约 35 个下划线名，注释明言 "Re-export ... that tests import" —— 测试应直接从新位置导入；现状使 helpers 的归属迁移后仍被旧路径钉死。
- 位置：`dashboard/app.py:36-108`
- 影响范围：dashboard 测试与模块演进自由度。
- 修复建议：批量改 tests/ 的导入到 `dashboard.views.*`，删除再导出层。
- 修复成本：低
- 状态：未修复（main 上 app.py 仍保留再导出）

## 总体评分
8/10 — 干净的偿还性 refactor + 白名单测试锁定，无逻辑变化；唯一瑕疵是用再导出层推迟了测试导入的迁移。
