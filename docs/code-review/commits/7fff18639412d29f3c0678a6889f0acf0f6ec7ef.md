# Commit 7fff18639412d29f3c0678a6889f0acf0f6ec7ef 审查报告

**提交信息**: feat(phase-d): confirmatory run script (assembles rel bundle, runs)（2026-07-29，Rethymus）
**改动范围**: 1 个文件，+145/-0；新增 `scripts/phase_d_run.py`——薄封装：读冻结数据 + SIC/13D 事件缓存 → 组装 rel bundle → commit_config（sha256 先行）→ run_confirmatory → 打印差分/placebo/LOO/H6；含 `PHASE_D_NO_LEDGER=1` artifacts-only 复跑模式。

## 四维度结论
| 维度 | 结论 | 一句话说明 |
|---|---|---|
| 稳定性 | ⚠️ | commit_config 前无覆盖守卫——部分/冒烟缓存也能一路走到确认运行（37 分钟后 a8c7798f 补上）；输入文件缺失会先在 read_parquet 处大声失败 |
| 可扩展性 | ✅ | 窗口参数（PEER_WINDOW/STAKES_WINDOW_DAYS）进 config 的 rel_meta；每个可测决策下沉到 eval.phase_d（脚本自身保持薄） |
| 生产可用 | ⚠️ | HIGH-1 顺序正确（commit → run → 观察结果）；但 results_base=None 叠加库层守卫 bug，docstring 声称的 "persists artifacts" 实际不发生 |
| 高内聚低耦合 | ✅ | scripts 薄层只做 I/O 与编排，复用 library 的 config/run 函数 |

## 对照参考
薄脚本 + src 布局与 [Cookiecutter Data Science V2](https://cookiecutter-data-science.drivendata.org/)（[V2 发布说明](https://drivendata.co/blog/ccds-v2)：run 脚本只编排、逻辑进 src 可测包）的社区共识一致；本脚本 "every testable decision lives in aionis.eval.phase_d" 的自我约束正是该模式。出处域名：cookiecutter-data-science.drivendata.org、drivendata.co。

## 问题清单
### P2-1 commit_config 前缺覆盖守卫，部分缓存可产生退化确认运行
- 描述：`_build_rel_extra` 直接读 `phase_d_sic_map.parquet`/`phase_d_13d_events.parquet`，若 fetch 中途中断留下部分事件表（或 SIC 覆盖极低），脚本仍会 commit_config 并跑出 degenerate `confirmatory:first`（rel 特征无信息量），账本被一条无意义行占据。
- 位置：`scripts/phase_d_run.py:50-73`（`_build_rel_extra` 与 main 的 commit 前段）
- 影响范围：Phase D 确认运行的账本有效性。
- 修复建议：commit 前检查 SIC 覆盖 ≥80%、peer_mom 有有效值、13D 事件非零告警。
- 修复成本：低
- 状态：已在 a8c7798f98cc40d324eed3cc638bd96f784cd3a1 修复（同日评审 MEDIUM）

### P2-2 `results_base=None` 叠加库层守卫，artifacts 实际不落盘
- 描述：docstring 写 "persists artifacts"，但 `run_confirmatory(..., results_base=None)` 命中 `if results_base is not None` 守卫 → save_run 被跳过；首次真实 run 无 dashboard 产物（与 08b0db56 的库层缺陷同根，此处脚本显式传 None 使必现）。
- 位置：`scripts/phase_d_run.py:121`
- 影响范围：runs/results/ 产物完整性（ledger 头条不受影响）。
- 修复建议：去守卫、base=None 走默认目录（ef4c9480 的做法）。
- 修复成本：低
- 状态：已在 ef4c94806657a185ee4a855eefedd6040535542d 修复

### P3-1 artifacts-only 分支内联复刻 sig 推导
- 描述：`sig = hashlib.sha256(json.dumps(config, sort_keys=True).encode()).hexdigest()` 与 `phase_d.commit_config` 内的推导逐字重复——若将来 sig 公式变（如加 canonical 分隔符），两处会静默分叉，导致 NO_LEDGER 复跑的 sig 与账本不一致。
- 位置：`scripts/phase_d_run.py:136`
- 影响范围：PHASE_D_NO_LEDGER 复跑模式的 sig 一致性。
- 修复建议：在 phase_d 导出 `sig_of(config)`，两处共用。
- 修复成本：低
- 状态：未修复（main 上仍为两份推导）

## 总体评分
7.5/10 — 薄封装结构与账本顺序纪律到位，但"部分缓存可入账本"与"artifacts 实际不落盘"两个缺陷都属于会把错误静默写进审计链的类别，好在均当日被评审捕获修复。
