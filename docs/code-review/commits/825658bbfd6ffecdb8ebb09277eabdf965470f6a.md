# Commit 825658bbfd6ffecdb8ebb09277eabdf965470f6a 审查报告

**提交信息**: feat(res): execute RES-02 + RES-03 baselines (2 worktree-isolated sonnet agents)（2026-08-03，Rethymus）
**改动范围**: 16 个文件，+3341/-1；新增 FF5/DFF ingest（`src/aionis/ingest/ff5.py`、`macro_dff.py`）、股票特定滚动暴露特征（`src/aionis/features/ff5.py`）、两个 exploratory runner（`scripts/res_02_baseline_ff5_run.py`、`res_03_baseline_rank_run.py`）、两份 config yaml、7-gate intake 文档、56 个新测试；`learner.py` 仅 5 行（rank 分支接入冻结 enum 校验）。覆盖范围：全部 5 个核心源码/脚本文件逐行读完，测试文件扫描了用例清单。

## 四维度结论
| 维度 | 结论 | 一句话说明 |
|---|---|---|
| 稳定性 | ⚠️ | FF5 解析器对真实文件的 footer 崩溃（P1，后修）；快照 sha 校验存在 fail-open 边（P2） |
| 可扩展性 | ✅ | 特征列/窗口/闸门常量集中定义；RD-13 闸门 fail-closed 可复用 |
| 生产可用 | ⚠️ | politeness（HttpRequestPolicy ≥2s+指数退避）合规、H6 双跑断言齐全；但 RES-03 的 ledger 闸门只写在文档里未落码（P1，后修） |
| 高内聚低耦合 | ✅ | ingest/features/eval 分层清晰；冻结文件（ranking_contract.py、diagnostics.py、FROZEN_PARAMS）0 修改，learner 仅把硬编码 "lambdarank" 改为经冻结 enum 校验 |

## 对照参考
- **French 数据解析**：pandas-datareader 的 `famafrench.py` 用空行分节解析，天然排除文件尾部的 "Copyright …" 行；手工 `read_csv` 路线必须 skipfooter/行过滤（Rand Low 教程专门记录此坑）。本 commit 的解析器「从首个 8 位日期行读到文件尾」未过滤尾部非数据行——真实 2026 文件带 Copyright footer 即崩溃，恰是此对照预示的缺陷（已在 6a4528c9 修复）。出处：github.com（pydata/pandas-datareader famafrench.py）、pandas-datareader.readthedocs.io、randlow.github.io。
- **PIT as-of join**：`merge_asof(direction="backward", allow_exact_matches=False)` 的严格早于语义与 pandas 官方文档一致；DFF first-print（`groupby("ref_date")["realtime_start"].idxmin()`）+ 严格早于 join 是保守正确的 PIT 构造（pandas.pydata.org）。

## 问题清单
### P1-1 RES-03 runner 无 config_committed 闸门（commit message 声称与 RES-02 一致，实际未实现）
- 描述：commit message 写「Both runners refuse to run until the owner's config_committed ledger row exists」，但 `scripts/res_03_baseline_rank_run.py` 的 `main()` 中无任何 `_require_owner_commit` 等价物——加载 config 后直接 `run_rank_cv(...)` 并打印 OOS rank-IC（mean/ci/t_hac/p_hac）。防泄漏闸门只存在于 docstring 文字（「config_committed … requires owner authorization」）。后续 fix commit 自述：「the previous version ran CV and printed OOS metrics with no gate at all」。
- 位置：`scripts/res_03_baseline_rank_run.py:211-233`（main，该 commit 版本）
- 影响范围：BASELINE-RANK-001 可在 owner 未冻结 config 前观察 OOS 指标（exploratory trial 的 rerun-to-significance 风险面）；与 commit message 的声明相悖，审计者可能被误导。
- 修复建议：镜像 RES-02 的 `_require_owner_commit`（按 config_sig 精确匹配 ledger 行），违规即 SystemExit。
- 修复成本：低
- 状态：已在 6a4528c982d2a5646f407cd37e862c47aaad8415 修复

### P1-2 FF5 解析器未过滤文件尾部 footer，真实 French 日频文件必然崩溃
- 描述：`parse_ff5_daily_csv` 从首个 8 位日期行起 `read_csv(lines[first_data_idx:])` 一路读到文件尾，随后 `pd.to_datetime(df["date"], format="%Y%m%d")`；2026 年代 French 日频文件数据行之后带 "Copyright Kenneth R. French" footer，date 列非 8 位数字 → to_datetime 抛 ValueError。单元测试 fixture（`_ff5_zip_bytes`）只有头部 doc lines、没有尾部 footer，故封闭测试全绿（mock-与-真实差距）。
- 位置：`src/aionis/ingest/ff5.py:130-145`（该 commit 版本）
- 影响范围：RES-02 真实数据路径首次运行即崩（fail-closed，无静默数据损坏；后续 SIG_ONLY 运行中实测复现并修复）。
- 修复建议：coerce 前过滤 `df["date"].str.fullmatch(r"\d{8}")` + footer 回归测试（即 6a4528c9 的做法）。
- 修复成本：低
- 状态：已在 6a4528c982d2a5646f407cd37e862c47aaad8415 修复

### P2-1 FF5 快照 sha 文件缺失时漂移校验被静默跳过（fail-open）
- 描述：`fetch_ff5_daily_snapshot` 缓存命中分支 `recorded = sha_path.read_text().strip() if sha_path.exists() else ""` + `if recorded and recorded != sha:`——若 `.sha256` 文件被删除/损坏而 CSV 被篡改，校验直接放行。G3/G4 冻结真值锚在此路径上 fail-open，与模块 docstring「a drift is a hard error」的承诺不符。
- 位置：`src/aionis/ingest/ff5.py:184-185`（该 commit 版本；main 至今同逻辑）
- 影响范围：快照不可变性（H6/G3）依赖该校验；sha 文件缺失属低概率但一旦发生即静默绕过。
- 修复建议：sha 文件缺失时同样 raise（或重算并强制落盘前先要求 owner 决策）。
- 修复成本：低
- 状态：未修复

### P2-2 DFF ALFRED 抓取在真实路径必然失败（FRED 2000-vintage 上限 vs DFF 5093 vintages）
- 描述：`fetch_dff_vintages` 复用 `fetch_alfred_vintages`（macro_surprise），其 `_download_vintages` 单请求拉全部 vintage；FRED API 单次 vintage 查询上限 2000，DFF 有 5093 条 → HTTP 400。本 commit 把 RES-02 管线接到该函数上时未验证真实可行性（封闭测试均用缓存/fixture）。
- 位置：`src/aionis/ingest/macro_dff.py:41-48`（该 commit 版本，实际缺陷在 `macro_surprise._download_vintages`）
- 影响范围：RES-02 真实运行的 DFF 侧阻塞（后续 SIG_ONLY 运行中实测发现）。
- 修复建议：按 vintage 窗口分片请求（即 ddbb47d7 的做法）。
- 修复成本：中
- 状态：已在 ddbb47d79099fc266aea0a15d23690b3f6eeac0b 修复

### P3-1 RES-03 特征对齐依赖「relevance==-1 ⟺ return NaN」隐式不变量且无长度断言
- 描述：`run_rank_cv` 用 `valid_mask = train_returns.notna()` 自行重建过滤掩码对齐特征行，而冻结合约的过滤条件是 `~pd.isna(returns) & (relevance != -1)`；两者当前等价仅因 `transform_to_relevance` 只对 NaN 收益赋 -1。`tr_feat.iloc[sort_idx]` 前无 `len(tr_feat) == len(rel_clean)` 断言，若不变量破坏将错位而非报错。
- 位置：`scripts/res_03_baseline_rank_run.py:176-186`（该 commit 版本）
- 影响范围：仅当冻结 ranking_contract 演进时才会暴露（当前被「禁止修改冻结文件」约束保护）。
- 修复建议：加一行长度断言（防御性，不改行为）。
- 修复成本：低
- 状态：未修复

### P3-2 baseline_ff5_001.yaml 与 runner 内嵌 config 双源真相
- 描述：yaml 自述「REGISTRY RECORD … executable source of truth is scripts/res_02_baseline_ff5_run.py; keep the two in sync」——config_sig 由 runner 的 `build_config()` 生成而非 yaml，两处特征列需人工同步（Fowler 重复知识坏味道），已自认但未机制化。
- 位置：`config/baseline_ff5_001.yaml:10-13`（该 commit 版本）
- 影响范围：仅文档性 drift 风险（`test_config_yaml_lists_exactly_the_feature_columns` 已用测试锁住一致性，缓解）。
- 修复建议：让 runner 从 yaml 读 FROZEN_FEATURE_COLS/参数，或由测试持续锁定（现状已可接受）。
- 修复成本：低
- 状态：未修复（有测试缓解）

## 亮点（防泄漏核查）
- 暴露窗口 `searchsorted(t, side="right")` 只取 `date <= t`；`broadcast_monthly_exposures` 用 `allow_exact_matches=False` 严格早于广播，session 永不见当月月末值——无同月未来使用，且有 `test_future_truncation_invariance` / `test_exposure_at_t_ignores_every_observation_after_t` / `test_broadcast_no_same_month_future` 直接验证。
- DFF first-print + 严格早于 as-of；基本面 filed-date PIT（复用 `pit_align`）；H6 双跑 bit-identical 断言（IC + 原始分数）；市场-wide 原始因子列被 RD-13 结构性排除。learner.py 改动是收紧（经冻结 enum 校验）而非放宽，未触碰 FROZEN_PARAMS。

## 总体评分
7/10 — 防泄漏设计与测试深度显著优于平均水平（真 beta 恢复、future-truncation、RD-13 fail-closed、H6 断言俱全），但「封闭测试全绿」与「真实路径可用」之间有两处断裂（footer 崩溃、DFF vintage 上限）且 commit message 对 RES-03 闸门的描述与实现不符（三处均已在当日后续 commit 修复）。
