# Commit 83f8f64c6085bc819ae88e63fa7734ef95336f3f 审查报告

**提交信息**: feat(web): ResearchGlance — Overview surfaces attribution + coverage（2026-08-12，Rethymus）
**改动范围**: 3 个文件，+121/-0；新增 `web/src/components/overview/research-glance.tsx`（108 行，Overview 上的"为何 null + 测了什么"两卡摘要），`overview.tsx` 插入该组件，`dict.ts` 中英各 +5 个 i18n key。

## 四维度结论
| 维度 | 结论 | 一句话说明 |
|---|---|---|
| 稳定性 | ✅ | 纯展示组件，`hasCI` 判空后 `ci_lo!` 断言安全；无网络/异步 |
| 可扩展性 | ⚠️ | `COVERAGE_TALLY` 硬编码常量与 coverage-map `FAMILIES` 手工同步，缺乏单一事实来源 |
| 生产可用 | ✅ | display-only，只读冻结 metrics.json，确定性、无外部调用，符合防泄漏边界 |
| 高内聚低耦合 | ✅ | 摘要卡 + deep-dive 链接避免复制重组件，i18n 双语齐全 |

## 对照参考
React 官方状态结构指南（react.dev "Choosing the State Structure"）与官方博客 "You Probably Don't Need Derived State" 都强调：同一数据只保留一个拥有者、派生值应在使用处计算而非复制副本。本 commit 的 `COVERAGE_TALLY` 正是"复制派生值"反例——理想做法是从 `coverage-map.tsx` 导出 `FAMILIES` 或派生计数供两处 import（react.dev）。后续修复 commit 未做单一来源，仅改名澄清语义。

## 问题清单
### P2-1 硬编码 COVERAGE_TALLY 与 FAMILIES 手工同步，且 gaps:5 语义错误
- 描述：`const COVERAGE_TALLY = { tested: 3, exploratory: 2, gaps: 5 } as const;` 注释自述"mirrors coverage-map.tsx FAMILIES, kept in sync manually"。实际 FAMILIES 族深度为 tested 3 / exploratory 2 / **gap 0**，gap 条目共 19 个——`gaps: 5` 既非族数也非条目数，是陈旧/语义混杂值，展示给用户会误导覆盖状况。
- 位置：`web/src/components/overview/research-glance.tsx:26`（该 commit 版本）
- 影响范围：Overview 页 coverage 摘要卡的用户认知（数字与 /themes 全图不符）
- 修复建议：从 `coverage-map.tsx` 导出 FAMILIES 并派生计数（单一事实来源），或至少显示正确口径的数字
- 修复成本：低
- 状态：已在 a18b80aeab63f10247023ae606852760d646e245 修复（改为 testedFamilies/exploratoryFamilies + "families 5"，移除错误 gaps 计数；但仍是手工同步常量，未做 SSOT）

## 总体评分
8/10 — 组件本身干净、防泄漏边界清晰，但引入的硬编码计数当天就被证实变陈旧并需修复。
