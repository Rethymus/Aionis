# Commit 86fce92963167974052d498725fe15862fbe6eb5 审查报告

**提交信息**: fix(web): overview picks preview shows company names (not raw tickers)（2026-08-07，Rethymus）
**改动范围**: 2 个文件，+11/-2；`web/src/components/overview/overview.tsx` 首页 top-5 picks 预览由裸 ticker 改为「公司名 + 小号 ticker」两行展示，并顺带提交了 Next.js 自动生成的 `web/next-env.d.ts`。

## 四维度结论
| 维度 | 结论 | 一句话说明 |
|---|---|---|
| 稳定性 | ✅ | `p.name || p.ticker` 正确处理 `Pick.name: string \| ""` 的空串回退；React key 改为 `${region}-${ticker}` 更稳 |
| 可扩展性 | ✅ | 与 picks 页复用同一 name+ticker 展示模式，无新增硬编码 |
| 生产可用 | ✅ | `truncate` + `min-w-0` 处理长公司名溢出，布局防御到位 |
| 高内聚低耦合 | ✅ | 纯展示层改动，未触碰研究管线数据 |

## 对照参考
不适用（纯展示层琐碎改动；`next-env.d.ts` 为 Next.js 官方自动生成文件，提交属常见做法，`web/.gitignore` 未忽略它，故无 `git add -f` 之嫌）。

## 问题清单
通过。唯一可吹毛求疵之处：`next-env.d.ts` 属生成物，按 nextjs.org 官方 create-next-app 惯例通常进 .gitignore，与 `.next/` 一致处理更整洁——但这是风格问题，不构成 P3。

## 总体评分
9/10 — 干净的展示层一致性修复，回退与 key 都处理正确，无风险。
