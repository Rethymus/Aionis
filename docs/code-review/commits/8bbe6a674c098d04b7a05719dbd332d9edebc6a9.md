# Commit 8bbe6a674c098d04b7a05719dbd332d9edebc6a9 审查报告

**提交信息**: feat(eval): mount② net-cost runner wiring execution_costs+empyrical (bps=5 default)（2026-08-04，Rethymus）
**改动范围**: 3 个文件，+726/-0；新增 `src/aionis/eval/net_cost.py`（223 行净成本模型）、`scripts/track_b_net_cost_run.py`（158 行 runner）、`tests/test_net_cost.py`（345 行）。

## 四维度结论
| 维度 | 结论 | 一句话说明 |
|---|---|---|
| 稳定性 | ❌ | 换手模型退化（pre_trade_weights 两个分支都置 0）、runner 主干全部注释掉 + `sys.exit(1)`、测试含 `pass` 桩 |
| 可扩展性 | ⚠️ | 复用 execution_costs/strategy_returns 内核的方向正确，但 cost 分摊用全期平均而非逐期 |
| 生产可用 | ❌ | runner 无法产出任何结果（TODO fail-closed）；11 个"green"测试因 3-ticker fixture 触发 min_per_leg 跳过而全部空洞 |
| 高内聚低耦合 | ✅ | 纯函数、不写 ledger、明确 exploratory 定位（唯一无缺陷的维度） |

**重要背景**：本 commit 的 3 个 blocker 被项目自身复审抓出，29 分钟后由 58814ff7567b8550bcf702b02d69d8f5070ee02c 全部重写修复（换手模型逐期真实化、runner 真实接线、反退化测试）。下列问题均已修复，计入 fixed_later。

## 对照参考
净成本模型应对照成熟实践：成本应按**逐期换手**计提（bt/zipline 的 commission 模型、FINSABER 论文的 next-open+bps 模型），本版本"每月从现金全量重建"（turnover 恒 2.0）相当于假设月度清仓重开，与业界月度 rebalance 的部分调仓现实相差 3-6×（修复版实测 avg_turnover 1.14 vs 2.0 印证）。empyrical（Apache-2.0）用作风险指标是正确复用。来源域名：pmorissette.github.io/bt、github.com/stefan-jansen/zipline-reloaded。

## 问题清单
### P1-1 换手模型退化：pre_trade_weights 在两个分支都置零，turnover 恒为 2.0（月月全量重建）
- 描述：`if i == 0: pre_trade_weights = pd.Series(0.0, ...)` else 分支同样是 `pd.Series(0.0, index=df.index)`——注释谎称 "assume previous target weights adjusted by previous returns"；`realized_returns` 也恒 0。导致 `one_way_turnover` 每月返回满配账本（多空合计 2.0），成本高估 3-6×。
- 位置：`src/aionis/eval/net_cost.py:156-167`（本 commit 版本）
- 影响范围：net Sharpe / total_cost_bps 全部失真（exploratory 透镜，不进 ledger，但会误导经济有效性判断）。
- 修复建议：跨 rebalance 跟踪 prev_target 并 reindex 到当期 universe。已在 58814ff7 实现并实测 turnover 1.1444。
- 修复成本：中
- 状态：已在 58814ff7567b8550bcf702b02d69d8f5070ee02c 修复

### P1-2 runner 主干整体为注释掉的 TODO + sys.exit(1)（stub 落库）
- 描述：`track_b_net_cost_run.py` 加载 session_opens 后即 `sys.exit(1)`，OOS panel 加载与 net_cost_summary 调用全部处于注释状态——违反项目 Definition of Done（"No placeholder/TODO/stub…left (these are blockers)"）。
- 位置：`scripts/track_b_net_cost_run.py:93-101`（sys.exit(1)）、`:103-158`（注释块）
- 影响范围：mount② 无法产出任何真实结果。
- 修复建议：已在 58814ff7 重写为加载 `runs/results/<sig>/oos_state.parquet` 的真实 runner。
- 修复成本：中
- 状态：已在 58814ff7567b8550bcf702b02d69d8f5070ee02c 修复

### P2-1 测试空洞：3-ticker fixture 触发 min_per_leg 跳过 → NaN 指标 → `if np.isfinite` 守卫使全部断言形同虚设；另有 `pass` 桩测试
- 描述：`test_net_sharpe_less_than_gross_when_bps_positive` 等用 3 只股票的 fixture，`long_short_returns` 的 min_per_leg=3 使每个月都被跳过 → 指标 NaN → 断言外层的 `np.isfinite` 检查全部短路，11 个"通过"的测试无一真正验证过模型；`test_missing_session_opens_raises_error` 只有 `pass` 与注释。
- 位置：`tests/test_net_cost.py:26-70`（isfinite 守卫）、`:151-162`（pass 桩）
- 影响范围：P1-1 的退化模型因此未被任何测试拦截。
- 修复建议：已在 58814ff7 改为 ≥20-ticker fixture + 硬断言 + 反退化测试（stable scores→低换手；churn→高换手；成本线性）。
- 修复成本：中
- 状态：已在 58814ff7567b8550bcf702b02d69d8f5070ee02c 修复

### P3-1 成本按全期平均摊到每个月而非逐期计提
- 描述：`avg_cost_per_period = total_cost / n_rebalance; net_returns = gross_returns - avg_cost_per_period`——在换手恒定的退化模型下碰巧等价，但作为通用模型是错的（换手逐期可变时成本时序错配）；`n_rebalance` 分母也计入了被 continue 跳过的月份。
- 位置：`src/aionis/eval/net_cost.py:172-174`
- 影响范围：net return 时序分布失真（Sharpe 量级不受常数平移影响太多，但 drawdown 失真）。
- 修复建议：已在 58814ff7 改为 per-period cost。
- 修复成本：低
- 状态：已在 58814ff7567b8550bcf702b02d69d8f5070ee02c 修复

## 总体评分
4/10 — 作为落库 commit 违反项目自身 Definition of Done（stub/TODO/空测试三类 blocker 全占），换手经济模型实质失效；所幸项目复审机制当天抓出并在 58814ff7 完整重写（修复版实测数据自洽），流程纠偏有效但提交门槛失守。
