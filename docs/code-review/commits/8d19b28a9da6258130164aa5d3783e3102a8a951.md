# Commit 8d19b28a9da6258130164aa5d3783e3102a8a951 审查报告

**提交信息**: test(ingest): add macro forward cumulative-preserve invariant (ORCH-01)（2026-08-03，Rethymus）
**改动范围**: 2 个文件，+94/-0；`tests/test_forward_ingest.py` 新增 `test_macro_forward_cumulative_parquet_preserves_prior_rows`（75 行），外加 ORCH-01 任务规格文件。

## 四维度结论
| 维度 | 结论 | 一句话说明 |
|---|---|---|
| 稳定性 | ✅ | 纯 hermetic 测试：ALFRED 缓存命中（tmp_path fixture），无网络依赖 |
| 可扩展性 | ✅ | 复用既有 `_alfred_obs`/`_write_alfred_cache` helper 与 13D 测试同构，便于第三处镜像 |
| 生产可用 | ✅ | 断言非平凡：len==24 + snapshot_ts 集合精确相等 + T1/T2 pub_date 子集双验，窗口过滤失效（重复追加）或覆盖旧行都会被抓到 |
| 高内聚低耦合 | ✅ | test-only，未触碰 src/ 与冻结面；补齐 13D/8-K 已有而 macro 缺失的不对称不变量 |

## 对照参考
pytest 官方 hermetic 测试实践：外部 IO 用 `tmp_path` fixture + 本地缓存替代（docs.pytest.org）。本测试通过 `_write_alfred_cache` 预置 ALFRED 缓存令 `fetch_alfred_vintages` 缓存命中（macro_forward.py 文档注释明确 "cache hit -> no HTTP"），符合该模式；断言风格与既有 13D 前例一致（repo 内先例即对照基准）。

## 问题清单
通过

（说明：审查核对了 `collect_macro_forward` 的窗口过滤逻辑——`last_poll_ts` 下界 + `snapshot_ts` PIT 上界 + `assert_forward_clock` 防御后置条件；若 T2 冻结错误地重复追加 T1 行，len 会是 36 而非 24，测试能捕获。断言强度达标。）

## 总体评分
9/10 — 干净闭合反泄漏回归缺口的 hermetic 不变量测试，断言强度充分，无问题。
