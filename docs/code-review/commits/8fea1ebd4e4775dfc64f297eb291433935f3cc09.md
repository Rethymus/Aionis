# Commit 8fea1ebd4e4775dfc64f297eb291433935f3cc09 审查报告

**提交信息**: docs(design): A2/A3 macro-6 fetch+broadcast spec (owner authorized full 41 path)（2026-08-05，Rethymus）
**改动范围**: 2 个文件，+102/-6；新增 90 行 A2/A3 实装 spec（macro_headline 6 的 fetch+broadcast join，复用 `macro_surprise.py`/`macro_dff.py` 既有件），handoff 更新 climax 路径进度与 A2 YELLOW verdict。

## 四维度结论
| 维度 | 结论 | 一句话说明 |
|---|---|---|
| 稳定性 | ✅ | 纯设计文档 |
| 可扩展性 | ✅ | reuse-first 复用图明确（fetch_alfred_vintages/surprise_time_series/broadcast 均指向既有实现行号） |
| 生产可用 | ✅ | PIT 边界预定义（merge_asof backward + allow_exact_matches=False）；7-gate 分级清楚 |
| 高内聚低耦合 | ✅ | 新逻辑收敛到单一新文件 macro_headline.py，0 改动 frozen surface |

## 对照参考
不适用（设计 spec 文档；merge_asof backward 的 PIT broadcast 惯例与 pandas 官方 as-of join 语义一致）。

## 问题清单
### P3-1 daily 序列 surprise 的"适配"口径未在 spec 钉死
- 描述：§2 对 term_spread/credit_spread 写 "z-score 复用 `surprise_time_series` 适配 daily"——`surprise_time_series` 的 trailing 24 窗 + min 12 是为月频 surprise 设计，直接套 daily 意味着回看窗从 24 个月缩为约 24 个交易日，两个市场/频率下 surprise 的统计含义不同，spec 未明确窗口换算。
- 位置：`reports/design/2026-08-05-macro-6-fetch-broadcast-spec.md:36`（§2 表格 surprise 列）
- 影响范围：后续 be115ea8 的实装口径与 confirmatory 41 特征的可解释性。
- 修复建议：在 spec/实装中显式声明 daily 窗口（如 trailing 24 月频 vs 252 daily 的等效性）并落测试。
- 修复成本：低
- 状态：待人工复核（实装 be115ea8 按原样复用 trailing 24，未见窗口换算说明）

## 总体评分
8/10 — 复用优先、PIT 边界清楚的设计 spec；daily surprise 窗口口径留了一个含糊点。
