# Commit 9414a2ca606309e931cb229bf0b82d3bf7aba1fb 审查报告

**提交信息**: feat(ingest+web): Form 4 insiders module live (real data) + 2 parser schema fixes（2026-08-06，Rethymus）
**改动范围**: 14 个文件，+871/-47；修复 form4.py 两个 schema P1（P/S 代码过滤 + 双 schema 日期）、夹具同步更新、新增 scripts/form4_fetch.py（5 个大盘股 CIK 限界抓取）、export_form4（awaiting_fetch 诚实降级）、/insiders 展示页与真实 form4.json。

## 四维度结论
| 维度 | 结论 | 一句话说明 |
|---|---|---|
| 稳定性 | ⚠️ | 解析器修复正确且带回归测试；但新脚本 form4_fetch.py 引用已更名列，运行即 KeyError（真实运行是由 orchestrator 旁路完成的） |
| 可扩展性 | ✅ | 限界 CIK 集合 + 幂等缓存；导出层 awaiting_fetch 降级路径完整 |
| 生产可用 | ⚠️ | UI/JSON 方法论文案仍描述旧的 A/D 过滤（与实际 P/S 语义不符），误导性披露 |
| 高内聚低耦合 | ✅ | ingest→parquet→export→静态 JSON→React 单向流，展示层不回写 |

## 对照参考
P/S=公开市场买/卖、A=授予、M=行权 的代码语义与 SEC 官方 Ownership Form Codes 表（sec.gov/edgar/searchedgar/ownershipformcodes.html）及 Ownership XML 技术规范（sec.gov/info/edgar/ownershipxmltechspec-v3.pdf）一致；edgartools（edgartools.readthedocs.io）同样以 P/S 提取公开市场内部人交易。修复后的双 schema 日期处理（value 优先、year/month/day 回退）符合规范中的版本演进。

## 问题清单
### P1-1 form4_fetch.py 引用已删除列 acquired_or_disposed，脚本不可运行
- 描述：同一 commit 内 form4.py 把 `acquired_or_disposed`（值 A/D）改名为 `buy_or_sell`（值 buy/sell），但新脚本仍写 `buys = (df["acquired_or_disposed"] == "A").sum()`——首个有交易的 issuer 即 KeyError 中断，且 parquet 在循环后才落盘，什么都不会写出。commit message 自述真实运行由 orchestrator 旁路完成（"committed without running the script — oversight"）。
- 位置：`scripts/form4_fetch.py:48-49`
- 影响范围：scripts/form4_fetch.py 全部运行路径（提交时点脚本完全不可用）。
- 修复建议：改为 `df["buy_or_sell"] == "buy"/"sell"`。
- 修复成本：低
- 状态：已在 552e35e3 修复

### P2-1 展示层方法论文案仍描述旧的 A/D 过滤语义
- 描述：`insiders.explain`（中英两处）与 form4.json methodology 写 "Non-derivative pure buy (A) / sell (D) only"——本 commit 刚把实际过滤改为 transactionCode P/S（A 在新语义里是"授予"而非"买入"）。公开终端上的方法论披露与实际数据语义相悖，恰好违背项目"诚实披露"的立身原则。
- 位置：`web/src/i18n/dict.ts`（insiders.explain 中英）；`scripts/export_terminal_data.py:135,163`（export_form4 methodology）
- 影响范围：/insiders 页所有读者对数据口径的理解。
- 修复建议：文案改为 "open-market purchase (P) / sale (S) only"。
- 修复成本：低
- 状态：未修复（main 上 dict.ts 两处与 export 脚本 methodology 字符串至今未更正）

### P3-1 export_form4 的 window 硬编码
- 描述：`"window": "2024-01..2026-06 (bounded large-cap set)"` 字面量写死，抓取窗口变化时会静默失真。
- 位置：`scripts/export_terminal_data.py:172`
- 影响范围：展示层窗口标注准确性。
- 修复建议：由数据 min/max 日期推导。
- 修复成本：低
- 状态：已在 1a0f4bf 修复（改为从数据推导 w_start/w_end）

## 总体评分
6/10 — 用真实数据暴露并修复两个解析 P1、补上回归测试，流程值得肯定；但同 commit 引入不可运行的新脚本（自认 oversight）且方法论披露文案与实际过滤相反未更新，扣分明显。
