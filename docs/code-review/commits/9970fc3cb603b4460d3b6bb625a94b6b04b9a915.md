# Commit 9970fc3cb603b4460d3b6bb625a94b6b04b9a915 审查报告

**提交信息**: feat(site): add FF5 residual decomposition section (mount 3, market structure)（2026-08-03，Rethymus）
**改动范围**: 2 个文件，+47/-0；`scripts/build_static_site.py` 新增 `_ff5_section_html()` 渲染 FF5 α/β/R² 对照表并挂到页面，`site/index.html` 为对应的生成产物。

## 四维度结论
| 维度 | 结论 | 一句话说明 |
|---|---|---|
| 稳定性 | ✅ | `mount` 为空 / KeyError / TypeError 均有降级文案，数值格式化对 NaN 也不致崩溃 |
| 可扩展性 | ✅ | 与既有 `_metrics_section_html` 同构，betas 用 (key, label) 列表驱动 |
| 生产可用 | ⚠️ | f-string 拼 HTML 无转义（当前数据源为本仓库自产 JSON，风险低）；GROSS/无 vintage 泄漏警示已明示 |
| 高内聚低耦合 | ✅ | 展示层函数独立，读取 mount_metrics.json，未触碰研究管线（display-only 边界保持） |

## 对照参考
Jinja2/OWASP 惯例：模板渲染须 autoescape，或 f-string 拼 HTML 时对插值 `html.escape()`（jinja.palletsprojects.com、codiga.io）。本实现裸 f-string 拼接，但数据全部来自本仓库自产的 `site/mount_metrics.json`（数值 + 项目自写 note），非用户输入，实际 XSS 面接近零，记 P3。

## 问题清单
### P3-1 循环变量命名 `l` 触发 ruff E741
- 描述：`for l, tv, pv in rows` 使用歧义变量名 `l`，ruff E741 报错。
- 位置：`scripts/build_static_site.py:141`（`_ff5_section_html` 内 body 生成循环）
- 影响范围：lint 门禁（无运行时影响）。
- 修复建议：改名 `lbl`。
- 修复成本：低
- 状态：已在 0343ce5e21c4233348b83f797d9ccea5a81d892b 修复

### P3-2 f-string 拼 HTML 未转义插值（含 ff5_note）
- 描述：`{note}`、`{l}`、`{tv}` 等直接内插 HTML。数据虽为自产 JSON，若未来 mount_metrics.json 引入外部来源字段（如抓取的说明文本），将形成存储型 XSS 面。
- 位置：`scripts/build_static_site.py:128-136`
- 影响范围：静态站点构建产物（当前可信输入，风险低）。
- 修复建议：对文本型插值统一 `html.escape()`，或迁移到 Jinja2 `autoescape=True`。
- 修复成本：低
- 状态：待人工复核

## 总体评分
8/10 — 干净的展示层增量，降级路径完备、诚实警示到位；仅余 lint 命名与转义卫生两类小问题。
