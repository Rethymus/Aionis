# Commit 9edd8793441742c0afd37c4c66086f532014771d 审查报告

**提交信息**: feat(web): TrustRibbon — surface anti-leakage trust basis next to the verdict（2026-08-13，Rethymus）
**改动范围**: 3 个文件，+84/-0；新增 `web/src/components/overview/trust-ribbon.tsx`（66 行，PIT/embargo/H6/config 四徽章 + `/discipline` 链接），`overview.tsx` 插入到 Hero 与 VerdictAnchor 之间，`dict.ts` 新增 zh+en 各 8 个 `trust.*` i18n 键。

## 四维度结论
| 维度 | 结论 | 一句话说明 |
|---|---|---|
| 稳定性 | ✅ | 纯静态展示组件，无数据获取、无外部调用、无状态 |
| 可扩展性 | ✅ | 徽章由 `GUARDS` 数组驱动，新增守卫项只需加数组项 + i18n 键 |
| 生产可用 | ✅ | `aria-label` 完整、`title` 提示、链接目标 `/discipline` 已确认存在 |
| 高内聚低耦合 | ✅ | 位于 `web/` 展示层，只读静态 i18n，与研究管线零耦合（leakage-safe） |

## 对照参考
与 Next.js 官方实践一致：`next/link` 用于内部路由导航（nextjs.org/docs）；i18n 键通过 `as const` 字典 + `keyof typeof` 派生 `DictKey`，键缺失在编译期报错，属于类型安全字典模式（typescriptlang.org/docs）。`GuardKey` 局部联合类型必须可赋值给 `DictKey` 才能通过 `t(g.key)`，因此键与字典的同步由 tsc 保证。

## 问题清单
通过（一处可选优化：`GuardKey` 手工声明而非 `Extract<DictKey, "trust.${string}">` 从字典派生，属锦上添花；后续 c7ad6b1 视觉审计已对该组件做过对比度调整）。

## 总体评分
9/10 — 干净、类型安全、零研究管线接触的展示层新增，自述 puppeteer 验证充分。
