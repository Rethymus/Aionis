# Commit a09b9e4280243037e649529fc83ff3848725bfc2 审查报告

**提交信息**: feat(track-b): FREEZE feature_cols amendment (ledger #41, 23 features); S0 complete（2026-08-02，Rethymus）
**改动范围**: 2 个文件，+3/-1；`runs/ledger.jsonl` 追加行 #41（track_b config_committed 修订，sig bf620bdf…c32ba4f9，23 特征）+ prereg 补记修订 sig 与物化清单。

## 四维度结论
| 维度 | 结论 | 一句话说明 |
|---|---|---|
| 稳定性 | ✅ | append-only（40→41 行）；修订行显式声明 amends #40，链路清晰 |
| 可扩展性 | ✅ | 「方法学冻结（#40）→ 数据构造（无 ledger）→ 特征集冻结（#41，首拟合前）」的阶段化修订范式可复用 |
| 生产可用 | ✅ | **签名独立复算 MATCH**、时序在首个模型拟合/OOS 之前（首个 track_b 结果行更晚出现） |
| 高内聚低耦合 | ✅ | feature_cols 与模块 TRACK_B_*_FEATURE_COLS 常量逐一吻合（13+10=23，无字面重复） |

## 审查验证（独立复核）
1. 行 #41 的 config 以 `json.dumps(sort_keys=True)` 规范化重算 sha256 = `bf620bdf756810d5aab4db639b70856613885ce48e11e6a198884ab4c32ba4f9`，与账本一致（MATCH）。
2. `feature_cols` 23 项与 af7b931 的两个清单常量完全一致；无 outcome 选择痕迹。
3. 账本时序：#41 之后的首个 track_b 结果（a-run / #42 基线）均在后续 commit 才出现——冻结先于观察成立。

## 对照参考
分阶段 config 修订（amendment as new ledger row）与临床试验中对 SAP 的 amendment 备案机制同构——不改写原冻结、显式引用被修订行。不适用代码对照。

## 问题清单
### P3-1 冻结集固化了两对共线特征与 sum 近似 momentum（继承自 af7b931，随冻结放大）
- 描述：23 特征中 `reversal_5d ≡ −momentum_5d`、`investment_12m ≡ asset_growth_12m`（同函数同参）；`momentum_*` 为收益率求和近似（见 af7b931 报告 P2-1）。本 commit 把这些口径**冻结进红线 config**——此后修正需走新的 amendment 行，纠错成本被抬高。
- 位置：`runs/ledger.jsonl:41`（feature_cols）
- 影响范围：S1 及此后所有 Track B 拟合的特征空间与重要度解读。
- 修复建议：后续 amendment（若 owner 认可）中删除两对共线条目之一并切换精确累计收益；或至少在 prereg 附录披露共线事实。
- 修复成本：低（文档披露）/ 中（特征变更需新修订行+重跑）
- 状态：未修复

## 总体评分
8/10 — 修订行机制教科书级执行（独立复算签名吻合、amends 显式、先冻结后拟合）；扣分点是被冻结的特征集本身带有上游模块的共线与近似瑕疵。
