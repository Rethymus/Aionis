# Commit a1a8094faa684d8d08dc5c08679698d79d1b96a5 审查报告

**提交信息**: feat(phase-d): EFTS self-filing filter for 13D (subject-side filer resolution)（2026-07-29，Rethymus）
**改动范围**: 2 个文件，+511/-0；新增 `src/aionis/ingest/stakes_13d_efts.py`（EDGAR 全文检索发现 13D + filer CIK 解析 + 自报过滤）与 `tests/test_stakes_13d_efts.py`（12 个 hermetic 测试：filer 顺序无关性/自报丢弃/URL 钉死/分页/缓存/退避/4xx fail-fast/空安全）。

## 四维度结论
| 维度 | 结论 | 一句话说明 |
|---|---|---|
| 稳定性 | ✅ | 分页终止条件双保险（batch<page 或 out>=total）、窗口键控的整列表缓存、无部分缓存写入、日期防御性二次过滤 |
| 可扩展性 | ⚠️ | `_get_json` 第三份拷贝（fundamentals/stakes_13d/此处），检索语义改动需三处同步；`_PAGE_SIZE`/表单常量化良好 |
| 生产可用 | ⚠️ | 活体探针结论（10 位 CIK、root form 展开、from= 分页、逗号列表陷阱）写进 docstring 防再踩坑是亮点；0.15s 间隔低于后定的 ≥2s 规则、UA 邮箱仍为占位符 |
| 高内聚低耦合 | ✅ | 与 stakes_13d（issuer-side submissions）职责互补、单向依赖，`filter_external_13d` 纯函数不改输入（有测试钉住） |

## 对照参考
EFTS 端点 `efts.sec.gov/LATEST/search-index` 是 [SEC EDGAR Full-Text Search 官方 UI](https://www.sec.gov/edgar/search/)（[官方 FAQ](https://www.sec.gov/edgar/search/efts-faq.html)、[EDGAR API 门户](https://www.sec.gov/search-filings/edgar-application-programming-interfaces)）背后的公开 JSON 接口，`q/ciks/forms/dateRange/startdt/enddt/from` 参数与 [社区文档](https://stackoverflow.com/questions/76948766/sec-filings-data-using-api-in-python-with-filter-for-specific-strings) 一致；本模块以 live-probe 证据钉住三个官方文档未明说的行为（10 位零填充 CIK、root form 自动含 13D/A、逗号表单列表会丢原始件），比盲从文档更可靠。出处域名：sec.gov、stackoverflow.com。

## 问题清单
### P2-1 0.15s 请求间隔（`_get_json` 第三份拷贝）低于项目后定的 ≥2s 礼貌红线
- 描述：与 stakes_13d 相同的 `time.sleep(0.15)`——符合 SEC 官方 ≤10 req/s 且与当时 G7 rubric 一致，但 3 天后 AUD-05A 将全部 14 个直连站点统一为 host 级 ≥2s。
- 位置：`src/aionis/ingest/stakes_13d_efts.py:129`
- 影响范围：批量拉取（每 issuer 多页）时的请求压力。
- 修复建议：统一接入 http 策略层。
- 修复成本：低
- 状态：已在 e8545d40a34b6cf73444d4621926c17ac6d707857 修复（该 commit 同时消除了三份 `_get_json` 拷贝）

### P3-1 filer 判定用「第一个非发行人 CIK」近似，自报假事件可残留
- 描述：`_filer_cik` 取 `ciks[]` 中第一个 ≠ issuer 的 CIK——命名了第三方受托人/信托的自报（如托管结构）会被判为外部事件而残留（多计事件、稀释信号，非泄漏）；返回的 `filer_cik` 字段本身对多实体 filing 也不精确。docstring 已自我声明"ciks[] 顺序不可靠"，属已知近似。
- 位置：`src/aionis/ingest/stakes_13d_efts.py:168-178`
- 影响范围：13D 事件指示特征的纯度（少量假事件）。
- 修复建议：cover-page 受益人解析（工程重，pre-reg §0.5④ 已列为可选）。
- 修复成本：高
- 状态：未修复（已在 e0c26b9db457e1f3d76715b1d789f811889bb83b 作为 MEDIUM-3 已知局限写入预注册披露）

### P3-2 `_get_json` 第三份近乎逐字重复
- 描述：与 `stakes_13d._get_json` 仅差 `fp=None` 可选缓存一处——DRY 漂移面扩大到三处。
- 位置：`src/aionis/ingest/stakes_13d_efts.py:80-134`
- 影响范围：传输层策略的维护一致性。
- 修复建议：提取到 ingest 公共模块。
- 修复成本：低
- 状态：已在 e8545d40a34b6cf73444d4621926c17ac6d707857 修复（统一 `_policy_get`）

## 总体评分
8/10 — 用 live-probe 证据解决"submissions 端点拿不到 filer"这一数据结构缺口的方案干净且测试极全；扣分在第三份传输层拷贝与 filer 近似（后者当日内即披露）。
