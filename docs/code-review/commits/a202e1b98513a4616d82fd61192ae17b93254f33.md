# Commit a202e1b98513a4616d82fd61192ae17b93254f33 审查报告

**提交信息**: docs(e3): implementation plan + owner decisions (GLM pin, shadow 1-2mo)（2026-07-30，Rethymus）
**改动范围**: 2 个文件，+185/-2；新增 `docs/phase-e3-implementation-plan.md`（架构 6 件套 + 9 条零泄漏不变量 I1–I9 + 7 个依赖序切片 + owner 决议 Q1–Q7），TASK-E3-launch 状态推进 PLANNED/Slice 1 解封。纯文档。

## 四维度结论
| 维度 | 结论 | 一句话说明 |
|---|---|---|
| 稳定性 | ✅ | 计划引用的复用接缝逐一核实真实存在（`ingest/reddit_sentiment.py`、`eval/two_arm.py`、`extraction/extract.py`、`reporting/run_log.py`、`features/macro_surprise.py`、`ingest/stakes_13d.py`、`reporting/results.py`），非纸面引用 |
| 可扩展性 | ✅ | I1–I9 每条不变量都给出机制 + 可测断言（如"reveal at t<target → REJECTED"），直接转化为 TDD 验收门；I8 明确 refinement=新 sig=平行序列，避免冻结序列被污染 |
| 生产可用 | ✅ | owner 决议记录完整（GLM pin + shadow 1–2 月 + 4 项默认采纳 + 2 项显式 deferred）；GLM"曾为 test-only"的历史风险被 shadow 期显式对冲 |
| 高内聚低耦合 | ✅ | 明确"E3 不复用 purged CV 做评分——t 时刻无未来测试块，防泄漏靠 t+21 标签物理未知"，与前视设计的关键区别讲透；LLM 池限定 GLM/SiliconFlow/ModelScope 符合红线 |

## 对照参考
不适用（纯规划文档）。其"commit-then-reveal"（先公布哈希承诺、到期才揭示）与密码学承诺方案（hash commitment，如 Wikipedia commitment scheme 条目所述）及预测注册表（osf.io/preregistration）的时锁披露实践同构，将项目"sha256-before-result"从每 run 粒度细化到每预测粒度是正确的延伸方向。

## 问题清单
通过。

## 总体评分
10/10 — 罕见质量的实施计划：不变量全部可测、复用点全部核实、风险决策全部显式留痕，直接支撑后续 TDD 切片。
