# Commit a5ca91582299f219211d6f7309ef64585a1e7e8a 审查报告

**提交信息**: fix(data): default BACKTEST_MONTHS=99 (full OOS history) in export + cron（2026-08-08，Rethymus）
**改动范围**: 3 个文件，+4329/-545；`export_terminal_data.py` 的 BACKTEST_MONTHS 默认 24→99，cron 删除 `'24'` 覆盖（回归脚本默认），picks_backtest.json 完整重导出回 131 个月/655 picks（hit_rate 48.09% 与 0a17416b 原始交付一致）。

## 四维度结论
| 维度 | 结论 | 一句话说明 |
|---|---|---|
| 稳定性 | ✅ | 修复经数据验证：131 个月与 655 picks 逐值恢复，与被 7d90176f 意外截断前的基线一致 |
| 可扩展性 | ⚠️ | 用「99=事实上全部」的魔法数表达"全历史"，OOS 历史超过 99 个月（约 2029 年后）会再次无声截断 |
| 生产可用 | ✅ | cron 与本地默认统一（不再覆盖），消除两套语义；commit message 如实描述了此前截断 |
| 高内聚低耦合 | ✅ | 配置语义收敛到脚本单点，workflow 仅注释指引 |

## 对照参考
不适用（参数默认值修复）。「用哨兵值表达 all」是通用配置设计惯例（如 `limit=0 → unlimited`），可参考常见 CLI 库（click/typer）对 flag 默认值的语义化处理（click.palletsprojects.com）。

## 问题清单
### P3-1 「99 = 事实上全部」是脆弱的魔法数上限
- 描述：`N_MONTHS = int(os.environ.get("BACKTEST_MONTHS", "99"))` —— 99 只是当前全历史（68 个月/区域、131 条目）的足够大值，并非"all"语义；数年后 OOS 历史超过 99 个月时将再次无声截断，重演本次事故。
- 位置：`scripts/export_terminal_data.py:277-278`（该 commit 版本）
- 影响范围：长期演进下的 track record 完整性。
- 修复建议：支持 `BACKTEST_MONTHS=all`（或 0=全部）哨兵值，默认 all。
- 修复成本：低
- 状态：未修复（main 上默认仍为 99）

（P3 备注：0a17416b 遗留的 docstring「past 6 realized months」在本 commit 仍未同步更新。）

## 总体评分
9/10 — 快速、诚实、经数据验证的完整修复（131/655/48.09% 三值精确还原）；仅魔法数语义可再机制化。
