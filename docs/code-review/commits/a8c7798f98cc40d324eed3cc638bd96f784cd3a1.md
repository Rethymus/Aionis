# Commit a8c7798f98cc40d324eed3cc638bd96f784cd3a1 审查报告

**提交信息**: fix(phase-d): window-keyed fetch cache + coverage guard (review HIGH/MEDIUM)（2026-07-29，Rethymus）
**改动范围**: 2 个文件，+42/-0；`scripts/phase_d_fetch.py`（per-ticker 缓存写入/校验 start/end 窗口键）与 `scripts/phase_d_run.py`（NYSE 网格 assert + commit_config 前的覆盖守卫）。

## 四维度结论
| 维度 | 结论 | 一句话说明 |
|---|---|---|
| 稳定性 | ✅ | 窗口不匹配即自失效重抓；旧格式缓存（无 start/end 键）经 `cached.get("start") != START` 自然失效，向后兼容正确 |
| 可扩展性 | ✅ | 0.8 覆盖阈值硬编码于脚本内，属一次性 confirmatory 守卫，可接受 |
| 生产可用 | ✅ | 覆盖守卫位于 `commit_config` 之前，正确阻止把退化数据写入账本（防"meaningless confirmatory:first"） |
| 高内聚低耦合 | ⚠️ | `phase_d_sic_map.parquet` 在 `_build_rel_extra` 与 `main` 中各读一次，小重复 |

## 对照参考
缓存条目内嵌请求参数并在读取时校验、失配即失效的模式，与 requests-cache 按完整请求（URL+params）作缓存键、参数变化不命中缓存的语义一致（requests-cache.readthedocs.io）；NYSE 会话网格取自 pandas-market-calendars 的标准 `valid_days` 用法（pandas-market-calendars.readthedocs.io）。本实现以"键字段写入条目 + 读取时自检"替代重新计算缓存键，是该场景下合理的轻量等价物。

## 问题清单
### P3-1 防御性检查使用 `assert`，`python -O` 下会被剥离
- 描述：`assert pd.DatetimeIndex(px.index).normalize().equals(...)` 作为"defense-in-depth"防线，若以 `python -O` 运行则整段检查静默消失，退回到它本要防止的 silent-NaN 失败模式。
- 位置：`scripts/phase_d_run.py:53-56`
- 影响范围：仅当用户以 `-O` 运行脚本时防御失效；默认运行不受影响。
- 修复建议：改为显式 `if not ...: raise ValueError(...)`。
- 修复成本：低
- 状态：未修复

### P3-2 同一 parquet 重复读取
- 描述：`_build_rel_extra` 内 `sic = pd.read_parquet(CACHE / "phase_d_sic_map.parquet")`，`main` 紧接着再次 `sic_df = pd.read_parquet(CACHE / "phase_d_sic_map.parquet")`，同一文件两次 IO。
- 位置：`scripts/phase_d_run.py:60` 与 `scripts/phase_d_run.py:101`
- 影响范围：一次性脚本，纯冗余 IO，无正确性影响。
- 修复建议：让 `_build_rel_extra` 返回 sic_df 或在 main 读取后传入。
- 修复成本：低
- 状态：未修复

## 总体评分
9/10 — 针对性极强的防泄漏加固：窗口键自失效与 commit 前覆盖守卫均落在正确的顺序位点上，仅余 assert 可剥离与轻微重复读取两处瑕疵。
