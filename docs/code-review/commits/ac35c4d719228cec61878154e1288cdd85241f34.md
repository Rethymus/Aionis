# Commit ac35c4d719228cec61878154e1288cdd85241f34 审查报告

**提交信息**: fix(web): explicit SmartMoney type — unbreak deploy (empty active_filers → never[])（2026-08-10，Rethymus）
**改动范围**: 1 个文件，+18/-1；`web/src/data/aionis/index.ts` 新增显式 `SmartMoney` 类型并把 `smartMoney` 从 `typeof smartMoney`（JSON 字面量推断）改为 `as SmartMoney`，修复 dd0ba707 导出 `active_filers: []` 后 TS 推断 `never[]` 导致的 deploy 编译失败。

## 四维度结论
| 维度 | 结论 | 一句话说明 |
|---|---|---|
| 稳定性 | ✅ | 类型与 dd0ba707 导出 payload 字段一一对应（含 `latest_date: string \| null`） |
| 可扩展性 | ✅ | 沿用本文件既有显式类型 + cast 模式（CalibrationReliability/ThemeSignals/MacroDrivers） |
| 生产可用 | ✅ | 热修复恢复 deploy；类型不随 JSON 内容漂移 |
| 高内聚低耦合 | ✅ | 单点修改，数据契约收敛在 index.ts |

## 对照参考
不适用（琐碎类型修复）。TS `resolveJsonModule` 对空数组字面量推断 `never[]` 属语言既定行为；显式接口 + 断言是该代码库已有约定（TypeScript handbook "Type Assertions"）。

## 问题清单
通过。

（备注：`as SmartMoney` 是无运行时校验的断言，导出端字段改名时 tsc 不会报警 —— 属代码库整体约定的取舍，非本 commit 引入。根因 —— 排名窗口被哨兵行挤空 —— 由紧随的 ca93f39e 修复。）

## 总体评分
9/10 — 精准、最小、符合约定的热修复；根因另 commit 处理。
