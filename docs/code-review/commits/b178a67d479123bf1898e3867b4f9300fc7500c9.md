# Commit b178a67d479123bf1898e3867b4f9300fc7500c9 审查报告

**提交信息**: feat(scripts): track_c_joint_run asymmetric35 toggle (A1 companion)（2026-08-05，Rethymus）
**改动范围**: 1 个文件，+89/-29；`scripts/track_c_joint_run.py` 增加 `TRACK_C_JOINT_MODE` 环境变量切换（shared 默认 / asymmetric35），产物按 mode 打标签（`track_c_joint_{shared10,asym35}_*`）避免覆盖，未知 mode 显式 SystemExit。

## 四维度结论
| 维度 | 结论 | 一句话说明 |
|---|---|---|
| 稳定性 | ✅ | 未知 mode fail-fast；运行前置存在性检查保留；mode 写入 summary JSON 可审计 |
| 可扩展性 | ⚠️ | 特征清单在 runner 内硬编码复制 ledger 冻结清单（见 P3-1） |
| 生产可用 | ✅ | 探索性 runner 不写 ledger；产物 mode 隔离防覆盖；caveats 随产物持久化 |
| 高内聚低耦合 | ✅ | 只改 runner 薄层，复用 A1 的 build_joint_panel 非对称接口 |

## 对照参考
不适用（薄 runner 脚本，无新算法逻辑；环境变量驱动模式 + 产物命名隔离与 MLflow 等实验追踪的 run-tagging 惯例一致，仅作类比）。

## 问题清单
### P3-1 特征清单硬编码复制 ledger #46/#48 冻结清单
- 描述：`US_FUNDAMENTALS_13`/`CN_EXTRAS_2`/`US_23`/`CN_12` 在 runner 内手写复制（注释自述 "must match ledger #46/#48 feature_cols"），与冻结配置存在漂移风险；仅靠注释 + `feature_cols_union_count` 落盘约束。
- 位置：`scripts/track_c_joint_run.py:47-57`
- 影响范围：asymmetric35/asymmetric41 探索性结果的可复现口径；若 ledger 修订而 runner 未同步，探索数字口径悄悄失配。
- 修复建议：从 ledger #48 行解析 feature_cols 或将清单提取到共享常量模块，runner 引用单一来源。
- 修复成本：中
- 状态：未修复（后续 9a13518 沿同一模式扩至 asymmetric41）

### P3-2 IC 序列 pd.concat(axis=1) 未显式 sort=
- 描述：三区域 IC 序列按 DatetimeIndex 外连接 concat 未传 `sort=`，Pandas4Warning 路径。
- 位置：`scripts/track_c_joint_run.py:165`
- 影响范围：日志噪音/未来 pandas 兼容。
- 修复建议：显式 `sort=True`。
- 修复成本：低
- 状态：已在 52a8d0982de07fda7fffbcef9fdec5db73328740 修复

## 总体评分
8/10 — 干净的 mode 隔离与 fail-fast 设计，两处 P3（配置复制、concat sort 后者已修复）不影响探索结论有效性。
