# Commit b39ed58adc33db12994c4b63b78906e19f696f7a 审查报告

**提交信息**: docs: OHLCV panel extension plan (step 1, evidence-grounded)（2026-08-11，Rethymus）
**改动范围**: 1 个文件，+699/-0；新增 `docs/ohclv-panel-extension-plan.md` —— 现状管线审计（OHLCV 在 ingest 被丢弃的确切位置）、纯增量扩展方案、H6/账本/预注册骨架（含 n_trials 决策）、owner 决策门与代码 diff 预览。

## 四维度结论
| 维度 | 结论 | 一句话说明 |
|---|---|---|
| 稳定性 | ✅ | 纯计划文档，未动任何代码/配置 |
| 可扩展性 | ✅ | 分步解耦（安全基础设施 1-3 vs 需 owner 批准的 4），与 adaptive 路线图对齐 |
| 生产可用 | ⚠️ | 以"evidence-grounded"为卖点，但至少一处关键"已验证"结论与代码事实不符 |
| 高内聚低耦合 | ✅ | 只读研究车道产物，预注册骨架含 SESOI/n_trials/多重检验方案 |

## 对照参考
不适用（内部规划文档）；其方法论（additive schema 变更 + H6 bit-identical 验证 + config_committed 前置）与项目 WORKFLOW/ADR-006 一致。

## 问题清单
### P2-1 「`_volume_from_alpaca` 无任何调用方（verified via grep）」是错误断言
- 描述：文档 §1.1 称 volume 抓取函数 "EXISTS BUT NEVER CALLED"，证据写明 "No caller in the codebase (verified via grep)"。但同一 commit 的代码树里 `scripts/track_b_fetch_volume.py:25` 明确 `from aionis.ingest.market import _from_alpaca, _volume_from_alpaca` 并在 `:115` 调用（该脚本 2026-08-02 b7472c3a 引入，早本文档 9 天）。该断言至今未被更正（main 上原文仍在），而文档的风险评估（"最小改动=停止丢列"）部分依赖这份审计的可信度；在一个明令"禁止捏造反泄漏事实"的项目里，假的"已验证"声明应零容忍。
- 位置：`docs/ohclv-panel-extension-plan.md:44-48、525`（该 commit 版本）
- 影响范围：后续实现者对 volume 数据链路的理解（track-b 的 turnover_21d 已有一条 volume 通路）；文档可信度。
- 修复建议：更正为"`_volume_from_alpaca` 被 `scripts/track_b_fetch_volume.py` 使用（track-b volume 通路），但未进入冻结面板构建"；重跑一次真实的 grep。
- 修复成本：低
- 状态：未修复

### P3-1 文件名拼写错误：ohclv → ohlcv
- 描述：文件名 `ohclv-panel-extension-plan.md` 把 OHLCV 的 l/c 写反（ohCLv）；正文标题全是 OHLCV。仓库规程禁止重命名既有 tracked 文档（断引用），错拼将成为永久文件名。
- 位置：`docs/ohclv-panel-extension-plan.md`（文件名）
- 影响范围：检索体验（按 "ohlcv" 搜不到）。
- 修复建议：若引用尚少（可全库 grep）可破例一次性改名并同步引用。
- 修复成本：低
- 状态：未修复

### P3-2 附录 diff 预览与所述现状结构不符
- 描述：§10 预览中 `P_close = prices["adjCount", tickers]`（原文 `prices["adjClose", tickers]`）假设 prices 是 MultiIndex 列宽表，而 §1.3 自己描述的现状是单层列（date × ticker 的 close-only 宽表）；tiingo 预览还残留与新逻辑矛盾的 `col` 变量处理。作为"实现蓝图"会误导第一步实现（实际 8b0154d 另起 `fetch_ohlcv_panel` 增量函数，未照抄本预览）。
- 位置：`docs/ohclv-panel-extension-plan.md:660-699`（§10 附录）
- 影响范围：按图施工的实现者。
- 修复建议：预览应标注"示意、非可直接套用"，或按真实结构改写。
- 修复成本：低
- 状态：未修复

### P3-3 文档内使用宿主机绝对路径
- 描述：全文引用 `/home/re/code/Aionis/...` 形式的绝对路径（作者机器布局），而非仓库相对路径，跨环境不可复用。
- 位置：`docs/ohclv-panel-extension-plan.md`（多处，如 §1.1、文末）
- 影响范围：可读性/可移植性。
- 修复建议：统一改为 `src/aionis/...` 相对路径。
- 修复成本：低
- 状态：未修复

## 总体评分
7/10 — 规划结构（增量性论证、H6 验证步骤、owner 门、预注册骨架）符合项目纪律；但"evidence-grounded"文档里存在可被一行 grep 证伪的"已验证"断言且未更正，加上文件名拼写与蓝图-现状不符，削弱了其作为施工依据的可靠性。
