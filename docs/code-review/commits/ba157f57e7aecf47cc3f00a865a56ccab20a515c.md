# Commit ba157f57e7aecf47cc3f00a865a56ccab20a515c 审查报告

**提交信息**: feat(ingest): A-share price fetch for CSI300 universe + S0 snapshot (sig a4614876)（2026-08-04，Rethymus）
**改动范围**: 2 个文件，+132/-0；新增 `scripts/ashare_price_fetch_csi300.py`（116 行真实拉取脚本，949 ticker 单会话批拉）+ intake doc 快照记录（sha256 a4614876…、覆盖核验、G3/G6/G7 留痕）。

## 四维度结论
| 维度 | 结论 | 一句话说明 |
|---|---|---|
| 稳定性 | ✅ | per-ticker try/except 隔离（一坏不废全批）、logout finally、50-ticker 进度输出；0 失败完成真实拉取 |
| 可扩展性 | ⚠️ | 不可断点续传（"re-runs overwrite wholesale" 为文档化取舍）；复用适配器私有函数 `_query_one/_ensure_login` |
| 生产可用 | ✅ | ≥2s G7 间距、raw adjustflag='3'（G3 方案 A）、停牌 NaN（G6，68,201 天）、sha256 + END 硬编码保证跨日重建确定性；覆盖核验（20 空 ticker 全为 2016 前成员 → 0 缺口）记录在案 |
| 高内聚低耦合 | ✅ | 脚本薄、逻辑在 ingest 适配器；快照不入 ledger 的边界规则明确 |

## 对照参考
脚本形态与 baostock 社区最佳实践（baostock.com 官方 API 文档、CSDN《baostock 常见 5 错》等：单 login 会话、每查询限速、finally logout、逐标的隔离失败）完全对齐——上一版适配器批循环无隔离的问题在本脚本层被正确补偿；"同源重算同一 sha256" 的确定性声明因 END 硬编码 + raw 价交易所固定而比 CSI300 快照（today() cap）更站得住。来源域名：baostock.com、blog.csdn.net。

## 问题清单
### P3-1 覆盖核验为人工一次性（commit message/doc），未固化为脚本断言
- 描述：20 个空返回 ticker 与 949 历史成分的差集核对（"全部 pre-2016-only → 0 覆盖缺口"）是人工结论；脚本只打印 `tickers_ok=929`，未来重跑若出现新的意外空 ticker（会话静默失效的已知失效模式），无人自动拦截，且失败清单不改变退出码（`return 0`）。
- 位置：`scripts/ashare_price_fetch_csi300.py:96-110`
- 影响范围：S0 快照的未来重建可靠性。
- 修复建议：脚本尾部加覆盖断言（空 ticker 必须全部满足"最后成分日 < START"）+ failures>0 时 `return 2`。
- 修复成本：低
- 状态：未修复

### P3-2 跨模块导入 ingest 适配器私有成员
- 描述：`from aionis.ingest.ashare_price import _COLUMNS, _ensure_login, _query_one, _require_baostock`——4 个下划线私有符号被脚本层直接使用，适配器内部重构会静默破坏脚本。
- 位置：`scripts/ashare_price_fetch_csi300.py:28-33`
- 影响范围：仅该脚本的可维护性。
- 修复建议：在 ashare_price.py 暴露公开的 `query_codes(codes, start, end, adjustflag, on_error=...)` 钩子，脚本只调公共 API。
- 修复成本：低
- 状态：未修复

### P3-3 整体重拉不可续传（已声明的取舍）
- 描述：docstring 明示 "NOT incremental/resumable — simplicity over partial-restart comfort"；949 ticker × 2s ≈ 32 分钟起步，中途崩溃全弃。作为一次性 S0 快照可接受，取舍已文档化。
- 位置：`scripts/ashare_price_fetch_csi300.py:15-16`
- 影响范围：重建成本（非正确性）。
- 修复建议：如后续需要月度增量（forward 阶段），另写增量 fetcher 挂 last_poll_ts 模式；本脚本保持现状可。
- 修复成本：中
- 状态：未修复（有意为之，文档化）

## 总体评分
8/10 — 干净的一次性真实拉取执行：隔离/礼貌/快照留痕/覆盖核验四项到位且把适配器层遗留的批脆弱性在脚本层补齐；扣分在覆盖断言未代码化、私有 API 耦合与退出码语义。
