# Commit bab3e18aeeaa2fe012f9d7412a9da70fb95c83c4 审查报告

**提交信息**: fix(web): mark Trump (+Biden) inaugurations as pivotal market events（2026-08-09，Rethymus）
**改动范围**: 3 个文件，+39/-0；`export_terminal_data.py` 策展事件表 +5 条（3 就职 + 2 关税）、`market-view.tsx` 新增 `inauguration` 事件类型的 emerald 样式与中文标签、`market_context.json` 再生（+30 行）。

## 四维度结论
| 维度 | 结论 | 一句话说明 |
|---|---|---|
| 稳定性 | ✅ | 纯数据/样式追加，未知 type 有 `?? ""`/`?? e.type` 回落 |
| 可扩展性 | ⚠️ | 事件类型→样式/标签映射仍是组件内硬编码字典 |
| 生产可用 | ✅ | 事件日期公开可验证，display-only 不触研究层 |
| 高内聚低耦合 | ✅ | export 脚本为事件唯一来源，前端只渲染 |

## 对照参考
不适用（策展展示数据）。事件日期抽查（2017-01-20/2021-01-20/2025-01-20 就职、2025-02-01 关税令、2025-04-02 对等关税）均与公开事实一致。

## 问题清单
### P3-1 事件类型标签绕过 i18n
- 描述：`EVENT_TYPE_LABEL` 为组件内硬编码中文映射（政治/贸易/危机/货币/就职），未走 `useI18n` 的 zh/en 字典——英文 locale 下徽章仍显示中文。本 commit 沿用了既有模式（前 4 类同样硬编码），非新引入的缺陷类别。
- 位置：`web/src/components/market/market-view.tsx:33-39`
- 影响范围：英文界面的事件类型徽章文案。
- 修复建议：迁移到 `dict.ts` 的 `market.eventtype.*` 键（zh/en 双份）。
- 修复成本：低
- 状态：未修复（后续 06834a44/bff871dd 等 commit 重构该面板时未见此问题修复记录，待人工复核）

## 总体评分
8/10 — 正确的展示层数据修复；扣分在顺手加固了硬编码 i18n 缺陷而非修正它。
