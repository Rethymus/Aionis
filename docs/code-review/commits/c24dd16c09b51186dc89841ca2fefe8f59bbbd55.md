# Commit c24dd16c09b51186dc89841ca2fefe8f59bbbd55 审查报告

**提交信息**: chore(data): daily terminal refresh（2026-08-11，Rethymus）
**改动范围**: 5 个文件，+1085/-3003；仅 `web/src/data/aionis/` 下的 JSON 数据导出（cot/form4/macro_drivers/reddit/taco）

## 跳过原因
纯机器生成的每日数据再生成 commit：只改 `web/src/data/aionis/*.json`（CI daily refresh 产物），无任何源码/配置/CI 改动，符合审查规程的跳过判定。macro_drivers.json 的大幅缩减（-2894 行）系宏观驱动面板重构后导出器只输出新系列（如 dxy），属数据内容变化而非 schema 破坏（status:"ok" 结构保持）。

## 总体评分
不适用 — skipped（纯数据刷新）
