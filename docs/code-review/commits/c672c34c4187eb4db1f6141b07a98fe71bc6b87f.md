# Commit c672c34c4187eb4db1f6141b07a98fe71bc6b87f 审查报告

**提交信息**: feat(track-b): expose monthly portfolio returns (top-q + equal-weight) for eval-layer mounts（2026-08-03，Rethymus）
**改动范围**: 2 个文件，+19/-3；`TrackBBaselineResult` 加法新增 `monthly_dates/monthly_model_returns/monthly_ew_returns` 三字段（与 DM 同一循环产出，不重复造数），a-run 导出进 site JSON 供 tearsheet/净成本/FF5 挂载消费。

## 四维度结论
| 维度 | 结论 | 一句话说明 |
|---|---|---|
| 稳定性 | ✅ | 空面板/组合数<2 分支下列表保持空且与 DM NaN 一致；NaN return 已在构造前 dropna |
| 可扩展性 | ⚠️ | 三条平行列表（dates/model/ew）而非结构化对象，消费方需 zip 对齐 |
| 生产可用 | ✅ | 加法暴露既有中间量，无新计算路径、无泄漏面（OOS 组合收益单向流出） |
| 高内聚低耦合 | ✅ | 评估层挂载点从 DM 内部变量升为一等结果字段，单一构造多消费 |

## 对照参考
不适用（结果容器的加法字段暴露，无新算法逻辑；组合构造沿用 d620e996 已审实现）。

## 问题清单
### P3-1 平行列表而非按月对齐的结构
- 描述：`monthly_dates: list[str]` + `monthly_model_returns: list[float]` + `monthly_ew_returns: list[float]` 三条等长列表靠位置隐式对齐；且月份因 `len(group) < 5` 被跳过时与 `ic_series` 的月份集合可能不一致，消费方（tearsheet/FF5）需自行保证 zip 正确。
- 位置：`src/aionis/eval/track_b_baseline.py:102-106`
- 影响范围：下游挂载（tearsheet_adapter、mount-runner）的实现脆弱性。
- 修复建议：单层 `dict[str, {"model": float, "ew": float}]` 或 DataFrame。
- 修复成本：低
- 状态：未修复

## 总体评分
8/10 — 干净的加法暴露：复用 DM 循环、空值分支一致、为评估层挂载铺路；仅数据形状选择偏脆。
