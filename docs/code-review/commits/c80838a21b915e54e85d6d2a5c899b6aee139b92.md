# Commit c80838a21b915e54e85d6d2a5c899b6aee139b92 审查报告

**提交信息**: feat(web): fintech data terminal (Next.js + shadcn) — xiaoyinsi-style main site（2026-08-06，Rethymus）
**改动范围**: 134 个文件，+18111/-19；克隆 MIT 脚手架 abderrahimghazali/shadcn-fintech（Next.js 16 + shadcn/ui + Tailwind v4 + recharts），落地 `web/`（5 条 Aionis 路由 + 5 个自研视图 + i18n + tracked data JSON），新增 `scripts/export_terminal_data.py`，CI 由 Quarto 改为 pnpm+Next 静态导出，并修复 08af75db 的 NaN JSON 缺陷（export_quarto_data.py +6/-3）。

**覆盖范围说明**（超大 diff）：逐行审查了全部自研代码——`scripts/export_terminal_data.py`（全文）、`scripts/export_quarto_data.py` 修复段、`web/src/app/**`（layout/page/loading/not-found）、5 个 Aionis 视图（overview/picks/evidence/power-floor/discipline）、`web/src/i18n/{provider,dict}.tsx|ts`、`web/src/data/aionis/index.ts`、app-sidebar/nav、`next.config.ts`/`package.json`/`tsconfig.json`/`pnpm-workspace.yaml`/`.gitignore`、deploy-pages.yml；模板组件（`web/src/components/{ui,accounts,budgets,cards,crypto,dashboard,investments,notifications,settings,support,transactions,transfers,analytics}/`、globe-demo、empty-state、seed.ts、globe.json）为脚手架代码，抽查未逐行审（其中绝大多数本就不可达，见 P2-1）；数据 JSON 抽验（metrics/picks/ic_monthly 无 NaN、与 ledger #49 一致）。

## 四维度结论
| 维度 | 结论 | 一句话说明 |
|---|---|---|
| 稳定性 | ⚠️ | 自研代码边界处理尚可（rank_change None、Change 组件 null 分支），但 CI 提交即坏（缺 lockfile） |
| 可扩展性 | ⚠️ | ~5,900 行不可达模板组件+mock seed 随车入库；i18n dict 集中可扩展 |
| 生产可用 | ⚠️ | 静态导出配置正确（basePath=/Aionis、unoptimized）；但「14 条预注册配置」过强表述进入公开站点 |
| 高内聚低耦合 | ✅ | 展示层只吃 tracked 聚合 JSON，研究管线零接触；导出器复用 export_quarto_data |

## 对照参考
对照 Next.js 官方静态导出文档（`output: "export"` + basePath）与 pnpm 官方 CI 指南（`--frozen-lockfile` 要求提交 pnpm-lock.yaml、`cache-dependency-path` 指向实际锁文件）：本 commit 的 next.config 正确，但 CI 违反 pnpm 前提（无锁文件先冻装）。脚手架 license 经检索确认为 MIT（github.com/abderrahimghazali/shadcn-fintech），符合项目许可白名单。出处：nextjs.org、pnpm.io、github.com。

## 问题清单
### P1-1 CI 部署管线提交即坏：frozen-lockfile 安装但未提交 pnpm-lock.yaml
- 描述：workflow 配置 `cache-dependency-path: web/pnpm-lock.yaml` + `pnpm install --frozen-lockfile`，但本 commit 的 `web/` 树中不存在 `pnpm-lock.yaml`（`git ls-tree` 验证为空）。pnpm 的 headless/frozen 安装在无锁文件时直接报错，push 到 main 后 deploy-pages 工作流失败，站点无法部署。commit message 声称「Verified: local pnpm build exit 0」仅覆盖本地（本地已有 node_modules/lock），未验证 CI 路径。
- 位置：`.github/workflows/deploy-pages.yml:34-40`（该 commit 版本）
- 影响范围：GitHub Pages 公开站点部署（主对外门面）。
- 修复建议：提交 `web/pnpm-lock.yaml`。
- 修复成本：低
- 状态：已在 2a45f23b6eed6b9b4976d5458e2d20978451a28c 修复（次 commit 专门补 lockfile，其 message 自述「CI cache-dep + frozen-install were failing」）

### P2-1 约 5,900+ 行不可达模板组件与 mock 数据随车入库，与 commit message「Removed…unrelated personal-finance pages」不符
- 描述：commit message 称已移除脚手架 demo 资产与无关个人理财页面，但 `web/src/components/{accounts,budgets,cards,crypto,dashboard,investments,notifications,settings,support,transactions,transfers,analytics}/`（约 60 个组件）、`empty-state.tsx`(466 行)、`globe-demo.tsx`(403 行)、`web/src/data/seed.ts`(973 行 mock 个人理财数据)、`web/src/data/globe.json` 全部被提交；`git grep` 验证 0 个 app 路由引用它们（页面确实删了，组件/数据没删）——全部是死代码。mock 数据仅在 web 展示层（非研究管线），不触红线；脚手架 MIT 亦无许可问题；但误导性 message + 6k 行死重量 + mock 交易/转账组件长期滞留是明确的维护负债。
- 位置：`web/src/data/seed.ts`、`web/src/components/{accounts,crypto,...}/`（该 commit 版本）
- 影响范围：仓库体积、安全审计面（如 `virtual-card-generator`、`trade-form` 等演示组件）、读者对「已清理」的信任。
- 修复建议：删除全部不可达模板组件与 seed/globe 数据。
- 修复成本：低（批量删除）
- 状态：已在 40f3b9d4a（模板组件批量删除）+ 1e7dd2f（seed.ts 删除）分阶段修复

### P2-2 公开站点沿用「14 条预注册配置 / 14 pre-registered configs」过强表述
- 描述：`dict.ts` 中英文四处（`module.evidence.window`、`evidence.intro` 等）称 14 条均为预注册配置，但 evidence.json 自身分级含 CV-proxy/探索变体/基线（同 023dfe4a 报告 P2-2、08af75db 报告 P2-2 的延续），且本站点为主门面、随 CI 自动部署公开。
- 位置：`web/src/i18n/dict.ts:34,52,92,110`（该 commit 版本）
- 影响范围：公开站点主张强度与自家证据分级不一致。
- 修复建议：改为「14 条配置（4 预注册阶段 + 时序探索 + 1 confirmatory）」。
- 修复成本：低
- 状态：未修复（当前 main 的 dict.ts 仍含「14 条预注册配置」）

### P3-1 export_terminal_data.py 的脚本导入 hack 与边界缺失
- 描述：(a) `sys.path.insert(0, ...)` 后 `import export_quarto_data as eq` 并改写 `eq.OUT = WEB` 全局重定向——可运行但脆弱（依赖脚本目录在 sys.path、被导入方模块级 mkdir 副作用）；(b) `dates[-2]` 在面板只有一个月时 IndexError；(c) `export_metrics` 第三处硬编码 ledger #49 数字（与前两个站点同型的重复常量）；(d) `rank_change` 用 ticker 单键匹配，若 US/CN 出现同 ticker 会碰撞（当前 CN 均为 sh./sz. 前缀，实际无碰撞）。
- 位置：`scripts/export_terminal_data.py:21-26, 38-39, 69-83`
- 影响范围：维护性与极端边界。
- 修复建议：共享 payload 抽成 `src/aionis/reporting/` 下的函数；`dates[-2]` 前判长；metrics 从 summary JSON 读取。
- 修复成本：低
- 状态：未修复

### P3-2 杂项（命名/i18n/静态 PASS）
- 描述：(a) `web/package.json` name 仍为 `"shadcn-fintech"`；(b) `<html lang="zh-CN">` 硬编码，EN 切换不更新 lang（无障碍/SEO）；(c) discipline-view 六张纪律卡的 title/detail/PASS 全部英文字符串硬编码（未进 dict），与其余页面的双语覆盖不一致，且「PASS」徽章为静态声明、未链接到任何证据锚点。
- 位置：`web/package.json:2`、`web/src/app/layout.tsx:44`、`web/src/components/discipline/discipline-view.tsx:16-22,49`
- 影响范围：展示一致性。
- 修复建议：改名 Aionis-terminal；lang 跟随 i18n state；纪律卡文案入 dict 并附 ledger/ADR 链接。
- 修复成本：低
- 状态：未修复（部分项在后续 IA 重构中可能已变化，待人工复核）

## 领域红线核查
合规：(1) 防泄漏——web 为纯展示层，数据全部来自 tracked 聚合 JSON（research 输出），无实时价格、无研究管线反向依赖；(2) 密钥——无任何硬编码 secret；(3) 许可——脚手架 MIT、依赖（next/react/recharts/tailwind 等）均许可友好；(4) 本 commit 还顺带修复了 08af75db 的非法 JSON（ic_monthly NaN→null + allow_nan=False），新导出的 `web/src/data/aionis/ic_monthly.json` 实测 0 处 NaN；(5) 无 ledger/冻结面改动。亮点：picks/shorts 从 confirmatory OOS 分数导出，metrics 与 ledger #49 一致，站点全文以「null 是预期结果 · 非投资建议」为主轴，反泄漏叙事贯彻到门面。

## 总体评分
6/10 — Aionis 数据模块本身（5 视图 + 导出器 + i18n）设计干净、数字诚实可溯，NaN 修复及时；但主门面 CI 提交即坏（次日才修）、约 6k 行死代码与 mock 组件随车入库且 message 声称已清理、公开页面延续「预注册」过强表述，工程交付完成度明显低于本仓既有标准。
