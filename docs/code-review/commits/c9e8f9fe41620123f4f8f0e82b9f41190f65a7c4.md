# Commit c9e8f9fe41620123f4f8f0e82b9f41190f65a7c4 审查报告

**提交信息**: fix(worker): bump CN cache key to v2 — bypass stale East Money 24h cache（2026-08-08，Rethymus）
**改动范围**: 1 个文件，+1/-1；`workers/prices/src/worker.js` 的 CN 缓存 key 由 `/cn/${tickersParam}` 改为 `/cn/v2/${tickersParam}`，使部署 Sina 后端后不再命中东财时代写入的旧缓存条目。

## 四维度结论
| 维度 | 结论 | 一句话说明 |
|---|---|---|
| 稳定性 | ✅ | 最小且正确的热修复：key 变更 = 全量 miss = 立即回源 Sina |
| 可扩展性 | ⚠️ | 手工版本号救火而非机制化（key 模板未纳入后端标识，下次换源仍要靠人记得 bump） |
| 生产可用 | ✅ | 秒级生效、无副作用（旧条目自然过期淘汰） |
| 高内聚低耦合 | ✅ | 单行变更，无其他影响面 |

## 对照参考
对照 Cloudflare Cache API 官方文档（developers.cloudflare.com/workers/runtime-apis/cache/）：`cache.match` 仅按 key 匹配、无通配失效手段（Cache API 不支持 delete by prefix），故换数据源时「改 key 版本」确实是官方语境下的标准做法——与 CDN 层 cache-tag purge 思路同源（developers.cloudflare.com/cache/how-to/purge-cache/）。

## 问题清单
### P3-1 缓存 key 版本号靠手工维护，未机制化
- 描述：key 模板硬编码 `https://cache.local/cn/v2/${tickersParam}`，`v2` 与后端实现（Sina）之间无代码层关联；未来再换源或改解析逻辑时仍需人工记得 bump，重演本次陈旧缓存事故。
- 位置：`workers/prices/src/worker.js:95`（该 commit 版本）
- 影响范围：后续维护成本与同类事故复发概率。
- 修复建议：定义 `const CN_CACHE_VERSION = "sina-v1"` 常量并在后端切换/解析变更时一并更新，key 由常量拼装。
- 修复成本：低
- 状态：未修复（main 上仍是硬编码 `/cn/v2/` 字面量）

## 总体评分
9/10 — 教科书式的最小热修复，正确诊断根因（缓存 key 未随后端演进）并立即处置；唯一遗憾是修标未修本（版本号未常量化）。
