# Commit c9e9eb66463e7fbc94d2cab90e9dbcea9d75a845 审查报告

**提交信息**: docs(evals): register BASELINE-FF5-001 + BASELINE-RANK-001 trial-intent manifests (RD-17)（2026-08-03，Rethymus）
**改动范围**: 2 个新文件，+44；`evals/trials/BASELINE-{FF5,RANK}-001.json`（trial-intent-v1 manifest，RD-17 注册）。

## 四维度结论
| 维度 | 结论 | 一句话说明 |
|---|---|---|
| 稳定性 | ✅ | 纯 JSON 注册工件，pydantic schema 校验（trial-intent-v1） |
| 可扩展性 | ✅ | manifest 字段（family/parent/multiplicity/status 迁移）符合 RD-17 设计 |
| 生产可用 | ⚠️ | 注册时序违背 RD-17 自述的「BEFORE observing results」（P2-1）；状态停留在 planned 与现实不符（P3-1） |
| 高内聚低耦合 | ✅ | 明示「不替代 ledger」；manifest 无任何观测指标（fail-closed 字段禁令被遵守） |

## 对照参考
RD-17 schema（`src/aionis/reporting/trial_intent.py`）docstring 明确「records trial intent BEFORE observing results」「prevents silent trial expansion」。本 commit 是**事后**注册（created_at 12:52:31Z，晚于 104b21e 的 ledger 行 12:37-12:44Z 与结果观察）。与临床试验预注册规范（如 ICMJE/WHO 预注册要求注册先于入组结束）对照，事后注册削弱 selection-honesty 保障，但 commit message 以「Post-hoc registration」如实披露。不适用外部开源对照（治理工件）。

## 问题清单
### P2-1 RD-17 注册晚于结果观察，违背 schema 自述的事前注册语义
- 描述：manifest `created_at: 2026-08-03T12:52:31Z`，而两个 trial 的 OOS 指标已在 104b21e（12:49Z commit，含 RANK p_hac=0.0421）被观察。RD-17 意图注册的多重计数/选择诚实锚在「已知结果后」才落盘——若结果为 null 是否仍会注册无从验证。缓解：config_committed ledger（第一防泄漏锚）确实先于结果；manifest 按 schema 拒绝携带任何观测指标。
- 位置：`evals/trials/BASELINE-RANK-001.json:19-20`（created_at，该 commit 版本）
- 影响范围：RD-17 注册表的选择诚实性（multiplicity 账本的事前性）；不影响已冻结结果本身。
- 修复建议：流程上把 manifest 注册并入 owner 授权闸门（res runner 的 `_require_owner_commit` 可同时要求对应 manifest 已存在且 status≥APPROVED）。
- 修复成本：低
- 状态：未修复（main 上两份 manifest 无后续改动）

### P3-1 manifest 状态与现实脱节且无后续迁移
- 描述：两份 manifest 记录 `"status": "planned"`、`"n_completed": 0`，但两 trial 均已执行并产生结果（104b21e）；schema 设计的状态机 PLANNED→APPROVED→ACTIVE→CLOSED 未推进，multiplicity 计数失真（n_trials=2, n_completed=0）。main 至今未更新。
- 位置：`evals/trials/BASELINE-FF5-001.json:15-19`、`evals/trials/BASELINE-RANK-001.json:15-19`（该 commit 版本）
- 影响范围：下游多重检验/家族计数若读该注册表将低估已完成 trial 数。
- 修复建议：执行批次后补一次状态迁移（ACTIVE→CLOSED、n_completed=2），并注明迁移时间。
- 修复成本：低
- 状态：未修复

## 亮点
- manifest 内容本身准确：validation_kind=purged_cross_fit（与 runner 的 PurgedGroupKFold 一致，未虚标 chronological）、estimand=rank_ic、planned_config_sha256 与 ledger 行 sig 逐字一致（0b0b9934…/22817980…）。
- 严格遵守「无观测指标」字段禁令，杜绝 manifest 变成第二个结果账本。

## 总体评分
7/10 — manifest 内容与 ledger 逐字对齐、字段纪律严明，但事后注册（P2）+ 状态未迁移（P3）削弱了 RD-17 作为事前选择诚实锚的设计意图。
