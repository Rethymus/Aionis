# Commit ce036449d0edd4390921d4c3d36298e4d86b7a99 审查报告

**提交信息**: chore: initialize Aionis git repo + commit conventions（2026-07-27，Rethymus）
**改动范围**: 79 个文件，+13626/-0；仓库初始化——完整 src/aionis 骨架（ingest/features/eval/extraction/reporting/schema/models）、scripts、tests（19 个测试文件）、pyproject/uv.lock、docs 预注册、runs/ledger.jsonl（25 行历史）。

## 四维度结论
| 维度 | 结论 | 一句话说明 |
|---|---|---|
| 稳定性 | ✅ | 抓取路径有重试/退避/空帧校验；CV 委托 purgedcv（违反即抛错）；无静默吞错关键路径 |
| 可扩展性 | ✅ | 配置集中在 config.py（pydantic-settings），provider 目录式扩展，CVSplit 适配层隔离第三方 |
| 生产可用 | ⚠️ | 抓取间隔低于后来绑定的 ≥2s 红线（Tiingo/Alpaca 1s、yfinance 0.4s、SEC 0.15s）；SEC UA 联系邮箱为占位符 |
| 高内聚低耦合 | ✅ | ingest/features/eval 分层清晰；learner 冻结参数单点；two_arm 显式共享 fold 隔离差分变量 |

## 对照参考
purge/embargo CV 委托 `purgedcv` 轮子并在适配层保留 CVSplit 形状，与 AFML ch.7/12 参考实现一致；LightGBM 确定性显式 pin 全部 seed + n_jobs=1，符合 lightgbm 官方确定性建议（LightGBM 官方文档 Deterministic 部分）；requests 抓取带 per-symbol 循环 + 线性退避，模式与 urllib3 Retry/tenacity 的 bounded-retry 实践一致（仅间隔数值偏低）。出处：lightgbm.readthedocs.io、urllib3.readthedocs.io。

## 问题清单
### P2-1 数据抓取礼貌间隔低于 ≥2s 项目红线
- 描述：`_from_tiingo`/`_from_alpaca` 中 `time.sleep(1.0)`（注释自称 "polite: 1s between symbols"），`_from_yfinance` 中 `time.sleep(0.4)`，`fundamentals.py` SEC 请求间隔 0.15s。按本项目现行绑定规则（AGENTS.md：SEC/EDGAR/FRED/Tiingo/Alpaca ≥2s + 指数退避）均不达标；yfinance/Stooq 也在后来被列为 blocked 源但仍作为 fallback 保留在本提交。
- 位置：`src/aionis/ingest/market.py:67`（`time.sleep(1.0)`）、`src/aionis/ingest/market.py:105`、`src/aionis/ingest/market.py:170`（`time.sleep(0.4)`）、`src/aionis/ingest/fundamentals.py:90`
- 影响范围：所有走真实抓取的 ingest 路径；存在被数据源限流/封禁的风险
- 修复建议：统一改为 host 级 ≥2s 间隔 + 真指数退避，并移除 blocked 源
- 修复成本：低
- 状态：已在 e8545d4 修复（AUD-05A 引入 `ingest/http_policy.py` host-scoped ≥2s；AUD-04 移除 yfinance/Stooq）

### P3-1 SEC User-Agent 联系邮箱为占位符
- 描述：`_UA = "Aionis research fundamentals contact@example.com"` —— SEC fair-access 政策要求 UA 带真实联系方式，example.com 占位符在 SEC 收紧校验时会 403。
- 位置：`src/aionis/ingest/fundamentals.py:27`
- 影响范围：EDGAR fundamentals 抓取（cik_map + company_facts）
- 修复建议：替换为维护者真实邮箱
- 修复成本：低
- 状态：未修复（main 中仍为 contact@example.com）

### P3-2 phase_b_run.py 账本/哈希使用 CWD 相对路径
- 描述：`_append` 写死 `open("runs/ledger.jsonl", ...)`、`_sha(Path("uv.lock"))` 均依赖进程 CWD；从非仓库根运行会写到错误位置或哈希空串（`_sha` 对不存在文件返回 `""`，config_sig 仍会生成，静默失去 uv.lock 锚定）。
- 位置：`scripts/phase_b_run.py:56-58`、`scripts/phase_b_run.py:81`
- 影响范围：确认性运行的账本纪律（config 完整性锚）
- 修复建议：以仓库根（`Path(__file__).resolve().parents[1]`）或 settings.runs_dir 拼绝对路径；`_sha` 对预期存在但缺失的文件应抛错而非返回空串
- 修复成本：低
- 状态：待人工复核（后续脚本沿用该模式；AGENTS.md 命令均假定仓库根执行）

## 总体评分
9/10 — 骨架质量极高：账本先于结果、共享 fold 双臂设计、PIT 宇宙、确定性 pin 一应俱全；仅礼貌间隔与占位邮箱等边角瑕疵。
