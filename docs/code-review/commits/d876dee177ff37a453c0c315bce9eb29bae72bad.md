# Commit d876dee177ff37a453c0c315bce9eb29bae72bad 审查报告

**提交信息**: feat(eval): FF5 residual + Amihud utility (track_b mount 3)（2026-08-02，Rethymus）
**改动范围**: 2 个文件，+669/-0；新增 `src/aionis/eval/ff5_residual.py`（FF5 残差回归 + Amihud 流动性 + FF5 数据加载）与 359 行 hermetic 测试（18 用例）。探索性工具，未接管线、不写 ledger。

## 四维度结论
| 维度 | 结论 | 一句话说明 |
|---|---|---|
| 稳定性 | ⚠️ | 回归与 Amihud 有校验/不足样本 NaN 路径；但 `load_ff5` 主路径数据集名错误（P2）、一处错误消息打印布尔值（P3） |
| 可扩展性 | ✅ | 参数化 maxlag/window，模块自包含，无管线耦合 |
| 生产可用 | ✅ | FF5 无 vintage 的轻微前视风险在 docstring 显式披露并限定 exploratory-only，符合 7-gate 的诚实披露要求 |
| 高内聚低耦合 | ✅ | statsmodels/pandas 复用而非手搓回归；网络加载独立函数便于隔离测试 |

## 对照参考
pandas-datareader 官方文档与源码（pandas-datareader.readthedocs.io/readers/famafrench.html、github.com/pydata/pandas-datareader）：famafrench reader 的合法数据集名是 Kenneth French 库文件名 `F-F_Research_Data_5_Factors_2x3`（monthly），本 commit 用的 `"Fama_French_5_Factors"` 不在库中（次日 74abe38 的 commit message「fallback when pandas-datareader fails」即为实锤——主路径真实运行即失败）。Amihud 公式与 Amihud (2002, JFM) 一致；NW 滞后 `4*(n/100)^(2/9)` 与 Newey & West (1994) 一致。

## 问题清单
### P2-1 `load_ff5` 数据集名非法——主路径从未成功过
- 描述：`web.DataReader("Fama_French_5_Factors", "famafrench", ...)` —— famafrench reader 按 `{name}_CSV.zip` 拼 URL 下载 Kenneth French 库文件，库中不存在 `Fama_French_5_Factors_CSV.zip`，合法名为 `F-F_Research_Data_5_Factors_2x3`。测试 hermetic 跳过网络，此缺陷零覆盖；次日 74abe38 加 bulk-ZIP fallback 兜底（fallback "when pandas-datareader fails"）反证主路径失败。
- 位置：`src/aionis/eval/ff5_residual.py:298`
- 影响范围：任何调用 `load_ff5` 的路径（当时无；后续 mount 运行时由 fallback 补救）。
- 修复建议：把数据集名改为 `"F-F_Research_Data_5_Factors_2x3"`（fallback 保留）。
- 修复成本：低
- 状态：已在 74abe38 修复（以 fallback 绕过；主路径名称至今未改，属部分修复）

### P3-1 校验错误消息打印布尔值而非缺失列集合
- 描述：`raise ValueError(f"volume missing columns: {_VOLUME_REQUIRED.issubset(volume.columns)}")` —— 在 f-string 里调用了 `issubset`，输出 `True/False`（恒为 False，因为仅在非子集时触发），应写 `_VOLUME_REQUIRED - set(volume.columns)`。
- 位置：`src/aionis/eval/ff5_residual.py:235`
- 影响范围：Amihud 输入缺列时的排障体验。
- 修复建议：改为差集表达式。
- 修复成本：低
- 状态：未修复（main:235 原样）

### P3-2 无效的自替换死代码
- 描述：`df.columns = [col.replace("Mkt-RF", "Mkt-RF") for col in df.columns]` —— 用自身替换自身，无任何作用，疑为未写完的重命名残留。
- 位置：`src/aionis/eval/ff5_residual.py:378`
- 影响范围：可读性。
- 修复建议：删除该行。
- 修复成本：低
- 状态：未修复（main:378 原样）

### P3-3 Amihud docstring 与 `min_periods=1` 行为不符
- 描述：docstring 称「Rows with insufficient data are dropped」，实现 `rolling(window, min_periods=1)` 使窗口不满的行产出部分窗口均值并保留，仅首个 NaN 被丢——窗口初期值系统性偏噪且与文档承诺相反。
- 位置：`src/aionis/eval/ff5_residual.py:253-255`
- 影响范围：Amihud 序列前 window-1 期的可用性与解释。
- 修复建议：`min_periods=window`（或修正文档）。
- 修复成本：低
- 状态：未修复

## 总体评分
7/10 — 回归/Amihud 主体实现干净、测试扎实、泄漏披露诚实；扣分在网络加载函数未经真实运行验证（数据集名错误）与三处低危残留。
