# Commit e07cab0fc93d3d206943ac349eb7aeec6d50ca0c 审查报告

**提交信息**: feat(dashboard): strategy L-S equity-curve (curve-evolution view)（2026-07-30，Rethymus）
**改动范围**: 1 个文件，+57/-0；dashboard/app.py 新增 Strategy Return tab 的累积多空权益曲线（缓存 parquet 读取 + cumsum 折线）。

## 四维度结论
| 维度 | 结论 | 一句话说明 |
|---|---|---|
| 稳定性 | ✅ | 工件缺失优雅降级（None/empty → st.info），`row` 有早退保护，空列 continue |
| 可扩展性 | ⚠️ | benchmark 列名在图内硬编码 "arm_base"，caption 却动态取 ledger 行（见 P3-1） |
| 生产可用 | ✅ | `st.cache_data` 缓存读取；明确标注 secondary/exploratory、gross-of-costs，不冒充 confirmatory |
| 高内聚低耦合 | ✅ | 纯展示层读 runs/ 工件，通过 R.results_dir() 复用路径约定，不触研究管线 |

## 对照参考
L-S 价差曲线用逐月收益 cumsum（算术累积）是 dollar-neutral 组合的常见展示约定；empyrical（QuantConnect，Apache-2.0）的 `cum_returns` 用 cumprod 复利口径，两者差异已在 caption 用「cumulative sum … gross-of-costs」明示，口径诚实。plotly 折线 + 虚线基准的做法与 quantstats tearsheet 的 benchmark 叠加一致（github.com/ranaroussi/quantstats）。

## 问题清单
### P3-1 基准列名硬编码与 caption 动态取值不一致
- 描述：`_equity_curve_chart` 内 `bench = "arm_base"` 硬编码决定虚线灰线样式，而 caption 用 `row.get('benchmark', 'arm_base')` 从 ledger 行动态取名。若未来 ledger 行的 benchmark 字段与 parquet 列名不同步，虚线标注与文字说明会静默不一致。
- 位置：`dashboard/app.py:349`（`bench = "arm_base"`）与 `dashboard/app.py:677-680`（caption）
- 影响范围：仅展示一致性；当前列名集合固定（B_arm_state/arm_base/…）暂不触发。
- 修复建议：把 bench 列名作参数从调用方传入，单一来源。
- 修复成本：低
- 状态：未修复（后续 7dede9b 面板重写后该函数仍在，硬编码保留）

## 总体评分
9/10 — 小而干净的展示层增量，降级路径与口径披露都到位，仅一处基准名双源的小瑕疵。
