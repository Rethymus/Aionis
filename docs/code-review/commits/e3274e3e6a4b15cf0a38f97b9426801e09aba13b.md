# Commit e3274e3e6a4b15cf0a38f97b9426801e09aba13b 审查报告

**提交信息**: fix(web): dev basePath 404 + empty taco chart（2026-08-09，Rethymus）
**改动范围**: 2 个文件，+7/-2；`web/next.config.ts` basePath 改为仅生产启用（dev 去 /Aionis 前缀），`taco-view.tsx` XAxis dataKey `date`→`month`。

## 四维度结论
| 维度 | 结论 | 一句话说明 |
|---|---|---|
| 稳定性 | ✅ | 两处均为确定性 UI 缺陷修复，根因清晰 |
| 可扩展性 | ✅ | isProd 判断集中 next.config，附注释说明双环境意图 |
| 生产可用 | ✅ | 生产导出保留 /Aionis basePath（next build 验证 prerender） |
| 高内聚低耦合 | ✅ | 不触数据/研究层 |

## 对照参考
`basePath` 条件化是 Next.js 官方支持的配置（nextjs.org/docs/app/api-reference/next-config-js/basePath），按 NODE_ENV 切换 dev/prod 前缀属社区通行做法；GitHub Pages 子路径部署模式同官方 learn 教程。

## 问题清单
通过。修复与 taco.json 实际 schema（`{month, vix}`）核对一致；`dataKey="vix"` 本就正确，仅 category 轴键错。

## 总体评分
9/10 — 小而准的双缺陷修复，验证方式（recharts path 计数）扎实。
