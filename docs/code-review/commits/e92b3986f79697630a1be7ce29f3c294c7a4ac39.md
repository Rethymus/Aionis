# Commit e92b3986f79697630a1be7ce29f3c294c7a4ac39 审查报告

**提交信息**: feat(track-c): first confirmatory OOS climax — ledger #49 (IC -0.0088, J-T look-1 NOT_EQUIVALENT)（2026-08-05，Rethymus）
**改动范围**: 9 个文件，+1537/-14；核心为 `scripts/track_c_confirmatory_run.py`（+492，confirmatory runner：冻结 #48 sig 校验→双重 H6 bit-identical 断言→J-T 等效门→账本追加）与 `tests/test_track_c_confirmatory_run.py`（+299，19 项 hermetic 测试）；`runs/ledger.jsonl`（+1 行 #49 `confirmatory:first`，append-only）；其余为文档（RESULTS.md、方法学 draft v1.0-draft、2 份自审报告、state/current+handoff）。

## 四维度结论
| 维度 | 结论 | 一句话说明 |
|---|---|---|
| 稳定性 | ✅ | 冻结 sig 不匹配即 SystemExit（观察 OOS 之前）、缺输入文件 fail-fast、J-T look 对空/短/NaN 序列均有护栏且有测试 |
| 可扩展性 | ⚠️ | looks/SESOI/alpha 冻结常量正确外部化为模块常量；`H6_deterministic:True` 与 `41` 字面量写死（P3-1） |
| 生产可用 | ⚠️ | 账本时序纪律成立（#48 config_committed 04:19 UTC → asym41 探索性观察 05:48 → confirmatory 沉淀 10:27，prior-exploratory 三处如实披露，无 rerun-to-significance）；但 from-artifact GO 路径的内容完整性校验薄弱（P2-1） |
| 高内聚低耦合 | ✅ | runner 只做编排，估计器/J-T 门/IC 汇总全部复用已测模块（track_c_joint、sesoi_gate、rank_ic）；dry-run 默认无账本副作用 + owner 双环境变量门 |

## 对照参考
J-T/OBF 三看 group-sequential 等效设计与开源标准实现同构：rpact 的 O'Brien-Fleming α-spending 边界示例（rpact.org/vignettes）与 gsDesign 的 spending function 文档（keaven.github.io/gsDesign）均按"每个 look 用其计划信息量上的截断数据 + 边界 z_k"实现——本 runner 的 `jt_reachable_looks` 严格消费 `look_summary`（已验证 `iloc[:n_obs]` 截断到前 60 个月，look-2/3 正确置 PENDING 而非偷看），与 rpact/gsDesign 的 look 纪律一致。"先验证后提交"的两阶段 artifact-reuse 模式与可复现构建的 verify-then-trust 原则方向一致，但见 P2-1 的落差。

## 问题清单
### P2-1 from-artifact GO 路径把账本内容完整性降级为"信任文件自声明"
- 描述：`_commit_from_artifact`（:339-366）从磁盘读取 `track_c_confirmatory_summary.json` 直接追加进 append-only 账本，校验仅两项：`row.get("config_sig") != FROZEN_SIG_48` 与 `row.get("H6_deterministic")`（:355-363）——二者都是 JSON 文件内的自声明字段，篡改 IC 数值或 verdict 后保留 sig 字符串与布尔位即可入账。测试 `test_commit_from_artifact_appends_ledger` 实际演示了任意伪造 summary（helper `_write_summary` 手写假行）可通过全部校验并落账。缓解因素真实存在：owner 双环境变量门（FROM_ARTIFACT 必须伴随 GO）、账本 git 追踪可 diff、H6 确定性保证第三方可 dry-run 复算比对、`independence_caveat` 已披露——但脚本本身不做内容级验证，"verify" 退化为 "trust"。
- 位置：`scripts/track_c_confirmatory_run.py:339-366`
- 影响范围：`runs/ledger.jsonl` #49 及未来所有 artifact-reuse 路径的 confirmatory 行可信度（审计核心）
- 修复建议：append 前从同目录 `track_c_confirmatory_ic_series.parquet` 重算 `rank_ic_summary` 并与 `row["combined_ic"]` 数值比对（不一致即 SystemExit）；或 dry-run 结束时输出产物 sha256 清单，GO 路径强制比对
- 修复成本：中
- 状态：未修复（该脚本在 main 上此后无改动）

### P3-1 账本行的两个硬编码字面量
- 描述：`"H6_deterministic": True`（:239）在 `build_confirmatory_row` 内无条件写死——是声明而非证明（证明在调用方的 assert_h6_identical，但行构造与断言无参数耦合，重构后可能失真）；`"feature_cols_per_region": 41`（:240）为字面量，而相邻 `"feature_cols_union_unique": len(FEATURE_COLS_41)` 是计算值——特征清单一旦演化，41 会静默失真且与 31 并排自相矛盾。
- 位置：`scripts/track_c_confirmatory_run.py:239-240`
- 影响范围：未来 confirmatory 行的元数据准确性
- 修复建议：`h6_passed: bool` 作为 `build_confirmatory_row` 参数；41 由 `len(US_23)+len(CN_12)+len(MACRO_HEADLINE_6)` 推导
- 修复成本：低
- 状态：未修复

（另注：账本时序核查——#48 `config_committed` ts=2026-08-05T04:19:50Z，#49 `confirmatory:first` ts=10:27:13Z，且 #49 内 `prior_exploratory` 明示 asym41 探索性观察（13:48 本地=05:48Z）介于两者之间并给出"config 由 D1-D5 spec 决策而非 IC 优化选定"的辩护与 H6 复现保证——账本纪律与披露义务均满足，不计问题。`_ledger_row_count` 的并发竞态在单用户研究仓库语境下不构成风险，不计。）

## 总体评分
8/10 — 防泄漏编排堪称范本（冻结校验先行、双跑 H6、look 纪律、诚实披露），扣分在 from-artifact 提交路径的文件自声明式校验与少量硬编码。
