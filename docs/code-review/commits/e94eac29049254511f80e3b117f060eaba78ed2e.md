# Commit e94eac29049254511f80e3b117f060eaba78ed2e 审查报告

**提交信息**: feat(site): add Track C confirmatory climax section to static page（2026-08-05，Rethymus）
**改动范围**: 2 个文件，+79/-1；`scripts/build_static_site.py` 新增 `TRACK_C_CLIMAX` 常量 + `_trackc_section_html()`（KPI tiles + 表格 + 诚实 reframing 段 + 导航链接 + 横幅措辞更新），`tests/test_static_site.py` 新增 3 个 hermetic 测试。

## 四维度结论
| 维度 | 结论 | 一句话说明 |
|---|---|---|
| 稳定性 | ✅ | 纯静态生成、仅模块常量，无 random/time，确定性重建 |
| 可扩展性 | ⚠️ | 数字集中为 `TRACK_C_CLIMAX` 常量，但个别数字仍在字符串中重复硬编码 |
| 生产可用 | ✅ | 与 README/draft/RESULTS/ledger #49 数字一致，诚实披露欠功率 |
| 高内聚低耦合 | ✅ | 复用 `_kpi_tile`/section-card 模式，纯增量 section |

## 对照参考
对照 Next.js/shadcn 与 Streamlit 官方对「展示数字单一来源」的实践（组件从 data props 取值，不在模板里重复字面量）：本脚本把 p_hac=0.484 同时放在常量表与 KPI tile 字符串两处，属模板内重复字面量。出处：nextjs.org/learn。

## 问题清单
### P3-1 KPI tile 副标题硬编码 p_hac=0.484，未引用 `TRACK_C_CLIMAX` 常量
- 描述：`_kpi_tile("combined rank-IC", ic_str, "null · p_hac=0.484", "amber")` 中 `0.484` 为字符串字面量，而表格行用 `{c['p_hac']:.3f}`；若常量更新，副标题与表格将静默不一致。
- 位置：`scripts/build_static_site.py:226`
- 影响范围：仅展示层一致性风险。
- 修复建议：副标题改为 f-string 引用 `c['p_hac']:.3f`。
- 修复成本：低
- 状态：未修复（后续 c10993b 归档了发布线，该文件退出演进）

### P3-2 测试无法区分「等价已宣告」的否定语境
- 描述：`test_trackc_honest_reframing` 断言 `"等价已宣告" in generated_html`，但该短语仅出现在否定结构 非“等价已宣告” 中；若未来有人误写为正面宣告，该测试仍通过。注释已自认此局限。
- 位置：`tests/test_static_site.py:151-153`
- 影响范围：展示诚实性回归防护强度。
- 修复建议：断言否定语境本身，如 `非“等价已宣告”` 或 `"<strong>非</strong>“等价已宣告”"` 的完整片段。
- 修复成本：低
- 状态：未修复

### P3-3 「68 折」表述含义不明
- 描述：KPI tile「IC 月数 71 · 副标题 `68 折 · 2016-01..2026-08`」——「折」应为 walk-forward fold 数，但与主指标「71 月」并列且无解释，读者易误读（“68 折”在中文里更像折扣）。
- 位置：`scripts/build_static_site.py:229`
- 影响范围：展示层可读性。
- 修复建议：改为「68 个 walk-forward 折」或删去。
- 修复成本：低
- 状态：未修复

## 总体评分
8/10 — 干净的增量展示 section，数字与账本一致、诚实 framing 到位、测试同步新增；扣分在字面量重复与否定语境测试的弱防护（均为 P3 展示层问题）。
