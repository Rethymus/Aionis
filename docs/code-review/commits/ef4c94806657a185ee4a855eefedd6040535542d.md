# Commit ef4c94806657a185ee4a855eefedd6040535542d 审查报告

**提交信息**: fix(phase-d): run_confirmatory always saves artifacts (base=None->default)（2026-07-29，Rethymus）
**改动范围**: 1 个文件，+7/-6；`src/aionis/eval/phase_d.py` 移除 `if results_base is not None` 守卫，使 `save_run(base=None)` 始终落盘到默认 `runs/results`。

## 四维度结论
| 维度 | 结论 | 一句话说明 |
|---|---|---|
| 稳定性 | ✅ | 修复正确：`save_run(base=None)` → `results_dir(None)` → `DEFAULT_RESULTS_DIR`（results.py:56-63 已核验），hermetic 测试不受影响 |
| 可扩展性 | ⚠️ | 同一 save-skip bug 已是第二次出现（Phase C 一次、本次 Phase D），且紧接着在 Phase E1 第三次复发（031e6ee）——按模块逐个修而非消除复制源 |
| 生产可用 | ✅ | 修复后默认路径必然产出 dashboard 工件；ledger headline 本就不受影响，无数据损失 |
| 高内聚低耦合 | ⚠️ | 各 phase 编排器复制同一收尾模式，缺共享抽象 |

## 对照参考
该 bug 模式（可选参数 None 被误当"跳过"信号）是典型的 sentinel-value 坏味道；重构为"始终执行 + 显式 base 重定向"与 Python 社区"None 即默认值"惯例一致（python.org docs, argparse/Path 常见实践）。重复出现的复制粘贴缺陷正是 Fowler《重构》"Duplicated Code"条目，标准处置是抽出共性函数或加防回归测试。

## 问题清单
### P2-1 默认路径（base=None）无测试覆盖，同一 bug 三次复发
- 描述：commit message 自述"Same save-skip bug Phase C had"；`tests/test_phase_c_run.py:132`、`tests/test_phase_d_run.py:82`、`tests/test_phase_e1_run.py:70` 全部显式传 `results_base=tmp_path`，`base=None` 的默认落盘路径从未被任何测试走到——这正是该守卫 bug 在 C、D、E1 三个编排器中逐个复发的根因（E1 的同一 bug 在 031e6ee 才修）。
- 位置：`src/aionis/eval/phase_d.py:235-241`（修复处）；测试缺口见 `tests/test_phase_d_run.py:82`
- 影响范围：未来新增 phase 编排器仍可能复制出同款守卫；dashboard 工件静默缺失。
- 修复建议：a) 抽出共享的 run-confirmatory 收尾（save+log）helper；或 b) 增加一条"默认 base 下 run 后工件目录存在"的冒烟断言（可用 monkeypatch 重定向 DEFAULT_RESULTS_DIR 到 tmp 保持 hermetic）。
- 修复成本：中
- 状态：未修复（Phase E1 的同款已在 031e6ee 修复；共享抽象/默认路径测试至今缺位）

## 总体评分
8/10 — 修复本身正确且最小，但只切症状不切根：缺少让这类守卫无法复发的共享收尾或默认路径测试。
