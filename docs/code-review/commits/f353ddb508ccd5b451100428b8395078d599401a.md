# Commit f353ddb508ccd5b451100428b8395078d599401a 审查报告

**提交信息**: ci(pages): use astral-sh/setup-uv + dashboard extra (fix cache:uv failure)（2026-08-01，Rethymus）
**改动范围**: 1 个文件，+4/-6；`.github/workflows/deploy-pages.yml` — 将 actions/setup-python(cache:uv) 换成 astral-sh/setup-uv@v6(enable-cache)，并把 `uv sync --all-extras` 收窄为 `--extra dashboard`（避免拉取 extraction 的 torch/transformers）。

## 四维度结论
| 维度 | 结论 | 一句话说明 |
|---|---|---|
| 稳定性 | ✅ | 修复了 cache:uv 需要 uv 预装的失败路径，改动直击根因 |
| 可扩展性 | ✅ | 只装 dashboard extra，按需装配，避免 CI 拖入重型依赖 |
| 生产可用 | ⚠️ | `uv sync` 未加 `--frozen/--locked`，CI 不校验 uv.lock 一致性（P3） |
| 高内聚低耦合 | ✅ | 单一 workflow 文件，无越界改动 |

## 对照参考
与 Astral 官方推荐一致：`astral-sh/setup-uv@v6` + `enable-cache: true` 是官方 caching 路径；官方 guides 同时建议 CI 用 `uv sync --frozen`（或 `--locked`）断言 lockfile 最新（docs.astral.sh/uv/guides/integration/github、github.com/marketplace/actions/astral-sh-setup-uv）。

## 问题清单
### P3-1 `uv sync` 缺少 `--frozen`/`--locked`
- 描述：`run: uv sync --extra dashboard` — 若 uv.lock 与 pyproject 漂移，CI 会静默重解析依赖而非失败，破坏可复现构建意图（本项目 H6 版本钉死原则在 CI 侧的体现）。
- 位置：`.github/workflows/deploy-pages.yml:34`
- 影响范围：仅 Pages 部署构建；展示层风险低，但与仓库"version-pinned (uv.lock)"纪律不完全一致。
- 修复建议：改为 `uv sync --frozen --extra dashboard`。
- 修复成本：低
- 状态：未修复（后续 c80838a 重写该 workflow 场景已变化，属待人工复核）

## 总体评分
8/10 — 正确且克制的 CI 修复，根因明确、收窄依赖方向正确；仅缺 `--frozen` 加固。
