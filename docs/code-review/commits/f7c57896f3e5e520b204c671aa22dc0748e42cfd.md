# Commit f7c57896f3e5e520b204c671aa22dc0748e42cfd 审查报告

**提交信息**: feat(features): composite builder 3-layer (macro+global+meso) — conditional-IC null under spec regime（2026-08-04，Rethymus）
**改动范围**: 1 个文件，+11/-4；`scripts/build_regime_composite.py` 在 meso 缓存存在时加入第 3 层，否则回退 2 层；commit 信息如实报告 2-layer→3-layer 把 CN 边际交互（p=0.070）稀释为 null 的敏感性发现。

## 四维度结论
| 维度 | 结论 | 一句话说明 |
|---|---|---|
| 稳定性 | ⚠️ | 回退路径无显式开关，合成构成由缓存文件存在性决定 |
| 可扩展性 | ⚠️ | 层按路径探测加载，新层=新 PATH 常量，可接受但无 CLI 化 |
| 生产可用 | ✅ | 探索性定位、CAVEAT 打印、sha256 记录、敏感性结果如实披露（不挑显著的那组） |
| 高内聚低耦合 | ✅ | 复用 `regime_composite` 通用 N 层入口，改动最小 |

## 对照参考
不适用（脚本胶水改动，无新算法逻辑；敏感性披露方式与预注册纪律自洽）。

## 问题清单
### P2-1 合成构成由"缓存文件是否存在"静默决定，且输出同名覆盖
- 描述：`if MESO_PATH.exists(): layers["meso"]=... else: 2-layer`——同一脚本在有无 `regime_meso.parquet` 的环境跑出**构成不同**的 `regime_composite.parquet` 并互相覆盖，仅靠一行 print 区分。本 commit 信息自身就证明了构成的实质影响（CN beta 0.0345/p=0.070 → 0.0155/p=0.361），在以 H6 bit-identical 为铁律的项目里，"构成随环境漂移"的产物是复现性隐患：任何人在缺 meso 缓存的机器上重跑会静默得到 2-layer 产物。
- 位置：`scripts/build_regime_composite.py:51-56`
- 影响范围：`regime_composite.parquet` 及下游 conditional-IC / joint-fold 结果的可复现性。
- 修复建议：加 `--layers {2,3}` 或 `AIONIS_REGIME_LAYERS` 显式参数；或 meso 缺失时直接 fail（既然 3-layer 是冻结规格）；至少把层数写进输出文件名或元数据列。
- 修复成本：低
- 状态：未修复（main 上该脚本此后无改动）

### P3-1 承接 df57a4a 的 skipna 部分可得退化，3 层下加剧
- 描述：三层历史起点不同（macro 2015 起、global/meso 预热更晚），`skipna=True` 使早期与日历错位日期的有效层数在 1/2/3 之间漂移（本 commit 自述 valid 2585→2592 仅 +7，说明 meso 与 2-layer 几乎同窗，但 2015 段仍为 macro 单层主导）。
- 位置：`src/aionis/features/regime_composite.py:74`（本 commit 未改，行为承接）
- 影响范围：regime_state 早期段的有效构成。
- 修复建议：见 df57a4a 报告（min_count 或全层掩码）。
- 修复成本：低
- 状态：未修复

## 总体评分
7/10 — 敏感性发现披露诚实且科学上有价值，但用文件存在性切换构成、同名覆盖输出，与本项目的确定性纪律存在张力。
