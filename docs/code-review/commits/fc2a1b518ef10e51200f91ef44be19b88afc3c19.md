# Commit fc2a1b518ef10e51200f91ef44be19b88afc3c19 审查报告

**提交信息**: feat(dashboard): Streamlit results dashboard + reporting interface（2026-07-28，Rethymus）
**改动范围**: 4 个文件，+1027/-0；新增 `dashboard/app.py`（Streamlit 四 tab）、`src/aionis/reporting/results.py`（save_run/load_run/list_runs + ledger 只读解析）及 217 行测试。

## 四维度结论
| 维度 | 结论 | 一句话说明 |
|---|---|---|
| 稳定性 | ✅ | read_ledger 逐行容错（坏行告警不中断）、list_runs 对损坏 artifact 降级、load_run 缺 meta.json 抛明确 FileNotFoundError |
| 可扩展性 | ✅ | 仿 mlflow 的 `<config_sig>/` 目录布局 + schema 版本号；base 参数支持 hermetic 测试根 |
| 生产可用 | ✅ | 只读展示层，无密钥/写入风险；st.cache_data 合理 |
| 高内聚低耦合 | ✅ | dashboard 只依赖 reporting.results 单一读取面，不触碰 eval/ingest 内部 |

## 对照参考
`@st.cache_data` 装饰数据加载函数与 Streamlit 官方 caching 指南一致（docs.streamlit.io）；run-artifact 目录布局（params.json+binary per run sig）为 mlflow（Apache-2.0）tracking 布局的简化复用，docstring 已注明不引依赖。plotly `add_hrect/add_hline` 用法符合官方 API（plotly.com/python）。

## 问题清单
### P3-1 `_pick_run` 为死代码，与 `main()` 内联逻辑重复
- 描述：`_pick_run()` 定义后从未被调用（`main()` 在 sidebar 中重新实现了同样的「有真实 run 则取第一个，否则合成」逻辑），两处逻辑将来易发散。
- 位置：`dashboard/app.py:139-145` 与 `dashboard/app.py:349-358`
- 影响范围：仅维护性；无运行时影响
- 修复建议：`main()` 改为调用 `_pick_run()`，或删除该函数
- 修复成本：低
- 状态：待人工复核（后续 dashboard 大改中该函数去留需人工确认）

### P3-2 `_diff_chart` 假设两臂 IC 序列索引完全一致
- 描述：`diff_series = run["ic_state"] - run["ic_base"]` 依赖两 Series 索引相同；若某月在单臂被 dropna 掉，pandas 按索引并集对齐产生 NaN 段，图中出现断线（不崩溃，均值/CI 来自 differential.json 不受影响）。
- 位置：`dashboard/app.py:280`
- 影响范围：仅展示层图表完整性
- 修复建议：先 `reindex` 到交集并 dropna，或按 ledger 差分口径（index.intersection）取交集
- 修复成本：低
- 状态：待人工复核

## 总体评分
9/10 — reporting 接口设计干净（容错读取 + sig 目录 + ledger 对账），dashboard 纯展示零泄漏；仅存死代码与图表对齐两处小瑕疵。
