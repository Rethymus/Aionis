"""Aionis MVP — autonomous financial world model.

This package implements a single falsifiable experiment:

    Do structured event representations (ERL) extracted from scheduled
    macro events (FOMC / CPI / NFP) carry information that improves
    sector-ETF return prediction beyond a price-only baseline?

See README.md for the alignment rule and pre-registered primary metric.
"""

__version__ = "0.1.0"
